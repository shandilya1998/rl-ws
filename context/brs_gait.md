# brs_gait.md — SD_BRS1 gait quality investigation (crouch, asymmetry, torso lean)

Created 2026-07-17. This file records the code grounded facts behind the gait improvement strategy in /ws/plans/GAIT_STRATEGY.md, for the run sd_brs1_flat/2026-07-15_06-59-45 whose final video shows a deeply crouched, forward pitched, occasionally sideways rolled shuffle with almost no foot lift.

Artefact retention note, added 2026-07-30. Of the fifteen runs cited in this document only sd_brs1_flat/2026-07-27_07-19-39 survives under `/ws/IsaacLab/logs/rsl_rl/`. The videos and numpy dumps of every earlier run have been deleted, so the file pointers that once accompanied the passages below were removed on that date. The observations they supported are retained as the record of the original reading, and the exported TensorBoard panels that do survive are held in `artefacts/`, indexed by `artefacts/README.md`.

## Video evidence (frames extracted at 2 fps with imageio, 2001 frames, 20 s, 100 fps render)

The robot progresses along the commanded heading but with the torso pitched forward by roughly 40 to 70 degrees, knees deeply flexed, feet dragged at ground level, and intermittent lateral roll, one robot in frame 8 appears fully collapsed while the episode continues, confirming that the termination set does not catch bent over postures.

## Run configuration versus current working tree

The dumped params/env.yaml of the recorded run differs from the working tree only in reward weights, the run used pen_flat_orientation -1.0, pen_base_height -5.0, rew_foot_clearance 2.0, pen_joint_torque -8e-5, pen_joint_accel -5e-7, pen_joint_vel_l2 -5e-5, and the tree now holds -5.0, -15.0, 20.0, -1e-5, -1e-7, -1e-5 respectively, all other terms are identical (rew_lin_vel_xy 50, rew_ang_vel_z 15, no_fly 4.5, keep_ankle_pitch_zero_in_air 1.0, feet_air_time 500 with threshold_min 0.2 and threshold_max 0.5, feet_slide -5, pen_feet_distance -100, feet_regulation -0.2, keep_balance 0.05).

## Kinematic facts (from SD_BRS1_Assembly2.urdf, cross checked with sd_brs_urdf.md and BRS.md)

1. 59.85 kg total, thigh 0.44 m, shank 0.43 m, hip cluster 0.181 m below the torso frame, foot sole plate bottom 0.124 m below the Link6 (ankle) frame, so the straight leg standing height of the torso frame is about 1.175 m, not the 1.3 m used as init_state.pos nor the 1.1 m the init_state comment claims.
2. Joint limits, HipRoll ±0.349, HipPitchR (-0.75, 1.25) about axis (0,-1,0), HipPitchL (-1.25, 0.75) about (0,1,0), KneePitch (0, 1.483) about (0,1,0) both legs, AnkleRoll ±0.454 about (-1,0,0), AnklePitch (-0.349, 0.75) about (0,-1,0) both legs. Hip pitch axes are mirrored between legs, knee and ankle pitch axes are not.
3. Mirror map for symmetry training in the IsaacLab DOF order [HipRollR, HipRollL, HipPitchR, HipPitchL, KneePitchR, KneePitchL, AnkleRollR, AnkleRollL, AnklePitchR, AnklePitchL], swap each R and L pair, then negate HipRoll, HipPitch, and AnkleRoll pairs, and keep the sign of KneePitch and AnklePitch pairs, this follows from the reflection algebra about the sagittal plane applied to each joint axis and is consistent with the mirrored reset ranges in brs_base_env_cfg.py (HipPitchR (-0.25, 0.42) versus HipPitchL (-0.42, 0.25)).
4. The default joint pose is all zeros (straight legs), which places the knee exactly on its hard lower limit 0 and outside its soft limit band (soft_joint_pos_limit_factor 0.9 gives a soft knee range of about (0.074, 1.409)), so the nominal pose is permanently penalised by pen_joint_pos_limits and sits at the straight knee kinematic singularity.
5. The base height target 1.1 m demands roughly 0.84 rad of knee flexion (hip to ankle distance 0.795 m with l1 0.44, l2 0.43), an upright crouch with hip and ankle pitch near 0.42 rad each, all within soft limits, a taller target of 1.14 m needs only about 0.57 rad of knee and looks far more human.

## Actuator facts (sd_brs1_identified_cfg.py, inertias from BRS.md section 4)

Gains at the time of the recorded run were hip roll K 150 D 5, hip pitch K 200 D 5, knee K 200 D 5, ankle roll K 20 D 4, ankle pitch K 20 D 4 (superseded in the working tree, see the status update section below). With the BRS.md effective inertias (hip pitch 6.494, hip roll 6.499, knee 1.272, ankle pitch 0.039, ankle roll 0.033 kg m^2) the damping ratios are hip pitch 0.069, hip roll 0.080, knee 0.157, ankle pitch 2.26, ankle roll 2.46, so the proximal joints are drastically underdamped and the ankles overdamped. BRS.md recommended D 40 to 70 for hip pitch, 40 to 75 for hip roll, 15 to 25 for knee at these stiffness levels, and its TRON1 effort scaling argument supports ankle pitch K up to about 65. The stiffness recommendations of BRS.md were adopted into the config but the damping recommendations were not.

## Reward function defects found (brs_base_env_cfg.py and mdp/rewards.py)

1. rew_foot_clearance target_height 0.12 is below the standing height of the Link6 body frame (about 0.124 m, the frame sits at the ankle, the sole extends 0.124 m below it), so the term is maximised by swinging the foot fast at ground level, it actively rewards the observed fast flat shuffle, and raising its weight from 2 to 20 amplifies the defect. The comment in the config assumes a 0.03 m foot radius geometry that belongs to TRON1, not to BRS. A correct target for 8 cm of sole clearance is about 0.20 in Link6 frame height.
2. feet_air_time (custom variant) pays (last_air_time - 0.2) only on first contact, clamped above at 0.3, at weight 500, so every exploratory short step costs up to -100 raw (-1.0 per step after dt scaling) while never stepping costs exactly zero, a textbook negative barrier that locks in the no lift local optimum. The IsaacLab biped variant feet_air_time_positive_biped is dense, non negative, and encodes single stance directly.
3. keep_ankle_pitch_zero_in_air returns its maximum value 1.0 whenever both feet are in contact (the weighted sum is empty), so at weight 1.0 it pays a small permanent bonus for keeping both feet planted.
4. feet_regulation uses base_height_target 1.1 as the exponential length scale, exp(-h/1.1) is essentially constant (about 0.9) over the whole 0 to 0.15 m foot height band, so the term penalises swing foot velocity almost as strongly as ground level foot velocity and fights the clearance and air time terms, the commented out original used 0.025 times the desired height as the scale, which is the correct order.
5. pen_flat_orientation even at the new weight -5 contributes at most about -0.05 per step at a 45 degree lean while the two tracking terms contribute up to +0.65 per step, the lean therefore buys more tracking reward than it costs in orientation penalty, the run that produced the video had it at -1.
6. There is no termination on orientation and low_height terminates only below 0.5 m, half the nominal height, so bent over and collapsed postures remain alive and keep collecting tracking reward, isaaclab.envs.mdp.terminations.bad_orientation exists and is already importable through the project mdp package.
7. The curriculum reduce_tracking_rewards_std drives the linear tracking std down to 0.09, an extremely sharp kernel that further raises the value of leaning into the command against a weak posture penalty.
8. No symmetry mechanism exists anywhere in the stack for this task, no mirror loss, no augmentation, no phase variable, and the commands are slow (lin_vel_x -0.1 to 0.5 before iteration 6000), slow commands are known to favour shuffling gaits.
9. GaitReward in mdp/rewards.py is a complete Walk These Ways style phase clock reward (force and velocity shaping against desired contact states from frequency, offset, duration parameters) but is unused and requires a gait parameter command term to drive it.
10. The vendored rsl_rl PPO at /ws/rsl_rl supports symmetry_cfg with use_data_augmentation and use_mirror_loss (Mittal et al. 2024 interface, RslRlSymmetryCfg in isaaclab_rl), so symmetry training is plumbing work only.

## DataLogger note

scripts/rsl_rl/play.py now logs feet contact forces and feet velocities through the module global FEET_LINK_NAMES, which defaults to "ankle_.*" (TRON1), any BRS evaluation must set it to "Link6[LR]".

Extended on 2026-07-20 with feet heights, so that the discriminating test of the ninth pass can actually be run. Two keys are written, feet_frame_heights, the body frame origin height that foot_clearance_reward reads, and feet_sole_clearances, the lowest world height of the sole points that foot_clearance_reward_v2 reads, computed by DataLogger._sole_clearance which carries the v2 batched transform verbatim. The sole table is a second module global SOLE_OFFSETS, defaulting to None, under which only the frame height is logged and the clearance key stays empty, so TRON1 and every existing caller are unaffected. The SD_BRS1 twelve point table is provided beside it as SD_BRS1_SOLE_OFFSETS and must be assigned to SOLE_OFFSETS together with FEET_LINK_NAMES = "Link6[LR]". Logging both heights is deliberate, their difference is the tilt that the frame height alone cannot distinguish from a lift, and the geometry was re-verified this pass, a level foot at frame height 0.204 measures exactly 0.080 of clearance, and a grounded foot measures 0.000 at pitches of 0, 20, 30, 40 and 45 degrees while its frame stands at 0.1240, 0.1690, 0.1860, 0.1973 and 0.2007 respectively, reproducing the fifth pass figures.

The signature to look for in the dump, restating the ninth pass test in terms of the logged keys, is a nonzero feet_contact_forces magnitude on a foot whose feet_sole_clearances sits near 0.08, which is forged contact through self collision since a genuinely grounded sole measures at most 0.7 mm, and nonzero force on both feet during the ballistic phases identified by base vertical velocity.

## Dashboard note

/ws/IsaacLab/logs/rsl_rl/dashboard-brs.py gained a Feet tab, create_feet_plot, gathering all four per foot channels into one window, a six row by two column grid with one column per foot and rows for contact force magnitude, contact force z, horizontal foot speed, vertical foot velocity, sole clearance and body frame height, with dotted reference lines at the 1.0 N contact threshold and the 0.08 m clearance target. Reading the rows against each other is the point, force against clearance exposes the forged contact and frame height against sole clearance exposes tilt. The feet order is FEET_NAMES = ["Link6R", "Link6L"], since find_bodies matches on body index order rather than regex order and the URDF declares the right leg chain first, the same convention as JOINT_NAMES. CORRECTED 2026-07-31, see the twentieth pass. That order is WRONG in both lists, the articulation enumerates left before right at every depth and the URDF declaration order is not the tie-break, so find_bodies("Link6[LR]") resolves as Link6L then Link6R. Both lists have since been corrected and demoted to fallbacks behind the names that play.py now records in the dump.

One pre-existing defect was fixed to make this possible. get_env_data handled only two and three dimensional dumps, so the per body quantities that stack to four dimensions (Time, Env, Body, Component), namely feet_contact_forces and feet_velocities logged since the earlier pass, silently returned an empty array and could never be plotted. A four dimensional branch returning data_np[:, env_id] was added ahead of the existing branches, which is purely additive since that case previously fell through to the empty return. Dumps predating the new keys still load, missing keys are skipped by the existing key check and an empty clearance list resolves to an empty array through the existing list handling.

## Status update after user feedback (2026-07-17, second pass)

1. Phase 0 of ../plans/GAIT_STRATEGY.md is now applied in the working tree, sd_brs1_identified_cfg.py reads hip roll K 150 D 45, hip pitch K 200 D 50, knee K 200 D 22, ankle pitch K 50 D 4, ankle roll K 20 D 4, and brs_base_env_cfg.py reads target_height 1.14 in pen_base_height and base_height_target 1.14 in feet_regulation. The resulting damping ratios are hip pitch 0.694, hip roll 0.721, knee 0.690, ankle pitch 1.43, ankle roll 2.46, so the proximal joints are on the ζ 0.7 target and both ankles remain overdamped, the residual recommendation is ankle pitch D ≈ 2.0 and ankle roll D ≈ 1.1. Phase 1 (reward and termination table) is not yet applied, feet_air_time is still the sparse variant at 500, clearance target still 0.12, flat orientation still -5, low_height still 0.5, minimum_std still 0.09.
2. The default joint pose stays all zeros by deliberate user decision. The user widened the reset joint randomisation (reset_joint_by_offset events, offsets sampled around the default and clamped to soft limits) specifically because earlier runs moved the hips and knees barely at all, and the widening improved training up to the current tracking-with-bad-gait state. The strategy document section 2.4 was rewritten to separate the three roles of the default pose, initial state distribution centre (randomisation keeps this covered, analogue of DeepMimic reference state initialisation, arXiv:1804.02717), zero action attractor of JointPositionActionCfg with use_default_offset=True (unaffected by randomisation, still the straight leg singular pose on the knee hard limit), and geometric reference for joint_pos_rel and the soft limit penalty. The monitored risk protocol, if stance knee angles still collapse toward zero in the new baseline's DataLogger dump, move the action offset to the stance pose and recentre the reset ranges so the initial state distribution is unchanged.
3. Section 2.2 of the strategy document was expanded into a full mechanistic account of the air time barrier with the per touchdown payoff table r(τ) = 5(τ − 0.2) clamped at +1.5, the three property barrier argument (event sparsity, negatively priced exploration region, degenerate gradient direction of suppressing lifts), the quadruped origin explanation (Rudin et al. arXiv:2109.11978, threshold 0.5, shallow barrier because quadrupeds keep static stability with one leg lifted), the van Marum section IV.B cross evidence, the Ng, Harada, Russell ICML 1999 policy invariance framing, and a step by step account of why feet_air_time_positive_biped (dense, non negative, single stance gated, min over feet of mode time, clamp at threshold) inverts the incentive from the first instant of weight transfer. Section 2.5 was rewritten with the explicit second order formulas and per joint tables for the old gains, overshoot 80 percent and 2.6 s ring envelope at hip pitch against a 0.3 to 0.4 s swing, and for the phase 0 gains.

## Reward scale conversion basis and corrected reference ratios (2026-07-17, third pass)

A references section was added to ../plans/GAIT_STRATEGY.md as section 7, and verifying it against the live sources exposed three numerical errors in the earlier drafts of that document, all now corrected. The errors and the principle that resolves them are recorded here so they are not reintroduced.

The principle. RewardManager.compute evaluates value = func × weight × dt (IsaacLab/source/isaaclab/isaaclab/managers/reward_manager.py:144), so for any reward function saturating at 1 the configured weight is exactly the reward rate per second, and weights are directly comparable across configurations regardless of control rate. This task pays linear tracking at 50 per second, the IsaacLab G1 reference pays 1.0, and van Marum pays 0.15 plus 0.15 for its two velocity components, so G1 weights map onto this task by a factor of 50 and van Marum weights by a factor of about 167. Event terms that fire once rather than continuously (is_terminated) are exempt from the rate reading and must be converted through forfeited income, since for them the dt multiplication yields a raw penalty of weight × dt.

Error 1, corrected. Earlier drafts claimed the G1 config sets flat orientation at half its linear tracking weight. It does not, G1 sets flat_orientation_l2 to -1.0 (rough_env_cfg.py:135) against track_lin_vel_xy_yaw_frame_exp at 1.0 (rough_env_cfg.py:24), that is parity, and it is half the ANGULAR tracking weight of 2.0. The van Marum ratio is roll and pitch orientation 0.2 against combined linear velocity 0.3, that is two thirds. The implied bracket for pen_flat_orientation here is therefore -33 (van Marum) to -50 (G1 parity), not the -25 previously proposed, and the phase 1 table now recommends -35 as the conservative entry point. The section 2.3 claim that -5 is "a factor of four to ten below" the references is now stated as roughly seven to ten times below.

Error 2, corrected. The phase 1 table proposed is_terminated at -200 "as in G1", copying the literal reference weight across a 50x scale gap. At this task's dt of 0.01 that yields a raw penalty of 2.0, about three hundredths of a second of tracking income, effectively a no op. The correct conversion, G1's -200 at dt 0.02 is a raw penalty of 4.0 against G1's combined tracking rate of 3.0 per second, hence about 1.3 s of forfeited ideal income and about 6.7 percent of a perfect 20 s episode. Reproducing that here (combined tracking rate 65 per second, dt 0.01) needs a raw penalty near 85, hence weight about -8000, which cross checks at about 6 percent of a perfect episode. Recorded caveat, a -85 single step spike against a typical 0.65 per step reward may inflate value loss variance, fall back to -4000 if Loss/value_function degrades.

Error 3, corrected. The proposed joint_deviation_l1 weight of -2.5 did not follow from any reference, the G1 ratio is 0.1 against tracking 1.0 (rough_env_cfg.py:57), which maps to -5 here. Also corrected, the rew_no_fly rationale previously said the current 4.5 is "seven times weaker" than van Marum, the correct figure is about four times weaker, van Marum prices feet contact at 0.1 against 0.3 combined linear velocity, one third, which maps to about 16 here, and the proposal of 15 stands.

Verified as already correct and unchanged, feet_air_time_positive_biped at 12.5 (G1 ratio 0.25 × 50), pen_base_height at about -10 (van Marum 0.05/0.3 × 50 ≈ 8), and feet_slide at the existing -5 (G1 ratio 0.1 × 50), which the config already matches.

All line numbers cited in ../plans/GAIT_STRATEGY.md section 7 were verified against the working tree on 2026-07-17. Confirmed anchors, project rewards.py feet_air_time:49 foot_clearance_reward:87 no_fly:228 keep_ankle_pitch_zero_in_air:240 feet_regulation:344 GaitReward:424, brs_base_env_cfg.py ActionsCfg:113 use_default_offset:120 RewardsCfg:629 low_height:747, events.py reset_joint_by_offset:350, play.py FEET_LINK_NAMES:100, IsaacLab contact_sensor.py compute_first_contact:182, velocity mdp rewards.py feet_air_time_positive_biped:49, terminations.py bad_orientation:51, envs/mdp/rewards.py is_terminated:37, manager_based_env.py step_dt:236 returning sim.dt × decimation (0.01 s here against 0.02 s for G1), rsl_rl ppo.py symmetry_cfg:46, symmetry_cfg.py RslRlSymmetryCfg:12.

Citation records verified against arXiv and publisher pages on 2026-07-17, Rudin CoRL 2021 arXiv:2109.11978, van Marum arXiv:2404.19173, Siekmann ICRA 2021 arXiv:2011.01387, Margolis and Agrawal CoRL 2022 arXiv:2212.03238 (the GaitReward class is this Walk These Ways variant, identifiable from its verbatim parameter names), Mittal ICRA 2024 arXiv:2403.04359, Yu Turk Liu TOG 37(4) art 144 arXiv:1801.08093, Ng Harada Russell ICML 1999 pp 278-287, Peng DeepMimic TOG 37(4) arXiv:1804.02717 (reference state initialisation is in the body, not the abstract), Peng AMP TOG 40(4) arXiv:2104.02180, Tang HumanMimic ICRA 2024 arXiv:2309.14225, Peng Bao Zhou arXiv:2505.20619, Ha et al IJRR 44(8) 2025 doi 10.1177/02783649241312698.

## Phase 1 partial application and the foot_radius discovery (2026-07-17, fourth pass)

Three phase 1 rows were applied to the code on request, keep_ankle_pitch_zero_in_air, feet_regulation, and the lin_vel_x command range, while the user independently applied several other rows. The changes and the findings that came out of validating them are recorded here.

Coding rule established by the user, now in /ws/CLAUDE.md as the Code change instructions section and in Claude's memory. Evaluate the options before changing code, write as little code as possible, and never break backwards compatibility. Where an optional argument can carry the new behaviour without altering existing logic, prefer the optional argument with a default reproducing the old behaviour, and set it explicitly only in the calling config, which also records it in that run's dumped params/env.yaml. Reserve a v2 of a method for cases where no optional argument can preserve the old behaviour. This matters here because the mdp package is shared, keep_ankle_pitch_zero_in_air is called by brs_base_env_cfg.py and SF/limx_base_env_cfg.py:1149, and feet_regulation by brs_base_env_cfg.py, SF/limx_base_env_cfg.py:1206 (base_height_target 0.75) and PF/limx_base_env_cfg.py:398 (base_height_target 0.65).

Code changes made, both backwards compatible and verified numerically to reproduce the original formulas when the new arguments are omitted.
1. mdp/rewards.py keep_ankle_pitch_zero_in_air gained require_airborne, default False. When True the reward is multiplied by an any-foot-airborne indicator. Verified, both feet planted returns 1.0 under the default (the standing bonus defect) and 0.0 with the flag, one foot airborne and level returns 1.0 under both, one foot airborne pitched 0.5 rad returns 0.0821 under both.
2. mdp/rewards.py feet_regulation gained height_decay_scale, default None meaning base_height_target, which is the original behaviour. BRS passes 0.03.
3. brs_base_env_cfg.py sets require_airborne True, height_decay_scale 0.03, foot_radius 0.124, and lin_vel_x (-0.3, 0.8).

The foot_radius discovery, which the strategy document had missed. feet_regulation converts body height to ground clearance through an explicit foot_radius, and BRS passed the TRON1 point foot value 0.03 while its sole sits 0.124 m below the Link6 frame. This was verified directly against the collision meshes, trimesh reports z_min exactly -0.1240 for both meshes/Link6R.STL and meshes/Link6L.STL in the link frame, matching the primitive URDF sole box bottom (-0.1010 origin minus half of the 0.046 box height) and reconciling with the chain 0.181 + 0.44 + 0.43 + 0.124 = 1.175 m. Also confirmed, IsaacLab body_pos_w is body_link_pos_w (articulation_data.py:1056), the link frame and not the CoM, so 0.124 is the right constant for both feet_regulation and foot_clearance_reward. The two feet_regulation parameters are coupled, changing the scale to 0.03 while leaving foot_radius at 0.03 makes a grounded foot read 0.094 m of clearance and evaluates exp(-3.13) = 0.044, retaining only 4 percent of the weight and effectively disabling the term. Measured penalties at 1 m/s foot speed, original (r 0.03, s 1.14) grounded 0.921 and 8 cm swing 0.858 for a selectivity of 1.07, scale alone (r 0.03, s 0.03) grounded 0.044, both (r 0.124, s 0.03) grounded 1.000 and 8 cm swing 0.070 for a selectivity of 14.4. Audit note, foot_landing_vel carries the same foot_radius parameter and would need 0.124 were it enabled for BRS, it is currently commented out in the SF limx config and absent from the BRS config.

Working tree versus the running experiment sd_brs1_flat/2026-07-17_04-51-50, which is effectively phase 0 only. The run has rew_no_fly 4.5, pen_flat_orientation -5.0, the sparse feet_air_time at 500 with thresholds 0.2 and 0.5, rew_foot_clearance target 0.12 std 0.025, feet_regulation foot_radius 0.03 with no decay scale, keep_ankle ungated, low_height 0.5, lin_vel_x (-0.1, 0.5), and the phase 0 gains (hip roll K150 D45, hip pitch K200 D50, knee K200 D22, ankle pitch K50 D4, ankle roll K20 D4). The working tree now additionally has rew_no_fly 15, pen_flat_orientation -35, feet_air_time_positive_biped 12.5 threshold 0.4, rew_foot_clearance target 0.20 std 0.035, low_height 0.65, plus the three changes of this pass. Still unapplied from the phase 1 table, bad_orientation termination, is_terminated penalty, joint_deviation_l1, pen_base_height -10, and minimum_std 0.20.

Stale comment left standing, brs_base_env_cfg.py above rew_foot_clearance still reads "~0.07 m ground clearance plus 0.03 m foot radius" although target_height is now 0.20, the arithmetic it describes belongs to the superseded TRON1 geometry. Not changed since it carries no behaviour, flagged for the user.

## Root cause of the shuffle, the tilted foot clearance exploit (2026-07-17, fifth pass, run 2026-07-17_07-49-02)

The run 2026-07-17_07-49-02 carries phase 0 plus most of phase 1 (clearance target 0.20 std 0.035, feet_air_time_positive_biped 12.5 threshold 0.4, no_fly 15, flat_orientation -35, low_height 0.65, keep_ankle require_airborne True, feet_regulation foot_radius 0.124 scale 0.03, lin_vel_x (-0.3, 0.8)). At iteration ~2500 the posture is fixed (pen_flat_orientation only -0.376 against a -35 weight implies about 6 degrees of lean) and tracking is fast and high, but the shuffle persists and rewards have plateaued. The root cause is now established and is a reward hacking exploit rather than an optimisation failure.

Useful identity for reading these plots, Episode_Reward/x = episode_sum / max_episode_length_s, and for a term held at value v with weight w over a full episode this equals v × w, so every logged Episode_Reward is a reward RATE per second and the logged numbers invert directly into physical quantities.

The decisive evidence, from /ws/context/artefacts/plots-latest at iteration ~2570.
1. rew_no_fly = -0.033 at weight 15 implies a mean no_fly value of -0.0022, and since no_fly = 1×single_contact - 5×no_contact, both single stance and flight are essentially zero, the robot is in DOUBLE SUPPORT about 99.9 percent of the time.
2. feet_air_time = 2e-5 against a theoretical max of 5.0, confirming single stance essentially never occurs, feet_air_time_positive_biped is gated on exactly one foot in contact so it pays nothing.
3. rew_keep_ankle_pitch_zero_in_air = 1e-4, the require_airborne gate added this pass is working correctly and is reporting that no foot is ever airborne.
4. rew_foot_clearance = 10.07 and still climbing, out of a theoretical max of 40 (it sums over both feet, 2 × 1.0 × 20). Substantial clearance reward is being earned while no foot ever leaves the ground.

Points 1 and 4 are only jointly possible if the clearance reward is being collected without lifting a foot. The mechanism, verified two independent ways. foot_clearance_reward measures asset.data.body_pos_w[Link6].z, the LINK FRAME origin height, and the Link6 frame sits at the ankle while the sole plate is 0.3156 m long and 0.19 m wide, so tilting the foot about the ankle raises the frame while the sole edge stays on the ground. Computed over the collision mesh Link6R.STL (47857 vertices, rotating the vertex cloud and taking the lowest point), the frame height while still touching the ground is 0.124 m flat, 0.170 m at 20 degrees of pitch, 0.186 m at 30 degrees, 0.197 m at 40 degrees, 0.201 m at 45 degrees, with a maximum of 0.2245 m at 48 degrees of pitch and -30 degrees of roll. The configured target_height of 0.20 m with std 0.035 is therefore hit at kernel 0.994 to 0.999, essentially the maximum, by a foot resting on its toe or heel edge at about 40 to 45 degrees. Independently, inverting the logs, feet_slide -3.36 at weight 5 gives a mean per foot contact speed of 0.336 m/s, and feet_regulation -0.0098 at weight 0.2 then implies a height gate of 0.217, hence h = frame_z - 0.124 = +0.046 m and a frame height of 0.170 m while in contact, which corresponds to about 20 degrees of foot pitch. A flat grounded foot would give h = 0 exactly, so h > 0 while in contact is direct log side proof that the feet are tilted.

The behaviour is therefore skating on tilted feet, the policy rocks both feet onto their edges to about 20 to 45 degrees, which reads as 0.17 to 0.20 m of "clearance" to the reward, slides them along the ground at about 0.34 m/s each to satisfy the tanh speed gate of the clearance term, and translates without ever entering single stance. It secures 87 percent of linear tracking (43.5 of 50) and 74 percent of angular tracking with no stepping at all.

Why the term worked on TRON1 and fails here. TRON1 has a point foot of radius 0.03, and a sphere's centre height equals its clearance under any orientation, so the link frame height is an exact proxy. The BRS sole foot breaks that assumption, pure tilt moves the frame by up to 0.10 m, comparable to the whole intended swing clearance, so the proxy cannot distinguish lifting from tilting. This is the same family as the foot_radius defect but deeper, it is not a wrong constant but a wrong measured quantity, and no choice of target_height repairs it, 0.12 rewarded ground sweeping and 0.20 rewards toe rocking.

Regression introduced by the feet_regulation change of the previous pass, recorded honestly. The height gate now releases exactly where the exploit lives, gate values on a grounded foot are 0.921 old versus 1.000 new when flat, but 0.884 old versus 0.216 new at the observed 20 degrees, 0.872 versus 0.127 at 30 degrees, and 0.864 versus 0.088 at 40 degrees, so the anti slide brake on a tilted grounded foot was weakened by 4x to 10x. The change is correct for a flat foot and wrong for a tilted one, for the same reason as the clearance term, it derives clearance from the frame height. Both terms need the true sole clearance, not the frame height.

Reward budget at iteration 2570, as rates per second. rew_lin_vel_xy +43.5, rew_ang_vel_z +11.1, rew_foot_clearance +10.1, keep_balance +0.04, feet_air_time +0.00002, rew_no_fly -0.03, feet_slide -3.36, pen_undesired_contacts -2.69, pen_action_smoothness -2.06, pen_joint_torque -0.44, pen_flat_orientation -0.38, pen_action_rate -0.19, pen_base_height -0.12, pen_feet_regulation -0.01, total about +55.4. The gait shaping terms that should produce stepping (no_fly max 15, feet_air_time max 5) are together worth at most 20 per second and are currently paying zero, while rew_foot_clearance pays 10.1 per second for the tilting that prevents stepping, and it can pay up to 40 because it sums over both feet with no alternation requirement. Loss/entropy is falling steadily (11.5 to 9.45) and the adaptive learning rate has collapsed to 1e-4, so the policy is converging into this optimum and exploration is shrinking, which matches the observed plateau.

Recommended fixes, not yet applied, ordered. First, measure true sole clearance rather than frame height, either by gating the clearance reward on the foot not being in contact (minimal, an optional sensor_cfg argument, a grounded foot then pays zero at any tilt) or by transforming the four sole corners through body_quat_w and taking their minimum world height (principled and tilt invariant), and apply the same clearance quantity to feet_regulation's gate. Second, add a feet level term, van Marum's Table I carries exactly this as Feet orientation at weight 0.05 against 0.3 combined linear velocity, which maps to about 8 at this scale, so that tilting is not free, note keep_ankle_pitch_zero_in_air cannot serve this role because it only acts in the air. Third, rebalance, clearance at a max of 40 per second dwarfs no_fly at 15 and feet_air_time at 5, so the loudest gait signal is the most easily hacked one, it should not exceed the stepping terms. Fourth, sliding remains profitable, feet_slide is -3.36 and growing more negative through training, meaning the policy is learning to slide more, not less.

## Contact gate on foot_clearance_reward, implemented and its residual quantified (2026-07-17, sixth pass)

Implemented per the backwards compatibility rule as an optional argument, not a v2. mdp/rewards.py foot_clearance_reward gained sensor_cfg (SceneEntityCfg | None = None) and force_threshold (float = 1.0). When sensor_cfg is given, the per foot reward is multiplied by ~in_contact, where in_contact is the history max of the net contact force norm above force_threshold, the same idiom as feet_slide (rewards.py:81), chosen so contact chatter cannot flicker a grounded foot into earning. The default None reproduces the original ungated behaviour exactly. Only brs_base_env_cfg.py calls this function (the analysis script foot_clearance_analysis.py carries its own numpy replica and does not import it), but the optional argument was still used so the term's history stays comparable and the choice is recorded in each run's dumped params/env.yaml. The BRS config now passes sensor_cfg SceneEntityCfg("contact_forces", body_names="Link6[LR]") and force_threshold 1.0. The body order pairing between asset_cfg and sensor_cfg is safe because SceneEntityCfg.preserve_order defaults to False so both resolve in index order, and feet_slide already relies on exactly this pairing. The stale TRON1 comment above the term ("~0.07 m ground clearance plus 0.03 m foot radius") was replaced with the correct 0.124 m frame offset arithmetic.

Verified effect, per foot reward rate at weight 20. Grounded flat sliding at 0.34 m/s, ungated 0.06 gated 0.00. Grounded tilted 20 degrees (the observed state), ungated 3.10 gated 0.00. Grounded tilted 40 degrees, ungated 6.51 gated 0.00. Grounded tilted 45 degrees, ungated 6.54 gated 0.00. Genuine swing at 0.20 m and 0.6 m/s, ungated 10.74 gated 10.74, unchanged. Whole term rates, the toe rock exploit with both feet grounded and tilted 40 degrees paid 13.03 per second ungated against 10.74 per second for a real single leg swing, so the exploit was strictly more profitable than walking (1.21x) while being easier and more stable, and the gate takes it to exactly 0 while leaving the real swing untouched.

Residual exploit surviving the contact gate, quantified and important. The gate forces lift off but the reward still reads the FRAME height, so tilt still buys clearance. With target 0.20 and std 0.035 the kernel exceeds 0.9 when the frame lies within +/-11.4 mm of 0.20, and the genuine sole clearance that satisfies this is 6.5 to 8.7 cm for a level foot, 1.9 to 4.2 cm at 20 degrees of tilt, 0.3 to 2.5 cm at 30 degrees, and 0.0 to 1.4 cm at 40 degrees. A tilted toe flick that barely breaks contact therefore still scores near maximum. The contact gate is a large improvement and is the correct first move, since it forces lift off and thereby unlocks no_fly (+15 per second for single stance) and feet_air_time, but it is not a complete fix. Completing it requires either measuring the true sole clearance (transform the four sole corners, offsets x -0.1376 and +0.178, y +/-0.095, z -0.124 in the Link6 frame, through body_quat_w and take the minimum world z, which is exactly tilt invariant) or adding a feet level term so that the foot is held flat and frame_z = 0.124 + sole clearance holds identically, which makes the 0.20 target mean 7.6 cm of genuine clearance. van Marum Table I carries the feet level term as Feet orientation at weight 0.05 against 0.3 combined linear velocity, mapping to about 8 at this scale. Note keep_ankle_pitch_zero_in_air cannot serve this role, it acts only in the air and only on ankle pitch, not on the foot's world orientation.

Secondary finding worth investigating, pen_undesired_contacts. It logs -2.69 per second at weight -0.5, implying a mean of about 5.4 bodies in undesired contact, which is high. The term counts contacts on Link[1-4][LR] and Part_Torso through net_forces_w, and net_forces_w registers self collisions as well as ground contacts because the asset spawns with self_collision True and enabled_self_collisions True. The hips sit at y +/- 0.13 and the thigh box is 0.21 m wide, so the thighs clear each other by only 5 cm at neutral hip roll, and sd_brs_urdf.md records thigh to thigh contact as inherent to the hardware geometry at hip roll extremes. The penalty may therefore be partly charging the robot for bringing its legs together, which narrow stance walking requires. Not yet confirmed, the 5.4 count is also consistent with fallen robots dragging their links, the discriminating check is to log the per body contact counts during a play run and see whether Link3 (thigh) contacts occur while the robot is upright.

## foot_clearance_reward_v2, true sole clearance (2026-07-17, seventh pass)

Implemented as a v2 rather than an optional argument, correctly per the ../CLAUDE.md rule, because the change alters what target_height MEANS (a sole clearance rather than a body frame height), and no optional argument can carry a semantic change to an existing parameter without inviting silent misconfiguration. v1 is left untouched and still exported, no config calls it now, and the analysis script foot_clearance_analysis.py carries its own numpy replica so it is unaffected. mdp/__init__.py does `from .rewards import *` and rewards.py declares no __all__, so the new name resolves as mdp.foot_clearance_reward_v2 with no plumbing.

Signature, foot_clearance_reward_v2(env, asset_cfg, target_height, std, tanh_mult, sole_offsets, sensor_cfg=None, force_threshold=1.0). It transforms sole_offsets through body_quat_w (which is body_link_quat_w, the link frame, articulation_data.py:1061), adds body_pos_w, and takes the minimum world z over the points as the clearance, which is exactly tilt invariant. The contact gate is retained as an optional redundancy. target_height is now the SOLE clearance, set to 0.08 in the BRS config, matching the intent of the previous frame target of 0.20 which for a level foot meant 0.20 - 0.124 = 0.076 m.

Sole geometry, measured from the collision mesh rather than taken from the primitive URDF, which matters. The primitive URDF sole box spans x -0.1376 to 0.178 and y +/-0.095, but the real mesh bottom face spans x -0.1091 to 0.1521 and y +/-0.0970 at z -0.124, and the fore and aft edges are CHAMFERED, rising to z -0.1144 at x -0.1262 and 0.1692. Using the primitive box corners would have given 14.31 mm of error against the mesh, the four true flat corners give 7.74 mm, eight points (corners plus chamfer tips) give 2.43 mm, and the twelve point table now in the config gives 0.85 mm over pitch +/-50 degrees and roll +/-30 degrees. The support set was found by taking the convex hull (162 vertices, whose minimum equals the minimum over all 47857 vertices exactly) and keeping only the hull vertices that are ever the lowest point over the reachable orientation band, which is 26 vertices lying along the two chamfered edges, then reducing to a y symmetric twelve point table that serves both feet. The ankle drum never contacts, its lowest point is a fixed 0.082 m below the frame while the sole is 0.124 m or more below whenever it touches, so the sole point set is sufficient.

Validated numerically, four ways. The batched expand and reshape path was checked against a naive per environment per foot loop over random poses and agrees to 0.0. A grounded foot reports at most 0.71 mm of clearance at any tilt from 0 to 45 degrees, against a truth of 0. Per foot reward rates at weight 20, the grounded flat foot sliding at 0.34 m/s pays v1 ungated 0.06, v1 gated 0.00, v2 0.00, the grounded foot tilted 20 degrees (the observed state) pays v1 ungated 3.10, v1 gated 0.00, v2 0.00, and the grounded foot tilted 40 degrees pays v1 ungated 6.51, v1 gated 0.00, v2 0.00. Decisively, the residual exploit that SURVIVES the contact gate, a foot hovering 1 cm while tilted 40 degrees, pays v1 gated 10.28 but v2 only 0.20, while a genuine level 8 cm swing pays v1 gated 10.60 and v2 10.74, unchanged. The v2 therefore closes the residual documented in the sixth pass.

Known limitation, recorded in the docstring. The clearance is an absolute world height, which equals the height above the terrain only on flat ground, so the term is correct for sd_brs1_flat (terrain_type "plane") but would need referencing to the terrain height beneath the foot for SDBRS1BlindRoughEnvCfg, the same limitation the v1 term and the feet_regulation TODO already carry.

Still outstanding after this pass. feet_regulation retains the frame height gate and so still releases the anti slide penalty on a tilted grounded foot (0.216 applied at the observed 20 degrees against 0.884 under the original settings), the same sole clearance quantity should be given to it. The feet level term (van Marum Feet orientation, 0.05 against 0.3 combined linear velocity, about 8 at this scale) is not implemented and is now optional rather than necessary, since v2 removes the reward for tilting, though tilting is still not penalised. Skating on flat feet remains profitable, feet_slide -3.36 per second against tracking at 43.5, and nothing but no_fly (15) and feet_air_time (5) pays for stepping.

## Root cause of hopping and dragging under v2, forged foot contact through self collision (2026-07-20, eighth pass, runs 2026-07-17_10-56-28, 10-59-30, 11-01-29)

Run configurations, established by diffing the dumped params. All three runs use foot_clearance_reward_v2 at weight 20 with the twelve point sole table, target 0.08, std 0.035, tanh_mult 1.0. The env.yaml of 10-56-28 and 11-01-29 are byte identical and carry NO contact gate (sensor_cfg absent), 10-59-30 is the same config plus the gate (sensor_cfg contact_forces on Link6[LR], force_threshold 1.0). The agent.yaml files are identical across all three. Weights of note in these runs, rew_lin_vel_xy 50, rew_ang_vel_z 15, rew_no_fly 15, feet_air_time 12.5, rew_foot_clearance 20, feet_slide -5, pen_feet_distance -100 (min_feet_distance 0.21 between Link6 frames), pen_undesired_contacts -0.5 (threshold 10 N, bodies Link[1-4][LR] and Part_Torso), pen_lin_vel_z -0.5, pen_flat_orientation -35.

Outcomes, exact means over the last 1000 iterations, parsed from the tfevents files directly. Gated run 59, rew_foot_clearance 0.000001 (maximum over the whole run 0.00055, the term never paid once in 30000 iterations), video shows the robot standing nearly frozen, feet planted. Ungated runs 56 and 01, rew_foot_clearance 6.99 and 10.99 respectively, videos show bouncing and vigorous motion with legs held together, run 01 has pen_lin_vel_z -0.240 (mean squared base vertical velocity 0.48, RMS 0.69 m/s, vertical pumping), run 56 only -0.039. In ALL three runs, rew_no_fly is -0.022 to -0.034 and feet_air_time is about 1e-5, and feet_slide deepens over training in lockstep with clearance growth (run 01 reaches -5.56, mean summed in contact foot speed 1.11 m/s).

The contradiction that identifies the defect. no_fly returns 1.0 x single_contact - 5.0 x no_contact (rewards.py:338), so at weight 15 flight costs -75 per second, and the observed -0.02 bounds the flight fraction near zero while feet_air_time near 1e-5 bounds single stance near zero, together placing both feet in sensor contact for essentially 99.9 percent of all steps. Yet clearance income of 10.99 per second at weight 20 requires a mean per step kernel x tanh sum of 0.55, that is a foot near 8 cm of measured sole clearance AND moving horizontally, roughly half of all time. A truly grounded foot under v2 measures at most 0.7 mm of clearance, kernel 0.005, at most 0.2 per second from both feet. Both facts can hold only if the contact force on Link6 is not coming from the ground while the sole is airborne.

The mechanism, forged contact through self collision. The asset spawns with self_collision=True (sd_brs1_identified_cfg.py:147) and the contact_forces sensor has filter_prim_paths_expr [] with net force reporting, so net_forces_w on a foot sums forces from EVERY contact partner including the other foot and the leg links. The policy presses its legs together into a single pillar, pen_undesired_contacts grows from -0.55 (about 1 body over threshold) at the start of training to -2.67 to -2.92 (about 5.4 to 5.8 of the nine monitored bodies) at convergence in all three runs, and the videos show the legs and feet held adjacent with motion blur from bouncing. With foot to foot or foot to ankle contact forged, every contact keyed term is deceived at once, no_fly reads permanent double support and never charges the -75 flight penalty during ballistic phases, the ContactSensor air time state machine (which also keys on net force against force_threshold 1.0) never accrues air time so feet_air_time stays at 1e-5, the v2 contact gate reads in_contact always and pays nothing ever, and feet_slide charges airborne feet as if they were sliding. Geometry admits the forgery cheaply, hip frames are 0.26 m apart and soles are +/-0.097 m wide so sole faces touch at 0.194 m of frame separation, and pen_feet_distance measures FRAME distance with min 0.21 so pressing to touch costs at most (0.21-0.194) x 100 = 1.6 per second, while ankle roll of about 15 degrees brings the inner sole edges into touch with frames still 0.26 apart, entirely invisible to the term (logged pen_feet_distance is -0.001 to -0.008, negligible). The origin of the jam is plausibly no_fly itself, its -5 x no_contact term at weight 15 makes early flailing flight enormously costly, and forging contact is the cheapest way to silence it.

Why the two configurations then diverge. With the gate, clearance is dead from the first step, so nothing pays for lifting feet at all and the policy settles into the frozen drag (run 59, tracking 33.6 of about 50). Without the gate, clearance pays for any lifted moving foot with no stance requirement, and with the alternation terms already neutralised by the forged contact, the symmetric hop with legs jammed is the dynamically easiest motion that holds feet near 8 cm while tracking (single leg balance on this underdamped robot earns nothing extra since no_fly and feet_air_time are dead), so runs 56 and 01 became bouncers whose clearance term, at bottom, pays foot SPEED, which is why feet_slide deepens in lockstep. The alternation machinery has been dead in every run to date, which is why air time and no fly read zero across all experiments and why no repair of the clearance measurement alone could induce stepping.

Fix directions, evaluated, none implemented in this pass. (1) Kill the jam directly, add a contact penalty the forgery cannot evade, a second ContactSensor on Link6[LR] with filter_prim_paths_expr targeting the OTHER foot (force_matrix_w isolates the pair force), or more cheaply add Link5[LR] (which has no legitimate contact) to the pen_undesired_contacts body list and raise that weight from -0.5, whose current cost of about 2.9 per second is far below the 11 per second the jam enables. (2) Restore truthful sensing for gait terms, a feet only sensor filtered against the ground prim provides force_matrix_w containing only foot to ground force, and the contact keyed terms can accept it through optional arguments, noting that feet_air_time_positive_biped is an isaaclab library function whose air time state derives from net force, so a package local v2 computing ground only air time would be required for it. (3) Raising min_feet_distance to about 0.24 narrows but does not close the forgery, since rolled sole edges touch at 0.26 of frame separation. (4) Disabling self collision would remove the forgery wholesale but changes the physics of a robot whose thighs genuinely interact, recorded as inherent to the hardware. Definitive confirmation available offline, play model_29999 of run 01 with the DataLogger (FEET_LINK_NAMES "Link6[LR]"), nonzero Link6 net force while the logged sole height exceeds 5 cm is direct proof of forged contact.

A note on run bookkeeping. The user's description associated the gated configuration with hopping and the ungated with dragging, the dumped params establish the opposite mapping, 10-59-30 carries the gate and froze, 10-56-28 and 11-01-29 carry no gate and bounce.

## Run 2026-07-17_11-01-29 in isolation, the forged contact proven (2026-07-20, ninth pass, supersedes the eighth pass narrative)

Method note. This pass replaces inference from the video with inversion of the logged episode rewards into physical quantities, then tests candidate gaits against every contact keyed channel at once. The eighth pass reasoned largely from the videos and asserted a legs jammed pillar bounce, and its ninth pass draft claimed the video showed the same foot raised throughout and offered no_fly's blind spot at hop duty factor 5/6 as a partial explanation. Both claims are corrected below, the video frames at this resolution are suggestive only, and the honest one leg hop that no_fly's blind spot would permit is decisively refuted by feet_air_time.

Configuration. No contact gate on rew_foot_clearance (sensor_cfg absent, env.yaml byte identical to run 10-56-28), foot_clearance_reward_v2 at weight 20, target_height 0.08, std 0.035, tanh_mult 1.0. Contact keyed terms all resolve to Link6[LR] on the contact_forces sensor. Base height target 1.14 m, hips sit 0.181 m below the root, so hips are near 0.96 m and thighs span 0.96 down to 0.52 m.

Inverted observables, mean over the last 1000 iterations. Clearance function 0.5493, no_fly -0.001488, feet_air_time 1.44e-6, feet_slide 1.1118 m/s summed over feet, mean vz squared 0.4808 giving RMS vertical velocity 0.693 m/s, tracking 0.7554, RMS base height error 0.0540 m, tilt 5.0 degrees, undesired contacts 5.84 bodies over 10 N, feet distance function 1.0e-5 so the Link6 frames sit essentially exactly at the 0.21 m limit.

Contact occupancy, solved jointly. no_fly returns P(exactly one contact) minus five times P(no contact), so the observed -0.001488 alone admits an honest one leg hop at 83.3 percent stance and 16.7 percent flight, which is the blind spot the draft invoked. feet_air_time closes it. feet_air_time_positive_biped (isaaclab velocity mdp rewards.py:49) gates on single_stance, the count of feet with contact_time above zero equalling one, and returns the minimum over feet of the in mode time, so a one leg hop earns the stance foot's contact time throughout stance. At hop periods of 0.4, 0.5 and 0.7 s that predicts 1.42, 1.78 and 2.49 per second against an observed 1.8e-5, short by factors of 79000 to 138000. Single stance therefore essentially never occurs. With P(one contact) at zero, no_fly forces P(no contact) to 0.0003, so BOTH feet report contact 99.97 percent of the time.

The contradiction. A grounded sole under v2 measures at most 0.7 mm, kernel 0.0054, so two grounded feet at the observed 0.556 m/s pay 0.0054 against the observed 0.5493, short by a factor of 101. Solving the residual, one foot must supply 0.5466 as kernel times tanh, which at a plausible foot speed of 0.62 m/s demands kernel 0.992, that is a sole clearance of 0.080 plus or minus 0.003 m. The policy parks one foot at EXACTLY the reward's target height and holds it there. Both feet nevertheless report contact. Ground contact cannot produce both facts.

Proof of self collision, independent of the reward inversion. pen_undesired_contacts monitors Link[1-4][LR] and Part_Torso at a 10 N threshold and reads 5.84 bodies over threshold. Part_Torso contributes nothing, because the base_contact termination applies illegal_contact to Part_Torso at a 1.0 N threshold and its rate is flat zero for all 30000 iterations, bounding torso contact below 1 N. So 5.84 of the eight Link[1-4][LR] bodies carry more than 10 N. Those are the hip yaw, hip roll, hip pitch and thigh links, which at a base height within 0.054 m of 1.14 m and a tilt of 5 degrees sit between 0.52 and 0.96 m above the ground and cannot reach it. Cross environment contact is excluded by env_spacing 2.5 with per environment sensor prims. Self collision is therefore the only possible source, and it is confirmed enabled in the dumped config (self_collision true at env.yaml:109, enabled_self_collisions true at :139). The legs are clamped together under substantial mutual force.

Why the foot sensor is deceived. ContactSensor.net_forces_w is get_net_contact_forces (contact_sensor.py:373), the sum of normal contact forces on the body from EVERY partner, and filter_prim_paths_expr is empty so no per partner breakdown exists. The same unfiltered quantity drives the air time state machine (contact_sensor.py:422 thresholds net_forces_w to set is_contact). One self contact on a raised Link6 therefore corrupts every contact keyed term simultaneously, no_fly reads double support and never charges its -75 per second flight tax even during genuine ballistic flight, current_air_time never accrues so feet_air_time forfeits up to 5 per second, and feet_slide charges the raised foot as though it were sliding. Geometry permits it cheaply, the Link6 collision mesh spans y -0.1130 to +0.0970 with the sole band at plus or minus 0.0970, so at the 0.21 m frame separation the policy holds, opposing sole faces stand 1.6 cm apart, closed by about 8 degrees of ankle roll (a sole corner at y 0.097, z -0.124 moves inward to 0.117 under 10 degrees of roll), and the raised foot at 0.08 m also overlaps the stance shank Link5 in height, which no term monitors at all.

Root cause, stated precisely. The PRIMARY driver is that ungated foot_clearance_reward_v2 is stance agnostic, always on, and multiplied by foot speed, so its argmax over gaits is not a periodic swing but one foot parked at the target height and carried at body speed, worth about 11 per second continuously where a genuine walking gait collects clearance only near each swing apex, roughly 4 to 5 per second. The hop is not the objective, it is the consequence, hopping is simply how a biped balances and tracks a velocity command while permanently committing one leg to earning clearance. The ENABLER is that self collision plus unfiltered net force sensing lets the parked foot forge contact, which silences the two terms that would otherwise make this ruinous, no_fly's flight tax and feet_air_time's alternation income. The LOCK IN is that the jam is reachable from the very start of training, pen_undesired_contacts already reads -0.53 (about one body) in the first 1000 iterations and deepens monotonically to -2.92, so by the time clearance income matures the two channels that would pay more for walking read identically zero and supply no gradient at all, and escaping requires unpressing the legs through a valley of lost clearance and lost stability that local policy updates never cross. Honest accounting favours walking, roughly 17 to 19 per second of gait income against the hop's net of about 5 after slide and vertical penalties, so the policy is not at the honest optimum, it is at the forged one.

Corollary for the gated run. This also explains why 10-59-30, the same config plus the contact gate, produced a frozen drag with clearance at 0.000001 and a maximum of 0.00055 over 30000 iterations. The gate keys on the same corrupted net force, so once the legs jam the gate reads in contact permanently and the clearance term is dead from the first step, leaving nothing that pays for lifting a foot at all.

Discriminating test, unchanged. Play model_29999 with the DataLogger and FEET_LINK_NAMES "Link6[LR]". The signature is nonzero Link6 net force while the logged sole height of that foot sits near 0.08 m, and nonzero net force on both feet during ballistic phases identified by base vertical velocity.

## ROOT CAUSE FOUND, permanent shank into foot self penetration in the URDF (2026-07-20, tenth pass, supersedes the forged foot to foot mechanism of the eighth and ninth passes)

The user's DataLogger plot (artefacts/plots-latest-2/clearance_and_forces.png, env 0 of run 2026-07-17_11-01-29) settled the mechanism and refuted the specific claim that the two feet press each other. The user correctly objected that no feet collision is visible in the videos, and they were right.

What the plot shows. Contact force magnitude on Link6R and Link6L never reaches zero at any point, sitting between roughly 2000 and 5000 N throughout, while sole clearance on the same feet oscillates cleanly between 0 and about 0.10 m against the 0.08 m target and body frame height oscillates between 0.13 and 0.25 m. Two features of the force are decisive beyond its being nonzero. First, Fz is NEGATIVE, between about -2000 and -5000 N, and ground contact can only push a foot up, never down. Second, the magnitude is 6.8 times the entire robot's weight PER FOOT, the URDF masses summing to 59.85 kg for a weight of 587 N. That is a penetration resolution force, not a ground reaction.

The defect, verified at mesh level. Link4 (shank, frame at the knee) and Link6 (foot, frame at the ankle) permanently interpenetrate, on both legs, and PhysX does not filter their collision because they are NOT directly jointed. The ankle is modelled as two revolute joints, AnkleRoll from Link4 to Link5 and AnklePitch from Link5 to Link6, with Link5 a 0.072 kg intermediate link whose own mesh is only 1.9 cm tall and whose AnklePitch origin is (0,0,0) so that Link5 and Link6 frames are coincident. Articulation self collision filtering excludes only parent child pairs, so the physically adjacent shank and foot are treated as a colliding pair. At the zero pose the overlap box is 0.2376 x 0.1364 x 0.0710 m, about 2301 cm^3, and 3195 vertices of Link4R lie strictly inside the convex hull of Link6R (3128 for the left leg). The discriminating control is Link1 against Link3, which share an overlapping bounding box at the hip but have ZERO vertices inside each other's hull, being merely coaxial, and correctly generate no such force. Self collision is enabled at sd_brs1_identified_cfg.py:147 and recorded in the run at env.yaml:109 and :139.

Why the force is downward and constant. Link4 sits above Link6, so PhysX separates them by pushing the shank up and the foot down, giving the observed negative Fz. The penetration is unescapable because the two links are held in relative position by the ankle joints, so the force is present at every foot height, which is exactly what the plot shows and is the single most diagnostic feature in it.

Consequences, which explain every anomaly recorded in the previous passes. Every contact keyed term on Link6 is permanently saturated, because a true ground reaction of at most about 600 N is buried under a permanent spurious bias five times larger and the terms threshold at 1 N. no_fly therefore reads permanent double support and never charges its -75 per second flight tax nor pays single stance income, feet_air_time never accrues air time and reads 1.8e-5 against a possible 5 per second, feet_slide charges genuinely airborne feet as sliding, and the optional contact gate on foot_clearance_reward_v2 never opens, which is precisely why run 2026-07-17_10-59-30 froze with clearance at 0.000001 and a maximum of 0.00055 over 30000 iterations. pen_undesired_contacts monitors Link[1-4][LR] and so counts Link4L and Link4R permanently, contributing to its 5.84 bodies. The alternation machinery has therefore been dead in EVERY experiment in this work stream, which is the standing explanation for air time and no fly reading zero throughout, and no reward redesign could ever have repaired it.

Correction to the ninth pass. The arithmetic of the ninth pass stands, both feet report contact 99.97 percent of the time and one foot is genuinely at 0.080 m of sole clearance, and the conclusion that the contact reading is not ground contact stands. The mechanism asserted there, the raised foot pressing the opposite foot or leg, is WRONG. No inter leg contact is required at all, each foot is permanently colliding with its own shank. The reward side reasoning that ungated stance agnostic clearance favours parking a foot at the target height remains a real secondary defect but is no longer the primary cause.

Fix options, none implemented. (1) Disable articulation self collision, one line at sd_brs1_identified_cfg.py:147 and the articulation_props flag, which removes the spurious force immediately and is not a fidelity regression since the current state is already unphysical, at the cost of allowing genuine thigh to thigh interpenetration. (2) Add an explicit filtered pair between Link4 and Link6 through UsdPhysics FilteredPairsAPI, preserving legitimate self collision, which is the correct fix but needs USD post processing after the URDF import. (3) Shrink or replace the Link4 collision geometry with a primitive that stops above the ankle, which also preserves thigh collision and is robust, at the cost of editing the asset. Recommended order is (2), falling back to (1) for a fast confirmation run since it is reversible and one line. Whichever is chosen, the contact terms should be re verified with the same DataLogger plot before a long run, the signature of success is Fz going to exactly zero whenever sole clearance exceeds about 1 cm.

## Confirmation of the root cause by experiment (2026-07-20, eleventh pass)

The user disabled articulation self collision and restarted training with the contact gate enabled on foot_clearance_reward_v2. The gated clearance reward is now nonzero where it was previously zero. This confirms the tenth pass diagnosis by the strongest available evidence, because the gate is effectively a boolean channel, the term multiplies by ~in_contact and in run 2026-07-17_10-59-30 it read 0.000001 with a maximum of 0.00055 over 30000 iterations, meaning ~in_contact was never true for either foot at any timestep of that entire run. The change is not a gradual improvement in magnitude but a permanently false predicate becoming sometimes true, which only the removal of the permanent Link4 into Link6 penetration force can produce. The contact forces on Link6 were therefore spurious and of self collision origin, as diagnosed.

Consequences now live, to be watched in this run. Three terms that were inert for the whole work stream are now connected. feet_air_time can accrue for the first time and can pay up to 5 per second at threshold 0.4 and weight 12.5. no_fly now discriminates genuinely, which cuts both ways, its -5.0 x no_contact branch at weight 15 is a -75 per second flight tax that was previously never charged and is now the largest single penalty in the configuration, so early exploration may be over penalised even though a walking gait has no flight phase. feet_slide now charges only genuinely grounded feet, so its logged magnitude should fall from the -5.56 of run 11-01-29 without any change in behaviour, and a drop there is expected and is not evidence of improvement by itself.

Regression introduced by the quick fix, to be monitored. With self collision disabled the legs may interpenetrate freely, and pen_undesired_contacts is now nearly inert because it monitors Link[1-4][LR] and Part_Torso, none of which can reach the ground in an upright posture, so it no longer penalises anything except a fall. Nothing therefore prevents the policy from scissoring its legs through one another. Watch pen_feet_distance (min_feet_distance 0.21 on Link6 frames, weight -100) and the videos for leg crossing, since that is now an available and unpunished exploit.

The permanent fix, still to implement. Filter only the offending pairs through UsdPhysics FilteredPairsAPI after URDF import, namely Link4L against Link6L and Link4R against Link6R, then re enable self collision so genuine thigh to thigh contact is restored and leg crossing is punished again. One caveat on scope, the mesh level interpenetration test of the tenth pass was run at the ZERO pose only. It found genuine penetration for Link4 against Link6 on both legs and correctly found none for Link1 against Link3, which share a bounding box at the hip but are merely coaxial with zero vertices inside each other's hull. Since Link1, Link2 and Link3 are all coaxial at the hip with (0,0,0) joint origins, the non adjacent Link1 against Link3 pair should be re checked across the reachable hip range before self collision is re enabled, and filtered as well if it penetrates at any reachable angle.

## Simplified URDF rebuilt for collision cleanliness and deployment fidelity (2026-07-20, twelfth pass)

The user authored a primitive shape URDF at assets/urdf/solefoot/SD_BRS1/SD_BRS.urdf to replace the mesh model, intending to train on it and deploy on SD_BRS1_Assembly2.urdf. It was rebuilt to remove the remaining self collisions and, more importantly, to restore joint fidelity to the deployment target. The previous file is preserved at SD_BRS.urdf.bak. A reusable geometric checker lives in the session scratchpad (urdf_tool.py, fastcheck.py, gen_urdf.py), sampling points inside each primitive and testing analytic containment in every other, over a grid of joint configurations.

Diagnosis of the user's version. At the zero pose it was already clean, and it had already fixed the Link4 into Link6 penetration that ruined the mesh model, the shank box ending at z -0.374 against the foot cylinder starting at -0.412. Sweeping the joint ranges found one genuine intra leg defect, Link1R against Link3R, which are non adjacent because Link2R sits between them, the identical structural pattern to the Link4 into Link6 bug. The intruders were Link1's yaw bearing cylinder at x -0.0885 radius 0.0575 struck by the thigh box (46 sample hits at HipRoll -0.349, HipPitch -0.576), and Link1's rear box reached by both the thigh box and Link3's rear plate at x -0.1015. Link5 already carried no collision geometry, only the protruding visual pipe.

Changes made. Link5 geometry removed entirely on both legs, leaving a pure inertial link, which removes the pipe jutting from the ankles. Link2's enclosing cube removed on both legs, leaving only the roll cylinder, which is the cube that wrapped the hip cylinders. Link3's rear plate removed, leaving the hip cylinder, the thigh box and the knee cylinder, where the hip cylinder is coaxial with the pitch axis and therefore invariant under that rotation. Link1's cylinder removed and its box shortened from x -0.2488..-0.106 to -0.2488..-0.156, chosen because Link3 sweeps back to x -0.1406 within Link1's y and z window over the full original hip roll and pitch ranges, giving 15 mm of margin. Removing collision or visual geometry cannot change inertia, since URDF inertials are declared independently, and this was verified numerically.

Joint limits, a defect found beyond the brief and the most consequential one. The user's version disagreed with the original on six of ten revolute joints. AnkleRoll and AnklePitch limits were SWAPPED, the original having AnkleRoll +/-0.349 (left +/-0.359) and AnklePitch +/-0.454, the simplified having the reverse, so a policy trained there could command roughly thirty percent beyond the real ankle roll limit and would be clipped on deployment. HipPitch was set to [-0.576, +0.314] on BOTH legs, but the pitch axes are opposite, (0,-1,0) on the right and (0,1,0) on the left, so identical limits give the two legs DIFFERENT physical ranges, the right able to swing forward 0.576 rad against the left's 0.314. That is a mechanical cause of asymmetric gait, independent of any reward, and it is worth noting the environment's own reset randomisation is correctly mirrored (HipPitchR -0.25 to 0.42 against HipPitchL -0.42 to 0.25), which confirms the URDF was the odd one out. All limits, origins, axes and types are now copied verbatim from the original by the generator, so they cannot drift.

Verification of the rebuilt file. Inertials match the original on all thirteen links with zero difference and a total mass of 59.8523 kg. Joint origins, axes, types and limits match with zero difference. A sweep of five samples per revolute joint over the full original ranges, 5^5 per leg, finds NO intra leg or torso overlap anywhere. Cross leg overlaps remain and are deliberately retained, since they are physically real and occur only near hip roll extremes, they are what stops the policy scissoring its legs, which is the protection that was lost when self collision was disabled wholesale. All reset randomisation ranges in the environment sit inside the restored limits, the tightest case being AnklePitch reaching +0.25 against a soft limit of 0.409 at factor 0.9.

Next step for the user. brs_solefoot_env_cfg.py binds SD_BRS1_IDENTIFIED_CFG at three sites, lines 44, 78 and 116, which points at the mesh URDF. SD_BRS1_IDENTIFIED_CFG2 already points at SD_BRS.urdf with enabled_self_collisions True, so switching those three bindings both adopts the simplified model and re enables self collision safely. Confirmation should use the same DataLogger force plot, the signature being Fz falling to exactly zero whenever sole clearance exceeds about 1 cm. Caveat, this is verified geometrically only, since no Isaac Sim is available in the analysis container.

## The contact machinery works, and the reward set now pays for holding a single stance (2026-07-20, thirteenth pass, run 2026-07-20_07-52-14)

This pass records the first run trained after the self collision defect was removed, and it changes the diagnosis entirely. Every contact keyed term is alive, the numbers are large and they move, and the failure that remains is a reward design failure rather than an asset failure. The run reached iteration 3544 with the adaptive learning rate settled at 3e-5, mean reward flat at 1590 to 1609 from iteration 2658 onward, and mean episode length at 1967 of a possible 2000 steps, so it is converged rather than still improving.

Logged rates over the last 200 iterations, with the inversion to mean per step function values through the weight as reward rate per second basis. rew_lin_vel_xy 44.29 at weight 50 gives 0.886, rew_ang_vel_z 10.12 at 15 gives 0.675, rew_foot_clearance 13.94 at 20 gives 0.697, rew_no_fly 13.63 at 15 gives 0.909, feet_air_time 3.43 at 12.5 gives 0.274, feet_slide -0.48 at -5 gives 0.096 m/s, pen_flat_orientation -0.88 at -35 gives 0.0250 hence an RMS torso tilt of 9.1 degrees, pen_base_height -0.012 at -15 gives an RMS height error of 0.028 m. Posture, height and velocity tracking are all good, which is why the failure is not visible in those channels.

Contact occupancy, from no_fly which returns 1.0 times single contact minus 5.0 times no contact (rewards.py:338). The inverted value 0.9089 forces P(single contact) between 0.909 and 1.0 and P(flight) below 0.018, so the robot is in single support at least ninety one percent of the time and essentially never airborne. That is not a shuffle and not a hop, it is a permanent one legged stance.

Cadence, derived from feet_air_time_positive_biped (isaaclab_tasks velocity mdp rewards.py:49). During single support the minimum over feet of in_mode_time is the swing foot's air age, which ramps from zero at lift off, so the mean of clamp(t, 0.4) over a single support phase of length T is T/2 for T below 0.4 and (0.08 + 0.4(T - 0.4))/T above it. The command gate P(|cmd_xy| > 0.1) is 0.803 for lin_vel_x uniform on (-0.3, 0.8) with two percent standing environments. Inverting 0.274 against that gate and the single support fraction gives a conditional mean of 0.34 to 0.38 s and therefore a single support phase length between 1.4 and 3.3 s, most likely about 2.5 s. Against a human single support phase of 0.4 s the robot is stepping roughly six times too slowly, about 0.2 to 0.4 steps per second against a natural 1.8.

Swing foot behaviour, from foot_clearance_reward_v2 (rewards.py:140). The term is exp(-(sole_clearance - 0.08)^2/0.035^2) times tanh(1.0 times the horizontal foot speed), summed over feet, with the grounded foot zeroed by the contact gate. The airborne foot therefore supplies the whole 0.697, and if it sits at the 0.08 m target so the kernel is near one, tanh(v) = 0.697 gives a foot speed of 0.86 m/s against a base speed near 0.21 to 0.30 m/s. The swing foot is being waved, not carried, which is what the user described and what the video shows.

Video evidence, no longer retained, 1280x720, 100 fps, 2001 frames, 20 s, sampled zoomed at both 0.08 s and 0.5 s intervals across the whole episode. The robot stands on one leg with the torso pitched forward, the other leg extended well forward with the sole clear of the ground, and it holds that configuration for seconds at a time with no swap visible inside a 2.56 s window. This corroborates the 2.5 s single support phase derived from the logs independently.

The decisive arithmetic, why holding beats walking. Modelling a gait as two single support phases of length T_ss and two double support phases of T_ds per cycle, and allowing the swing foot either to pass through the clearance band or to PARK inside it, the three gait terms pay as follows. Park and hold at T_ss 2.5 s pays air 3.62, no_fly 14.71 and clearance 13.66 for a total of 31.99 per second, which reproduces the logged 3.43, 13.63 and 13.94 totalling 31.01 to within three percent and validates the model. A genuine walk at T_ss 0.40 s pays 1.61, 12.00 and 5.02 totalling 18.62. Walking therefore costs 13.3 per second, which is sixteen percent of the 85.6 per second gross income, and no amount of further training at a learning rate of 3e-5 will cross a barrier of that size.

Why each of the three terms prefers the hold, stated separately since the remedies differ. no_fly has no grace window, so every instant of double support is unpaid, and since a walk cannot avoid double support the term strictly prefers minimising the number of transitions, hence long phases. feet_air_time_positive_biped clamps at the threshold rather than peaking at it, so its per phase mean rises monotonically towards 0.4 as T_ss grows, 0.20 at T_ss 0.4 s against 0.38 at 2.5 s, and every foot swap additionally RESETS both accumulators and costs a dip, so alternation is doubly penalised. foot_clearance_reward_v2 is stance agnostic and velocity multiplied, so a foot parked at the target height and waved earns the kernel continuously, whereas a genuine swing passes through the band for only about forty five percent of a much shorter phase.

Which defect is load bearing, from a sweep of the candidate repairs against both strategies. Adding a 0.2 s grace window to no_fly leaves the hold ahead by 9.9 per second. Adding that plus a peaked air time kernel of width 0.15 centred on 0.4 s leaves the hold ahead by 7.7. Only when the clearance term stops paying a parked foot does the ranking invert, and the clearance gate applied alone inverts it by a mere 0.32 per second, which is inside noise, whereas the full package gives walking a margin of about 9 per second. The conclusion for sequencing is that the clearance term is necessary but not sufficient and the three must move together.

Corroborating trend evidence for the reward hacking reading. Across training rew_foot_clearance climbs from 0.08 to 13.94 while every effort and quality penalty worsens monotonically, pen_action_smoothness from -0.22 to -1.65, pen_joint_torque from -0.12 to -1.25, pen_ang_vel_xy from -0.06 to -0.41 and pen_flat_orientation from -0.30 to -0.88. An agent paying steadily rising costs to farm one rising term is the standard signature of reward hacking rather than of skill acquisition.

Standing note on the earlier analysis. The second section of ../plans/GAIT_STRATEGY.md argued that the original sparse air time term was an exploration barrier and replaced it with the dense variant. That reasoning was correct and the change succeeded, single stance went from essentially zero to at least ninety one percent. The dense term has now done its job and has become the plateau described above, so the correct reading is a curriculum transition rather than a reversal, the dense term buys single support and something that prices step EVENTS must now buy alternation.

## Fourteenth pass, the GaitReward run 2026-07-21_06-03-36 and the symmetry data augmentation plan (2026-07-22)

The overnight run 2026-07-21_06-03-36 enabled the GaitReward contact clock at weight 40, the change wired into brs_base_env_cfg.py in the thirteenth pass. The clock succeeded at its one job, the one legged hold of run 2026-07-20_07-52-14 is gone and the robot alternates its feet. Tensorboard over 21992 iterations, 45 tags, quartile means Q1 to Q4. rew_lin_vel_xy strong near 31.6, feet_air_time risen 1.95 to 2.14, rew_no_fly risen 11.4 to 12.2, rew_foot_clearance risen 8.6 to 10.3, so the policy genuinely steps. rew_gait sits near minus 6.8, which at weight 40 inverts to a function value near minus 0.17 per step, between a perfect clock match near minus 0.079 and the old hold near minus 0.488, so compliance is partial. The learning rate has again collapsed to the adaptive floor near 1e-5. Video evidence, no longer retained, 20 s, two contact sheets read, confirms alternation and shows three residual defects.

The three residual defects, the user's observations 1, 2, 3. One, the gait is asymmetric, a visible limp, corroborated by pen_flat_orientation pinned near minus 1.72 across the whole run, the base leans rather than sitting level. Two, the legs are stiff and near straight, corroborated by pen_joint_torque worsening minus 0.48 to minus 1.09 and feet_slide worsening minus 0.52 to minus 0.73 as the policy shuffles on straight legs, and by the config, the KneePitch limit is [0, 1.483] so zero is a straight leg, the init pose sets KneePitch to zero, and no reward term directly rewards knee flexion. Three, the robot shuffles when it should stand, because gait_command frequencies are degenerate at (1.0, 1.0) so the clock never stops and GaitReward has no zero command gate. The current work addresses observation 1 only.

The instrument for observation 1 is symmetry data augmentation, from the SymmLoco paper arXiv 2403.17320, ported onto the RslRlSymmetryCfg interface. The full plan is /ws/plans/SYMMETRY_PLAN.md, the literature is literature.md cluster 9, the rsl_rl consumer contract is rsl_rl.md symmetry section. The change is additive and backwards compatible, a new file mdp/symmetry.py holding compute_symmetric_states, an optional export, and one symmetry_cfg kwarg on SD_BRS1FlatPPORunnerCfg only, no shared function edited.

The SD_BRS1 observation and action layout, needed by any mirror and recorded so it need not be re derived. Two facts overturn the naive expectation. First, both PolicyCfg and CriticCfg set history_length 10 and flatten_history_dim true, so obs["policy"] is 440 wide, eight per term blocks of ten time major frames each, base_lin_vel [0:30], base_ang_vel [30:60], proj_gravity [60:90], joint_pos [90:190], joint_vel [190:290], last_action [290:390], velocity_commands [390:420], gait_phase [420:440]. A mirror must reshape each block to (N, 10, width) and act on the last axis. Second, the ten actuated joints in articulation order are HipRollR, HipRollL, HipPitchR, HipPitchL, KneePitchR, KneePitchL, AnkleRollR, AnkleRollL, AnklePitchR, AnklePitchL, so the left right permutation is the pairwise swap [1,0,3,2,5,4,7,6,9,8], and the sign flips are {0,1,2,3,6,7}, that is HipRoll, HipPitch and AnkleRoll. HipPitch flips despite being a pitch joint because HipPitchR and HipPitchL carry OPPOSITE URDF axes, (0,-1,0) against (0,1,0), decisively corroborated by negated URDF limits, HipPitchR [-0.75, 1.25] against HipPitchL [-1.25, 0.75], by reset events that split HipPitch by side with negated ranges, and by KneePitch and AnklePitch using identical ranges on both sides. The action tensor is a single 10 dim frame, no history, same permutation and signs. The per frame non joint sign table, base_lin_vel [1,-1,1], base_ang_vel [-1,1,-1], proj_gravity [1,-1,1], velocity_commands [1,-1,-1], gait_phase [-1,-1]. The gait_phase double negation holds only while the command offset is exactly 0.5, HipYawR and HipYawL are fixed joints and contribute no DOF. The one unsettled point is depth major versus leg major joint ordering, which cannot be read from files since no dumped joint_names artifact exists, so the plan builds the joint map from env.unwrapped.scene["robot"].joint_names at runtime, correct either way, and requires a one time joint_names print to confirm the depth major expectation.

Design decision recorded, the plan mirrors the POLICY group only and tiles the CRITIC group unchanged. This is correct for training, returns and advantages are symmetry invariant and tiled, the surrogate uses only the policy group and the mirrored actions, the value loss uses only the critic group, so the heads never cross contaminate, and deployed policy symmetry is enforced entirely through the policy observation and the action mirror. The defect left standing, the critic value function does not receive symmetric augmentation, so a later phase wanting full value symmetry must mirror the critic group, which is larger and carries a runtime width uncertainty in the material properties term and a lateral grid flip for the height scan. Primary config is use_data_augmentation true and use_mirror_loss false, faithful to SymmLoco aug and to the paper's recommendation that a real biped prefers the soft augmentation over hard equivariance, with the mirror loss held in reserve as an additive strengthening.

## Fifteenth pass, the critic observation inversion supersedes the policy only decision (2026-07-22)

The fourteenth pass recorded a decision to mirror the POLICY group only and tile the critic. That decision is SUPERSEDED. The critic group must be mirrored too, and the plan `/ws/plans/SYMMETRY_PLAN.md` section 3.5 now does so. The reason is a value function correctness point, in `/ws/rsl_rl/rsl_rl/algorithms/ppo.py` update the value head reads `obs_batch["critic"]` at line 252 and is trained toward the tiled `returns_batch` at line 307, so a tiled critic feeds the ORIGINAL privileged state while carrying the replicated return of that same state, which merely duplicates the value loss and never induces the value invariance V(gs)=V(s) that the augmentation is meant to teach. SymmLoco mirrors the critic for exactly this reason, it defines a separate `critic_in_field_type` and transforms `critic_observations` at `ppo_augment.py:123,179`. The mirror module is therefore robot specific and lives at `mdp/symmetry/brs.py` in a package, not a single `symmetry.py`, so a TRON1 mirror can sit beside it.

The critic layout and the inversion, resolved from the dumped tensors of run 2026-07-21_06-03-36, since deleted, a dict of logged arrays loaded WITHOUT torch by stubbing `torch._utils._rebuild_tensor_v2` and `torch.storage._load_from_bytes` (see the scratchpad `torchless_load.py`). The critic is 489 wide per frame, 4890 flattened over the ten frame history. The single most load bearing result is the BODY order, which is LEFT before RIGHT at every level, contradicting the earlier right first assumption for both bodies and joints. The dumped `robot_mass` is [8.2464, 2.726, 2.726, 3.86, 3.8609, 9.6512, 9.645, 5.8054, 5.805, 0.0722, 0.0722, 3.691, 3.691], matched to the asymmetric identified masses Link2L 3.86 vs Link2R 3.8609, Link3L 9.6512 vs Link3R 9.645, Link4L 5.8054 vs Link4R 5.805, and confirmed by the inertia products, body5 ixy positive is Link3L, body6 ixy negative is Link3R. Body order Part_Torso, Link1L, Link1R, Link2L, Link2R, Link3L, Link3R, Link4L, Link4R, Link5L, Link5R, Link6L, Link6R, body perm [0,2,1,4,3,6,5,8,7,10,9,12,11]. This does NOT change any permutation, every swap is between adjacent partners, so the joint perm [1,0,3,2,5,4,7,6,9,8] and flip set {0,1,2,3,6,7} are unchanged, the module builds all maps from runtime names so it is order robust.

The per term inversion of the critic. Base frame terms mirror by axis sign, base_lin_vel [1,-1,1], base_ang_vel [-1,1,-1] pseudovector, proj_gravity [1,-1,1], velocity_commands [1,-1,-1]. Joint space terms mirror by the joint perm with sign flip {0,1,2,3,6,7}, these are joint_pos, joint_vel, last_action, robot_joint_torque, robot_joint_acc, robot_joint_pos. The two gain terms robot_joint_stiffness and robot_joint_damping mirror by the joint perm with NO sign flip, they are non negative magnitudes. Morphology terms mirror by the body perm, robot_mass scalar no sign, robot_inertia is 13 by 9 flattened in column major [Ixx,Iyx,Izx,Ixy,Iyy,Izy,Ixz,Iyz,Izz] and a sagittal reflection R I R^T with R=diag(1,-1,1) flips the y coupling products, local indices {1,3,5,7}, sign vector [1,-1,1,-1,1,-1,1,-1,1] on each nine block, verified on the dump as an exact involution reproducing the partner sign structure. heights is a 187 ray grid, 11 lateral y rows by 17 forward x columns (GridPatternCfg size (1.6,1.0) res 0.1 ordering xy), mirror reverses the 11 y rows, no value sign. gait_phase flips [-1,-1] same as the actor, since the foot swap shifts the shared clock by the fixed 0.5 offset. robot_material_properties is 57 wide, that is 19 collision shapes by 3 = [static_friction, dynamic_friction, restitution], NOT 13 shapes as a naive one collision per link count would give, the identified meshes decompose into 19 convex shapes, restitution column is all zero, mirror swaps each body's contiguous shape block with its partner's by a runtime shape count `_NUM_SHAPES_PER_BODY` (sum 19, partner equal), no sign.

The four WORLD frame critic terms stay IDENTITY, a recorded approximation. feet_lin_vel and feet_contact_force (both world frame, per foot Link6R then Link6L, confirmed by the dump where a planted foot force is almost pure world z), robot_pos (root_pos_w absolute, includes the env origin), robot_vel (root_vel_w). A sagittal mirror of a world frame vector needs the base yaw to rotate into the base frame, reflect, rotate back, and the augmentation function sees only the stored history batch with no per transition yaw, the yaw is in no observation term, so it cannot mirror these, and a naive world y flip is a DIFFERENT inconsistent operation valid only at zero yaw. Leaving them identity keeps the base frame proprioception, joint space, morphology, height scan and clock mirrored exactly, and only these four privileged coordinates approximate, which the value function tolerates. IsaacLab physics tensor facts recorded in isaaclab_env.md.

## Sixteenth pass, the first successful walker and the natural gait plan (2026-07-23)

The run `2026-07-22_11-36-53` is the FIRST SD_BRS1 policy that walks properly, a coordinated alternating gait tracking the velocity command to a lin_x RMSE of 0.08 m/s, feet alternating cleanly with swing clearance near 0.10 m against the 0.08 target. It is the new baseline. Three naturalness defects remain, analysed in full in `/ws/plans/NATURAL_GAIT_PLAN.md`, the deliverable of this pass, grounded in the play.py dump of that run, since deleted, 3001 steps, 32 envs, dt 0.01, the eight plots in `/ws/context/artefacts/plots_latest_play`, twelve extracted video frames, and the dumped `params/env.yaml` which is byte identical to the working tree today.

Defect one, the LOCKED knee. The knee temporal std is 0.008 rad, about half a degree, twenty five times less than the hip pitch at 0.21 rad, yet it carries the LARGEST mean torque on the robot, 177 Nm, nearly three times the hip pitch 62 Nm, peaking at its 601 Nm ceiling, while doing the LEAST work of the major joints, 24 W, a 4.9 percent share against the hip pitch 22 percent each. The torque against velocity scatter is a vertical stripe at zero velocity, the static strut signature. Root cause is a straight knee ATTRACTOR not a single term. The straight leg standing height is 1.175 m (0.181 torso to hip plus 0.44 thigh plus 0.43 shank plus 0.124 ankle to sole) while pen_base_height targets only 1.14 m at weight minus fifteen, so standing tall overshoots by 0.035 m and costs 0.018 per step, negligible, and bending to the 0.41 rad that hits 1.14 buys back only that. The knee default is 0 which is its hard lower extension stop and below the soft limit 0.074, so pen_joint_pos_limits levies a small 0.148 per knee toll, use_default_offset true makes the zero action fixed point the straight stance, and the 177 Nm is active anti buckling extension not motion. No reward rewards knee flexion, joint_deviation_l1 is imported in the robot cfg but wired to nothing. The literature is clear that the straight STANCE knee is correct and efficient, the missing signal is SWING flexion, human gait flexes the knee in swing and the robot instead lifts by hip and ankle.

Defect two, forward LEAN. No orientation is logged in the dump, read from video plus base pitch rate mean plus 0.042 and roll and pitch rate std 1.18 and 0.99 and a vertical bob std 0.20 m/s. The only orientation term flat_orientation_l2 is symmetric and reference free at weight minus thirty five, which is WITHIN the recommended minus thirty three to minus fifty band (van Marum two thirds, G1 parity, scaled to the 50 tracking weight), so the lean is a shaping not a magnitude problem, a small steady forward pitch pays for itself in tracking. Expected to partly relax once the knee unlocks since straight leg propulsion is a controlled forward fall.

Defect three, non smooth ACTUATION. Torque single step jump up to 578 Nm per 10 ms, joint accel spikes to 4000 to 5830 rad/s2, AnklePitch position saturates its 0.454 limit 13 to 14 percent of steps, contact impact 7444 N which is 12.7 times the 587 N body weight (total mass 59.85 kg). Hips saturate 500 Nm, knee 601, ankle roll its 131 limit. Smoothness penalties are two to seven orders below the tracking rewards, pen_action_rate minus 0.01, pen_action_smoothness minus 0.075 which is a second difference of the action, joint_acc minus 1e-7, joint_torque minus 1e-5, and there is NO torque rate penalty, only action rate and joint accel. The action is unclipped position with scale 0.25. The ankle is left overdamped, zeta 1.43 and 2.46, while hips and knee are near zeta 0.7.

Ground truth not to re derive. Actuator gains as trained, hip roll K150 D45, hip pitch K200 D50, knee K200 D22, ankle roll K20 D4, ankle pitch K50 D4, effort limits hip 500, knee 600, ankle roll 131, ankle pitch 262, velocity 40 hip and knee 25 ankle, stiffness and damping randomised by a factor in (0.9, 1.1) at startup. IdentifiedActuator DCMotor with a Coulomb and viscous friction, static 0.3 hip and roll, 0.8 knee, 0.1 ankle. Config class SDBRS1BlindFlatEnv2Cfg, robot SD_BRS1_IDENTIFIED_CFG2 which binds SD_BRS.urdf, the primitive twin kinematically identical to Assembly2. Reward formulas, base_height_rough_l2 is sum(root_z minus target)^2, flat_orientation_l2 is sum(proj_grav_xy^2), action_rate_l2 is sum((a_t minus a_{t-1})^2), ActionSmoothnessPenalty is sum((a_t minus 2 a_{t-1} plus a_{t-2})^2) with the first three steps zeroed, all in mdp/rewards.py. The CALLER map for backwards compatibility, foot_clearance_reward_v2 is SD_BRS1 EXCLUSIVE and editable in place, base_height_rough_l2 and no_fly and keep_ankle_pitch_zero_in_air and feet_slide are shared with TRON1 SF, feet_regulation is shared with SF and PF, GaitReward is shared with PF and commented out in SF, ActionSmoothnessPenalty and feet_distance touch all four tasks, and flat_orientation_l2 with the other l2 penalties are isaaclab library functions not to be edited. A WEIGHT change in the SD_BRS1 RewardsCfg is config local and safe, only a FUNCTION edit triggers the optional argument or v2 rule.

The PLAN, `/ws/plans/NATURAL_GAIT_PLAN.md`, three ordered phases, knee FIRST because it is causally upstream of the lean and the shock. Phase A a new swing gated knee flexion reward, r = sum over feet i of (1 minus desired_contact_i) times exp(minus (q_knee_i minus q_target)^2 over sigma^2) with q_target 0.5 to 0.8 rad, plus an optional action offset recentre onto a bent nominal, a new term wired to SD_BRS1 only. Phase B the bad_orientation termination near 0.8 rad which is recorded but unapplied, plus an optional directed forward pitch penalty relu(g_x minus g_ref)^2. Phase C raise the existing smoothness weights which is config local, add a new torque rate penalty sum((tau_t minus tau_{t-1})^2), clip the action, correct the ankle damping toward zeta 0.7, then add CAPS to the rsl_rl PPO loss.

CRITICAL, the WORKING TREE has DIVERGED from the analysed 2026-07-22 env.yaml, confirmed against the newest run `2026-07-23_10-32-20`. The current `cfg/SF/brs_base_env_cfg.py` RewardsCfg uses pen_base_height target 1.0 at weight minus three hundred, not 1.14 at minus fifteen, which by the section 2.4 arithmetic forces roughly 0.94 rad of knee crouch, and it has already added pen_joint_torque_rate via the JointTorqueRatePenalty class at minus 1e-3, raised pen_flat_orientation to minus fifty, and raised the action rate, action smoothness, and joint accel weights. The user has trained four runs on 2026-07-23 from this changed config. Always read the newest run's env.yaml, the working tree is being actively tuned. The knee defaults are still zero in `robots/brs_solefoot_env_cfg.py`, no action offset recentre yet.

## Seventeenth pass, Phase A trained and FAILED, the root cause is the nominal pose (2026-07-27)

Run `2026-07-23_11-31-57`, 21161 logged iterations, trained the Phase A knee flexion reward. It FAILED completely and the failure is now fully diagnosed in `/ws/plans/NATURAL_GAIT_PLAN.md` sections 2.5 to 2.9 and 3.5 to 3.8. TensorBoard has no python package in this container, the event file was parsed with a hand written TFRecord and protobuf reader kept in the scratchpad as `tb_parse.py`, which is worth reusing.

The knee reward was MATHEMATICALLY DEAD, not merely weak. `Episode_Reward/rew_knee_flexion` ranged 6.57e-10 to 1.05e-6 against a saturation of 10, one ten millionth of its ceiling. The kernel `exp(-(q-1.1)^2/0.2^2)` was centred 0.88 rad from where the policy stood with a tolerance of 0.2, so the operating point sat 4.4 tolerances into the tail. Gradient at the operating point was 1.7e-6 per radian against 11.1 for a linear reward of the same weight, a ratio of 1.6e-7. Because the term is monotone in the knee angle its magnitude INVERTS to measure the knee, `q = t - s sqrt(-ln(rate/(w f)))` with airborne duty f=0.8, giving a swing knee of 0.22 rad (12.6 deg) FLAT across the whole run, 0.218 at iteration 423 and 0.213 at 21160. The duty f is confirmed independently by `no_fly` mean 0.778 implying 77.8 percent single support. Second obstacle, the default knee is 0 so holding 1.1 rad needs a SUSTAINED action of 2.75 at scale 0.4 against exploration std 0.742, i.e. 3.7 sigmas, with no gradient pointing there.

REWARD BUDGET of that run, the inversion rule `Episode_Reward/x divided by weight = mean func value` validated by accounting, 24 rates sum to +36.04/s which over the 20 s horizon predicts 720.7 against a logged `Train/mean_reward` of 710.8. Positive side +55.80/s, negative side -19.76/s. Positive, rew_lin_vel_xy 27.19 (48.7 pct), rew_no_fly 11.68 (20.9), rew_foot_clearance 8.90 (15.9), rew_ang_vel_z 5.97 (10.7), feet_air_time 1.91 (3.4), rew_knee_flexion 1e-7 (one part in 6e8). Negative, rew_gait -6.79 (34.4 pct), pen_base_height -4.21 (21.3), pen_action_smoothness -2.08, pen_ang_vel_xy -2.06, pen_joint_torque -1.78, feet_slide -0.90, pen_flat_orientation -0.87. Had the knee term saturated it would be 15.2 pct of the positive budget, so the WEIGHT was right and only the KERNEL was wrong.

THREE further defects found. One, `rew_gait` is a PURE PENALTY bounded in [-2, 0], since `tracking_contacts_shaped_force` and `_vel` are both -1.0 which selects the `(1 - exp(...))` branch, so it can NEVER be positive despite the name and the +40 weight, and at -6.79/s it is the LARGEST penalty in the stack. Two, a STALE CONTACT INDEX, `ContactSensor` rolls with the newest sample at index 0 (`contact_sensor.py:377-378`), so `[:, -1]` is the OLDEST, and `no_fly` (rewards.py:342), `keep_ankle_pitch_zero_in_air` (:377) and `knee_flexion_in_swing` (:417) all read the two OLDEST of the 4 slot buffer, a 10 to 15 ms lag, while `no_contact` (:434) correctly reads `[:, 0]`, proving the inconsistency. The max-over-history users, feet_slide and both clearance rewards, are order independent and fine. Three, `pen_joint_pos_limits` costs 0.297/s STRUCTURALLY at the zero default, since the knee soft lower edge is 0.07415 and both knees sit below it, which is 78 pct of that term's whole observed value.

THE BASE HEIGHT TARGET IS KINEMATICALLY UNREACHABLE and this is the biggest finding. For a balanced flat footed vertical torso squat, `H = 0.305077 + 0.44 cos(hip) + 0.43 cos(ankle)` with the closed chain `l1 sin(hip) = l2 sin(ankle)` and `knee = hip + ankle`. The ANKLE PITCH limit 0.454 binds FIRST and binds decisively, giving a floor of H = 1.0891 m. The configured target of 1.0 m needs an ankle of 0.654 rad, 44 pct beyond the limit, INFEASIBLE. The knee does not reach its own soft edge until about 0.968 m, twelve centimetres below where the ankle has already hard stopped. So the -300 penalty demands a pose that does not exist, is never discharged, pays a flat 4.21/s forever, and is instead discharged by TRUNK PITCH and LEG SPLAY. The height penalty is therefore BUYING the forward lean that `pen_flat_orientation` at -50 is paying to prevent, and it outweighs it several fold. Measured stance 1.119 m, whereas a balanced squat at the measured knee 0.22 would be 1.170 m, so about 0.05 m of descent comes from lean and splay not from the knee. The OLD 1.14 m target was comfortably feasible (knee 0.570, ankle 0.288), so the recent change swapped a feasible target weakly enforced for an infeasible one strongly enforced.

SIGN CONVENTION, verified from `SD_BRS.urdf` axes, all joint origins are `rpy=0` so rotations compose additively. `HipPitchR` axis (0,-1,0) and `HipPitchL` axis (0,1,0), MIRRORED, opposite numeric signs. `KneePitchR` and `KneePitchL` BOTH (0,1,0), NOT mirrored, same sign. `AnklePitchR` and `AnklePitchL` BOTH (0,-1,0), NOT mirrored, same sign. Confirmed independently by the limits, KneePitch is [0, 1.483] on BOTH legs so positive must be flexion on both, while HipPitchR (-0.75,1.25) against HipPitchL (-1.25,0.75) is a mirrored pair. AnklePitch is definitively (-0.454, 0.454) on both, the (-0.349, 0.75) figure recorded earlier is WRONG for this asset. `HipYaw` is `type="fixed"` on both legs, so there are ten actuated DOF and HIP ROLL is the entire off axis set.

RECOMMENDED NOMINAL POSE, H=1.15 m, HipPitchR +0.2379, HipPitchL -0.2379, KneePitchR/L +0.4814, AnklePitchR/L +0.2435, hip rolls 0. Knee sits mid soft band, ankle keeps 0.165 rad (40 pct) of soft range in reserve. IK is `k = arccos((d^2 - l1^2 - l2^2)/(2 l1 l2))`, `beta = atan2(l2 sin k, l1 + l2 cos k)`, `ankle = k - beta`, `d = H - 0.305077`.

CONFIG FACTS CORRECTED. `init_state.pos` is 1.2 m NOT 1.3 m (`sd_brs1_identified_cfg.py:118`), and `reset_robot_base` has no z key so it is exact at every reset. `sd_brs1_identified_cfg.py:118-130` ALREADY contains a crouch attempt (hip/knee/ankle all 0.2618) but it is DEAD CODE, overwritten by `_JOINT_POS_DEFAULTS` in `brs_solefoot_env_cfg.py` at lines 48, 82, 121, and it is also arithmetically wrong, placing the ankle 0.114 m forward of the hip since the correct hip for knee 0.2618 is 0.1294. The four reset events all ADD to the default and were written for a zero default, so they must be recentred with any pose change, `reset_knee_pitch_joints` (0.0,0.475), `reset_hip_pitch_r_joint` (-0.25,0.42), `reset_hip_pitch_l_joint` (-0.42,0.25), `reset_ankle_pitch_joints` (-0.113,0.25). `pen_feet_regulation` carries its OWN `base_height_target` of 1.0 that must move with `pen_base_height`.

LITERATURE VERDICT, cluster 11 in `/ws/context/literature.md`. NO mainstream humanoid system obtains knee flexion from a reward, all of them carry it in the DEFAULT POSE, IsaacLab G1 knee 0.42 hip -0.20 ankle -0.23, H1 knee 0.79 hip -0.28 ankle -0.52, unitree_rl_gym G1 knee 0.30, Walk These Ways Go1 calf -1.5. Where a reward touches posture it is `joint_deviation_l1` pulling BACK toward that flexed default, applied in G1 to hip yaw and hip roll at -0.1 and DELIBERATELY NOT to hip pitch or the knee. Digit, the closest biped analogue, adds a knee deviation at only -0.2 plus `bad_orientation` at limit_angle 0.7. NO IsaacLab humanoid config uses a height termination at all, they use torso contact, so our 0.65 m floor against a 1.15 m stance truncates exactly the crouched states where knee flexion would be discovered. Kernel width must be sized to the CURRENT error scale, legged_gym `tracking_sigma` 0.25 in squared units, and Peng arXiv 2004.00784 spans interior coefficients 0.1 to 40 across quantities.

THE REVISED PLAN is `/ws/plans/NATURAL_GAIT_PLAN.md` section 5.2, Phase A2, ONE coordinated experiment since the parts are mutually dependent. Recentre `_JOINT_POS_DEFAULTS` onto the crouch, move `pen_base_height` to target 1.15 and weight -30, move `pen_feet_regulation`'s target with it, recentre the four reset events, REMOVE `rew_knee_flexion` (the G1/H1 recipe carries none) keeping the function unwired for later, add `joint_deviation_l1` on `HipRoll[LR]` at -0.1, fix the contact index via an optional `history_index` argument defaulting to -1 so the shared TRON1 callers are untouched, lower `low_height` from 0.65 to about 0.45, and move `init_state.pos` to about 1.17. A monotone knee reward `clip((q - 0.4814)/(0.9 - 0.4814), 0, 1)` gated to swing is held as the contingency if the stance knee moves but the swing knee does not.

## Eighteenth pass, Phase A2 IMPLEMENTED, the current state of the configuration (2026-07-27)

Phase A2 of `/ws/plans/NATURAL_GAIT_PLAN.md` section 5.2 is now APPLIED in the working tree. This pass records the state of the code AS IT NOW STANDS, so a future session need not re read the files to know what trained. All four touched files py_compile clean.

THE NOMINAL POSE is now a crouch, carried IDENTICALLY in both places that define it, `_JOINT_POS_DEFAULTS` in `tasks/locomotion/robots/brs_solefoot_env_cfg.py` (the operative one, it overwrites the asset at lines 48, 82, 121) and `init_state.joint_pos` in `assets/config/sd_brs1_identified_cfg.py`. Values, HipRollR/L 0.0, HipPitchR +0.2379, HipPitchL -0.2379, KneePitchR/L +0.4814, AnkleRollR/L 0.0, AnklePitchR/L +0.2435. Standing height 1.15 m. Spawn `init_state.pos` moved 1.2 -> 1.17, keeping the previous 20 mm settling margin. The old misleading docstring claiming a 1.1 m IK squat is REPLACED by the actual closed chain derivation and the sign convention.

REWARD AND EVENT CHANGES in `cfg/SF/brs_base_env_cfg.py`. `pen_base_height` target_height 1.0 -> 1.15 and weight -300 -> -30. `pen_feet_regulation` base_height_target 1.0 -> 1.15, its `height_decay_scale` left at 0.03 since 0.03/1.15 = 0.026 still matches the guiding comment. `low_height` minimum_height 0.65 -> 0.4. The four reset events recentred so each STRADDLES the new nominal, `reset_hip_pitch_r_joint` and `reset_hip_pitch_l_joint` and `reset_knee_pitch_joints` all to (-0.24, 0.24), and `reset_ankle_pitch_joints` to (-0.15, 0.15), narrower because the ankle keeps only 0.165 rad of soft limit margin at the nominal. NEW term `pen_hip_deviation` using the library `mdp.joint_deviation_l1` on `HipRoll[LR]` at weight -0.1, the G1 weight, which also discharges the dead import. HipYaw is `type="fixed"` in the URDF so hip roll is the ENTIRE off axis set, there is no hip yaw DOF to penalise.

THE KNEE REWARD was NOT removed, by direction, so that an ablation can measure the pose change with and without it. A NEW function `knee_flexion_in_swing_v2` was added to `mdp/rewards.py`, a MONOTONE ramp `sum_legs airborne_i * clip((q_i - nominal)/(cap - nominal), 0, 1)` with nominal 0.4814 and cap 0.9, bounded [0,2], wired as `rew_knee_flexion` at weight 10. A v2 rather than an optional argument because the quantity changes meaning, flexion RELATIVE to the stance nominal rather than proximity to an absolute angle, so rule 5 of ../CLAUDE.md applies. The v1 `knee_flexion_in_swing` is LEFT UNTOUCHED and unwired, which keeps the dumped env.yaml of run `2026-07-23_11-31-57` replayable, and its docstring now RECORDS the defect per rule 6. Gradient of the v2 at the new nominal is 10/(0.9-0.4814) = 23.9 per radian against the 1.7e-6 of the v1. IMPORTANT COUPLING, the ramp is FLAT BELOW its nominal, so it would have been just as silent as v1 without the pose change, since the old operating point of 0.22 rad lies beneath its foot. The pose is what places the policy inside the active region.

APPLIED after the survey, section 5.2.6, the stale contact index. `no_fly` and `keep_ankle_pitch_zero_in_air` now take an optional `history_index: int = -1` whose DEFAULT REPRODUCES THE STALE BEHAVIOUR BIT FOR BIT, so the TRON1 SF caller is untouched, and `cfg/SF/brs_base_env_cfg.py` sets it to 0 on both terms and nowhere else. `keep_ankle_pitch_zero_in_air` derives its second sample as `previous_index = history_index - 1 if history_index < 0 else history_index + 1`, the adjacent OLDER slot in either direction, so -1 pairs with -2 (old) and 0 pairs with 1 (correct, the ancestor's current-or-previous debounce). `knee_flexion_in_swing_v2` reads indices 0 and 1 directly with no argument, being SD_BRS1 exclusive with no prior run to preserve. Semantics VERIFIED NUMERICALLY by emulating the roll and write over four steps, index 0 returns the newest, -1 the oldest, the default pair returns the two oldest as before and the explicit pair the two newest. CONSEQUENCE FOR THE EXPERIMENT, `no_fly` is 21 pct of the positive budget and its value will now shift for reasons unrelated to the pose, so recompute the budget against the new run rather than differencing term by term against the old, though the A2-a against A2-b comparison is unaffected since the correction is common to both arms. THE VERDICT that justified it. The survey verdict is that it is a PORTING BUG, not a design, on five independent grounds. One, IsaacLab DOCUMENTS the ordering explicitly, `contact_sensor_data.py:84-91` states that in the history dimension the first index is the most recent and the last index is the oldest, so `[:, -1]` reads the oldest BY CONTRACT. Two, NO first party IsaacLab code reads a trailing index anywhere, a repo wide sweep finds only `[:, 0]` or a max or mean over the whole axis, and the framework's own debounce primitives `compute_first_contact` and `compute_first_air` avoid raw history slicing entirely, using tracked contact and air time scalars. Three, the Isaac Gym sibling `limxdynamics/tron1-rl-isaacgym` has `_reward_keep_ankle_pitch_zero_in_air` built on `contact_filt = contact OR last_contacts` with an INSTANTANEOUS force tensor and a manually cached previous step, so the intended semantics are current OR previous and `[:, 0]` with `[:, 1]` would reproduce them exactly. Four, the direct IsaacLab ancestor `Andy-xiong6/bipedal_locomotion_isaaclab` has `no_fly` reading `[:, 0, :, 2]` CORRECTLY in its first commit and every commit after, and does not contain `keep_ankle_pitch_zero_in_air` at all. Five, the git history shows the defect arriving in two unremarked steps, the genesis vendor squash `b5217a9` already had `keep_ankle_pitch_zero_in_air` buggy while `no_fly` and `no_contact` were correct, and commit `a2f1e2d` (2026-02-24, "Biped Locomotion Policy Learning Updates") REGRESSED `no_fly` from `[:, 0]` to `[:, -1]` as an unremarked side effect of editing beside the buggy neighbour, its message concerning a velocity curriculum and saying nothing about contact timing. A later commit `ee6e4ed` fixed a DIFFERENT bug in the same function, hardcoded joint indices, without noticing this one, so the idiom has survived two dedicated bug fixing passes. NO implementation, paper or comment anywhere deliberately delays a contact signal, and the only plausible motive, chatter rejection, is fully served by a disjunction over the two NEWEST samples.

CALLER SET CORRECTION from the same sweep. `no_fly` and `keep_ankle_pitch_zero_in_air` are called ONLY from `cfg/SF/limx_base_env_cfg.py` (TRON1 SF) and `cfg/SF/brs_base_env_cfg.py` (SD_BRS1). TRON1 PF does NOT call either. `no_contact` has ZERO callers and is dead code. So a correction's blast radius is ONE other task.

`knee_flexion_in_swing_v2` deliberately REPRODUCES the existing `[:, -1]` and `[:, -2]` convention rather than quietly correcting it, so all four contact gated terms share one gate until the user decides, and that choice is recorded in its docstring.

THE PENDING EXPERIMENT is a two run ablation, A2-a with `rew_knee_flexion` wired and A2-b with that single term commented out, everything else common. Gates in plan section 5.2.8. AS OF THIS WRITING the working tree sits at the A2-b arm, the user having commented the `rew_knee_flexion` RewTerm out, so check whether that block is live before reading any new run's reward channels. A useful instrument, since the v2 term is monotone in the swing knee its logged value again MEASURES that knee, the realised rate divided by weight 10 and by the airborne duty 0.78 giving the mean normalised flexion beyond the nominal.

BASE HEIGHT SITES, the full sweep, so this need not be redone. `pen_base_height` target_height and `pen_feet_regulation` base_height_target in `cfg/SF/brs_base_env_cfg.py` are the two SD_BRS1 ones, both now 1.15. `cfg/PF/limx_base_env_cfg.py` carries 0.65 for TRON1 PF and is UNTOUCHED. `mdp/rewards.py` `nominal_foot_position` takes a base_height_target but is wired into NO config. `mdp/terminations.py` `root_height_below_minimum` carries the termination floor, handled separately.

Phase A of `/ws/plans/NATURAL_GAIT_PLAN.md` was IMPLEMENTED in an earlier session on the user's instruction. A new project local reward `knee_flexion_in_swing` was added to `mdp/rewards.py`, r = sum over legs of exp(minus (q_knee minus target)^2 over std^2) gated by an airborne indicator, the two frame contact history filter of keep_ankle_pitch_zero_in_air, paired knee to foot by ordered lists left before right. It is wired as `rew_knee_flexion` into the SD_BRS1 RewardsCfg only, weight 10, target 1.1 rad (above the current crouch), std 0.2, sensor Link6[LR]. Star exported through mdp so no __init__ edit, both files py_compile clean, backwards compatible since the function is new and the RewTerm is SD_BRS1 only. Training is the user's action. If the reward alone cannot lift the knee, the contingency in plan section 5.1.5 is the action offset recentre. Phases B and C of section 5 stay reserved.

## Nineteenth pass, Phase A2-b evaluation, narrow stance and hip rotation (2026-07-29)

### Run analysed

`sd_brs1_flat/2026-07-27_07-19-39`, Phase A2-b (rew_knee_flexion commented out, pose recentred onto the crouch nominal), 16987 iterations, final quartile cutoff at iteration 12741. Dumped `params/env.yaml` confirms `min_feet_distance 0.21`, pen_base_height target 1.15 weight -30, pen_flat_orientation -50, pen_ang_vel_xy -2.0, rew_gait 40, all matching the eighteenth pass state except that the user set `min_feet_distance` to 0.21 (lower than the 0.30 now in the working tree).

### Video evidence (8 frames extracted at 3.75 s intervals, 3001 frames, 30 s, 100 fps)

Frame 0 is black (loading). Frames 375 through 2625 show the robot walking with alternating feet, knees visibly bent at the crouched nominal, tracking the commanded heading. Three defects are conspicuous. One, the feet are planted very close together, nearly touching in several frames, the lateral stance width is well below the 0.26 m hip width and sometimes approaches the 0.19 m collision threshold. Two, the torso oscillates in roll about its longitudinal (x) axis during each step cycle, the hips rotating in place rather than swaying laterally. Three, a residual forward lean persists, smaller than the earlier runs (pen_flat_orientation reads only 5.7 degrees RMS) but visible in profile.

### TensorBoard final quartile summary (iterations 12741 to 16987, n=4247)

Positive reward rates per second, rew_lin_vel_xy 32.54, rew_no_fly 14.00, rew_foot_clearance 13.45, rew_ang_vel_z 6.75, feet_air_time 2.48, keep_balance 0.049, keep_ankle 0.104, total positive 69.38. Negative rates, rew_gait -7.17, pen_action_smoothness -2.38, pen_ang_vel_xy -1.34, feet_slide -0.83, pen_feet_distance -0.57, pen_flat_orientation -0.50, pen_joint_torque -0.48, pen_joint_pos_limits -0.24, pen_action_rate -0.16, pen_joint_torque_rate -0.15, pen_joint_accel -0.07, pen_feet_regulation -0.04, pen_hip_deviation -0.01, pen_base_height -0.01, pen_lin_vel_z -0.008, pen_joint_vel_l2 -0.001, pen_undesired_contacts -0.00007, total negative -13.93. Net 55.45/s, mean reward 1108.2 over mean episode 1977.6 of 2000 steps.

Training metrics, learning_rate settled at 3.1e-5, Loss/value_function 7.94, Loss/surrogate 0.010, Loss/symmetry 0.061, Loss/entropy 8.27, Policy/mean_noise_std 0.684. Velocity tracking error 0.179 m/s xy, 0.560 rad/s yaw. Termination rates, time_out 96.6%, low_height 3.4%, base_contact 0.0%.

Curriculum, lin_vel_x range widened to (-0.445, 0.890), lin_vel_y to (-0.10, 0.10), ang_vel_z at (-0.30, 0.30) saturated, linear tracking std down to 0.087, angular tracking std 0.267, push force at saturation 3.0.

### Reward budget inversion (rates per second divided by weights)

Using the Episode_Reward = weight times mean_func_value identity. rew_lin_vel_xy, 32.54/50 = 0.651 (good tracking). rew_no_fly, 14.00/15 = 0.933 (93.3% single support, excellent alternation). rew_foot_clearance, 13.45/20 = 0.672 (summed over two feet, about 0.34 per foot). rew_ang_vel_z, 6.75/15 = 0.450. feet_air_time, 2.48/12.5 = 0.198. rew_gait, -7.17/40 = -0.179 (still a pure penalty, bounded [-2,0], partial compliance). pen_flat_orientation, -0.496/(-50) = 0.0099, RMS tilt sqrt(0.0099) = 5.7 degrees. pen_base_height, -0.012/(-30) = 0.000393, RMS height error sqrt(0.000393) = 0.0198 m. pen_ang_vel_xy, -1.34/(-2.0) = 0.670, substantial angular velocity in roll and pitch. pen_feet_distance, -0.566/(-100) = 0.00566, the term is actively firing, the feet penetrate below 0.21 m frequently enough to contribute 0.566/s of penalty, which is the FIFTH largest penalty in the stack. pen_hip_deviation, -0.014/(-0.1) = 0.135 rad = 7.7 degrees of mean hip roll deviation, matching the visible hip roll oscillation.

### URDF geometric analysis for minimum feet distance

The kinematic chain from Part_Torso to Link6 carries a lateral (y) offset of -0.130 m on the right leg (HipYawR origin y = -0.12999628) and +0.130 m on the left (HipYawL origin y = +0.13), with subsequent joints contributing only +0.00045 m (KneePitchR) and -0.00045 m (KneePitchL). The nominal Link6-to-Link6 lateral distance at zero hip roll is therefore 0.259 m. Each foot sole plate is 0.19 m wide (box 0.3156 by 0.19 by 0.046 in the URDF), giving a y half-width of 0.095 m per foot.

The collision avoidance floor, at which the inner edges of the two sole plates would touch, is 2 times 0.095 = 0.190 m. With 1 cm clearance per side the floor rises to 0.210 m, which is exactly the value used in this run, meaning the run's threshold sat at the bare geometric collision boundary with only a single centimetre of margin. With 2 cm clearance per side the floor is 0.230 m.

Hip roll at its ±0.349 rad (20 degree) limit shifts the foot laterally by approximately 0.994 m times sin(0.349) = 0.340 m (leg length from hip roll joint to sole). Both legs rolling inward would place the feet at y = +0.210 and -0.210, overlapping their sole plates by 0.23 m, which confirms that pen_feet_distance is essential and must act well above the collision floor.

For stability, human walking stance width is 1.0 to 1.3 times the hip width (Perry, Gait Analysis, 1992). The hip width on this robot is 0.260 m, giving a recommended stance width of 0.260 to 0.338 m. The lateral CoM shift needed per step is half the stance width, so at 0.21 m it is only 0.105 m (achievable by torso roll alone), at 0.30 m it is 0.150 m (requiring some lateral sway), and at 0.35 m it is 0.175 m (requiring deliberate hip sway). The current working tree value of 0.30 m sits at 1.15 times the hip width, the lower edge of the recommended band. A value of 0.32 m (1.23 times the hip width) would place it comfortably within the stability band while keeping a 0.13 m margin (6.5 cm per side) above the collision floor.

### Hip rotation root cause and proposed remedies

The hip roll oscillation visible in the video and quantified by pen_hip_deviation (7.7 degrees mean) and pen_ang_vel_xy (func value 0.670, meaning RMS angular velocity of about 0.82 rad/s) is a direct consequence of the narrow 0.21 m stance width. During single support, the CoM must lie over the stance foot's support polygon. With a stance foot at only 0.105 m from the midline, the CoM shift needed is small, and the robot achieves it primarily through torso roll rather than lateral hip translation. A 5.7 degree roll of the torso shifts the CoM laterally by about 1.15 times sin(5.7 degrees) = 0.114 m, which is sufficient for the 0.105 m shift, confirming that roll alone can balance the narrow stance.

Widening the stance to 0.32 m raises the required lateral shift to 0.16 m, which would need about 8 degrees of roll to accomplish purely through torso rotation, exceeding the 5.7 degree operating point and incurring substantially more pen_flat_orientation cost (-50 weight is steep at 8 degrees). The policy should therefore redistribute the lateral balance from roll toward hip sway, which is kinematically available through hip roll actuation (the joint, not the base roll) combined with ankle inversion.

Three ordered interventions are recommended.

First, raise min_feet_distance from 0.21 (run) or 0.30 (working tree) to 0.32 m. This is a pure config change in `cfg/SF/brs_base_env_cfg.py` line 815, no function edited, no backwards compatibility concern since pen_feet_distance is not called from any other task.

Second, raise pen_ang_vel_xy weight from -2.0 to -5.0. The current -2.0 gives a penalty of 1.34/s against a tracking income of 39.29/s (lin + ang), a ratio of 3.4%, far below the G1 reference where ang_vel_xy_l2 is set to -0.05 against track_lin_vel_xy 1.0, a ratio of 5%. At -5.0 the penalty would be about 3.35/s, a ratio of 8.5%, closer to the reference proportion and enough to shift the roll versus sway tradeoff. This is also a pure config change.

Third, if the first two changes are insufficient, a new reward function `lateral_com_over_stance_foot` would reward the base y-position aligning with the stance foot y-position during single support. This is the most principled approach since it directly rewards the desired behaviour, but it requires a new function in `mdp/rewards.py` and should be held in reserve until the config-only changes are evaluated.

### Working tree versus this run, confirmed divergences

The working tree as of 2026-07-29 carries min_feet_distance 0.30 (run had 0.21) but is otherwise identical to the run's env.yaml for all reward weights and parameters. The rew_knee_flexion term is commented out in both the working tree and the run (Phase A2-b arm).

Correction, verified 2026-07-30. The working tree figure is 0.25, not 0.30. The committed value in `brs_base_env_cfg.py` is 0.21, matching the run, and the sole uncommitted change to that parameter raises it to 0.25, which is short of the 0.32 that the nineteenth pass above recommends. The companion recommendation of that pass has by contrast been adopted in full, `pen_ang_vel_xy` carries a weight of minus 5 in the committed configuration. Two further divergences from the run were found in the working tree at the same time, the SD_BRS1 runner configuration raises `max_iterations` from 15000 to 30000, and `mdp/observations.py` gains NaN safe shadows of five Isaac Lab built in observation functions, described in the 2026-07-30 audit section of knowledge_base.md, which silently rebind the observation terms named in this robot's policy group.

### Correction to the eighteenth pass, the history_index argument regressed and was restored (2026-07-31)

A full audit of every reader of `net_forces_w_history` found that the correction recorded in the eighteenth pass had been partially reverted in the working tree. `no_fly` retained its `history_index` argument and the SD_BRS1 configuration retained the explicit zero, but `keep_ankle_pitch_zero_in_air` had lost both the argument and its docstring defect note, and its body had been rewritten to read indices zero and one unconditionally. That form is the correct physics but it is not a correct change, because `cfg/SF/limx_base_env_cfg.py` line 1148 calls the same function for the TRON1 SF task and would have silently acquired the fresh contact frame, invalidating comparison against every TRON1 run recorded before it. The argument has been restored with its default of minus one, the `previous_index` pairing rule and the defect note, and `cfg/SF/brs_base_env_cfg.py` line 700 now sets `history_index` to zero alongside `require_airborne`, so SD_BRS1 reads the current frame and TRON1 SF is untouched. The pairing was re-verified numerically against an emulated roll and write, the default returning the two oldest samples exactly as the reverted hardcoding of minus one and minus two did, and the explicit zero the two newest.

The audit also fixes the boundary of the problem, which the eighteenth pass left implicit. Only two functions ever needed the argument. `feet_slide` at rewards.py:81 and both foot clearance rewards at :134 and :210 take a maximum over the whole history axis and are therefore indexing agnostic, `no_contact` at :533 and `knee_flexion_in_swing_v2` read index zero already, `knee_flexion_in_swing` v1 retains the stale pair but is superseded and unwired, and `robot_feet_contact_force` in observations.py flattens the entire tensor. No further correction is outstanding anywhere in the contact history family.

## Twentieth pass, the four run ablation of 2026-07-28 to 2026-07-30, and the anatomy of the stomp (2026-07-31)

This pass analyses four runs together rather than one, since they were trained from near identical configurations and evaluated on the same day and therefore constitute an ablation already performed. It is the first pass in this record to work from the numpy dump directly rather than from TensorBoard scalars and video frames, so its figures are measurements rather than inversions, and where an inversion of a logged rate is quoted it is quoted as corroboration. The full analysis, its literature survey and the phased remedy are in `../plans/GAIT_EFFICIENCY_PLAN.md`. What follows is the factual residue that a future session should not have to re-derive.

### The four runs and what separates them

`2026-07-28_06-37-24` at 15138 iterations, `2026-07-29_04-38-14` at 27662, `2026-07-29_10-47-14` at 14999, and `2026-07-30_07-59-22` at 26732. Diffing the four dumped `params/env.yaml` establishes that they differ in exactly three things, `min_feet_distance` at 0.21, 0.30, 0.21 and 0.25, `pen_ang_vel_xy` at minus 2, minus 2, minus 5 and minus 5, and a `pen_ankle_deviation` term applying the library `joint_deviation_l1` to `AnklePitch[LR]` and `AnkleRoll[LR]` at minus 0.1, absent in the first two and present in the last two. The last two also lack the `history_index` argument on `keep_ankle_pitch_zero_in_air`, which the 2026-07-31 correction section above records as having been reverted and then restored. A fifth run `2026-07-31_05-11-37` was training as this pass was written and its `env.yaml` is byte identical to that of `2026-07-30_07-59-22`.

Provenance of the artefacts. The eleven evaluation plots and the thirty second video the user supplied are held at `artefacts/2026-07-28_06-37-24-play/`. The video was attributed by a byte identical md5 match against `videos/play/42/rl-video-step-0.mp4` in that run's own directory, and the plots carry the run label in their legends. All four runs carry a play dump at `data/42/dump.npy`, 3001 steps by 32 environments, written on 2026-07-31.

### THE JOINT AND FOOT INDEX ORDER IS LEFT BEFORE RIGHT, and the dashboard labels are transposed

This corrects an assumption that has stood since the third pass and governs the reading of every per joint plot ever exported from this project. The fifteenth pass established that the BODY order in the critic observation is left before right and explicitly noted this contradicted the earlier right first assumption, but the JOINT order was left as right first in `dashboard-brs.py` and in `play.py`. It is not.

The proof is decisive and assumption free. Over the dump, joint index 2 spans minus 1.2521 to plus 0.6090 radians, and the only joint in `SD_BRS.urdf` whose limits admit minus 1.25 is `HipPitchL`, whose range is (minus 1.25, plus 0.75). Joint index 3 spans minus 0.6998 to plus 1.2502, and the only joint admitting plus 1.25 is `HipPitchR`, whose range is (minus 0.75, plus 1.25). The order at that depth is therefore left then right, and the depth major pattern gives the full order as HipRollL, HipRollR, HipPitchL, HipPitchR, KneePitchL, KneePitchR, AnkleRollL, AnkleRollR, AnklePitchL, AnklePitchR.

The FOOT order follows the same convention, established independently. Knee index 4 is below 0.5 radians exactly when foot index 0 reports contact, agreeing on 97.2 percent of steps, and knee index 5 pairs with foot index 1 at 97.3 percent. Since index 4 is the left knee under the ordering just proved, foot index 0 is Link6L.

Consequences. `JOINT_NAMES` and `FEET_NAMES` in `/ws/IsaacLab/logs/rsl_rl/dashboard-brs.py` were both transposed, as was the comment above the feet distance logging in `scripts/rsl_rl/play.py`. Every panel in `artefacts/2026-07-28_06-37-24-play/` labelled R is the left joint or foot and every panel labelled L is the right. The conspicuous asymmetry visible in those plots, the panel titled Ankle Roll R showing a saturating square wave while Ankle Roll L does not, is in truth the LEFT ankle saturating. No aggregate conclusion is affected since every statistic below is computed over both sides, but a reader tracing one limb must apply the correction to the stored artefacts, which are kept as they were exported. The symmetry map of the fourteenth pass, `[1,0,3,2,5,4,7,6,9,8]` with flips on `{0,1,2,3,6,7}`, is unaffected because every swap is between adjacent partners.

WHY THE RULE COULD NOT BE INFERRED, which is what makes the fix a matter of recording rather than of correcting. The breadth-first claim in the original comment is confirmed, the depth sequence being HipRoll, HipPitch, KneePitch, AnkleRoll, AnklePitch. Only the within-depth tie-break is wrong. Two candidate rules explain the observation, name sorting since `L` sorts before `R`, and reversal of the declaration order, and this asset cannot distinguish them because the URDF declares the whole right chain before the whole left chain, so both rules predict left first for every pair. The ordering is therefore not derivable from the asset by any reasoning available here, and any hardcoded list is a standing hazard that would recur on the next robot.

### The correction, APPLIED 2026-07-31

Two changes, both made in this pass, both verified.

`scripts/rsl_rl/play.py` now RECORDS the order rather than leaving it to be guessed. Three keys were added to the `DataLogger.data` dictionary, `joint_names`, `body_names` and `feet_names`, populated once on the first `log` call from `asset.joint_names`, `asset.body_names` and the resolved `feet_ids`. A module level `_METADATA_KEYS` tuple exempts them in `DataLogger.plot` from both the leading axis wrap that every per-step series receives and the `np.stack`, so a consumer reads `data["joint_names"]` as the list itself rather than as a singleton nesting of it. The change is purely additive, no existing key changes and no existing behaviour changes, so the TRON1 callers of this shared script are untouched and dumps predating it simply lack the keys. The stale comment on the feet distance logging was corrected in the same pass, its sign having been given as R minus L where the quantity is in fact L minus R, and the correction is recorded in place per rule 6 of ../CLAUDE.md rather than silently overwritten.

`dashboard-brs.py` now PREFERS the recorded order. `JOINT_NAMES` and `FEET_NAMES` were corrected to left before right and demoted to fallbacks for dumps predating the play.py change, carrying the full provenance of the correction in their comments. Three functions were added, `_names_from_dumps` and the two wrappers `resolve_joint_names` and `resolve_feet_names`, which return the order recorded in the first experiment that carries it and otherwise the fallback. The four use sites now call them, and the module level `NUM_JOINTS` and `GRID_ROWS` were removed in favour of per figure derivation from the resolved list, since a dump may carry a different articulation.

Verified on four cases. A real pre-change dump, which carries no name key, resolves to the corrected fallback. A new format dump resolves to its own recorded list. A foreign articulation of six degrees of freedom resolves to six joints and three grid rows, so the dashboard is no longer hardwired to the SD_BRS1 ten degree of freedom layout. And a mixed set of one old and one new experiment takes the order from the new one. Both files are `py_compile` clean.

### Measured gait, run 2026-07-28_06-37-24, over all 32 environments and 3001 steps

Cadence, over 2016 completed cycles, a MEDIAN period of exactly 1.000 s against a commanded 1.000 with a mean of 0.929 s, the difference being a tail of short cycles whose tenth percentile lies at 0.720 s and which is the signature of an occasional stumble or of a contact breaking and re-forming within one phase. Stance duty 50.9 percent per foot, double support 2.3 percent, single support 97.3, flight 0.4. Linear velocity error 0.223 m/s RMS against a commanded magnitude of 0.53. Yaw rate error 0.741 rad/s RMS against a commanded magnitude of 0.51 and an achieved 0.29, correlation 0.23. Base height 1.157 m with a standard deviation of 0.025 against a target of 1.15. Swing apex sole clearance 0.092 m mean and 0.135 at the 99th percentile against a target of 0.08. Touchdown vertical velocity 1.68 m/s mean and 4.37 worst. Peak contact force within 80 ms of touchdown 3.08 body weights mean, 6.90 at the 95th percentile, 13.76 worst. Horizontal foot speed in swing 3.40 m/s at the 99th percentile against a base speed of 0.61. Mean absolute joint power 850 W against a net mechanical power of 89 W. Mechanical cost of transport 2.38 on absolute power and 0.25 on net.

Per joint, as (range in rad, 99th percentile velocity, 99th percentile torque, 99th percentile acceleration, mean absolute power, percent of steps within 0.02 rad of a limit, percent of steps above 98 percent of the effort ceiling). HipRollL (minus 0.352 to plus 0.350, 2.82, 152, 195, 43 W, 1.0, 0). HipRollR (minus 0.353 to plus 0.353, 2.84, 154, 196, 42 W, 1.2, 0). HipPitchL (minus 1.252 to plus 0.609, 7.14, 209, 978, 106 W, 0, 0). HipPitchR (minus 0.700 to plus 1.250, 7.27, 211, 972, 108 W, 0, 0). KneePitchL (minus 0.005 to plus 1.488, 16.51, 206, 642, 165 W, 37.6, 0). KneePitchR (minus 0.009 to plus 1.486, 16.46, 204, 647, 166 W, 37.1, 0). AnkleRollL (minus 0.361 to plus 0.398, 10.22, 131, 1229, 23 W, 31.4, 6.1). AnkleRollR (minus 0.363 to plus 0.351, 9.99, 131, 1216, 25 W, 31.9, 6.9). AnklePitchL (minus 0.501 to plus 0.606, 16.30, 166, 1730, 88 W, 11.3, 0). AnklePitchR (minus 0.492 to plus 0.619, 15.62, 160, 1901, 84 W, 11.9, 0).

Three facts in that table deserve separate statement. The knee distribution is BIMODAL, 35 percent of the time below 0.05 rad against a hard extension stop at 0, 13 percent above 1.40 against a hard flexion stop at 1.483, and only 15 percent in the intermediate band from 0.3 to 1.1, with a stance mean of 0.086 and a swing mean of 1.25. The ankle roll actuator sits above 98 percent of its 131 Nm effort ceiling for 6 to 7 percent of all steps with the joint at its limit and its velocity near zero, which is the actuator pressing the joint into its mechanical stop, and reaching that torque through the proportional term alone would need a position error of 6.55 rad against a joint whose whole travel is 0.70. And the ankle pitch EXCEEDS its declared hard limit of 0.454 rad, reaching 0.619, while both ankles exceed their 25 rad/s velocity ceilings, reaching 29.5 and 30.7, which is the position solver yielding at `solver_position_iteration_count` 2 under loads of this size.

### Reward budget of run 2026-07-28_06-37-24, final quartile

Positive side 63.04 per second, negative side minus 14.64, net 48.39, against a logged mean reward of 967.4 over a mean episode of 1787.9 of 2000 steps. Positive, rew_lin_vel_xy 30.45 (48.3 percent, func 0.609), rew_no_fly 12.56 (19.9, func 0.837), rew_foot_clearance 11.55 (18.3, func 0.578), rew_ang_vel_z 5.95 (9.4, func 0.397), feet_air_time 2.14 (3.4, func 0.171), rew_keep_ankle 0.34, keep_balance 0.045. Negative, rew_gait minus 7.10 (48.5 percent, func minus 0.178), pen_action_smoothness minus 2.885 (19.7, func 38.46), pen_ang_vel_xy minus 1.838 (12.6, func 0.919), pen_flat_orientation minus 0.815 (func 0.0163, RMS tilt 7.3 degrees), feet_slide minus 0.791 (func 0.158 m/s), pen_joint_torque minus 0.451 (func 45131), pen_joint_torque_rate minus 0.199 (func 19888), pen_action_rate minus 0.188 (func 18.85), pen_joint_pos_limits minus 0.169 (func 0.0845), pen_joint_accel minus 0.085 (func 8.54e5), and eight further terms none of which reaches 0.03 per second. Termination, time_out 70.4 percent and low_height 29.6, base_contact zero. Learning rate 2.73e-5, value loss 12.03, entropy 10.92, noise std 0.833.

The six effort and smoothness terms together supply 3.81 per second, 6.0 percent of the positive budget, and 2.885 of that 3.81 is the action smoothness term alone, leaving 1.5 percent of income spread across squared torque, squared acceleration, squared joint velocity, action rate and torque rate together.

### The gait clock is correctly specified and is being defied in duty, not in phase

`gait_command` samples degenerate ranges, frequency exactly 1.0 Hz, offset exactly 0.5, duration exactly 0.6, swing height exactly 0.08 m, so every environment carries the same clock. Reconstructing the smoothed desired contact state exactly as `GaitReward.compute_contact_targets` does, with the von Mises smoothing at kappa 0.05, gives a commanded stance duty of 58.4 percent per foot and therefore a commanded DOUBLE SUPPORT of 19.6 percent. Against the human figures, 62 percent stance and 24 percent double support at a cadence of 113 steps per minute, the clock asks the robot to walk as a human walks.

The policy complies in phase and defies it in duty. The correlation between the measured contact state and the desired one is 0.928 and 0.931 on the two feet, while the measured double support is 2.3 percent against the commanded 19.6, and the same figure is 1.4, 1.9 and 2.1 percent in the three sibling runs, so it is structural.

Decomposing `GaitReward` over the dump gives force term minus 0.0507 and velocity term minus 0.1851, summing to minus 0.2359 against the logged func value of minus 0.178, the discrepancy being the difference between the dump's evaluation population and the training distribution. The VELOCITY term is 3.7 times the force term, and splitting it further gives 33 percent accrued while the foot is in contact and 67 percent while it is airborne. The gait penalty is therefore principally charging the SWING foot for moving fast during the interval the clock still calls stance, which is the same fast foot motion that `foot_clearance_reward_v2` pays for through its tanh factor.

### Why double support was eliminated, the arithmetic

`no_fly` returns single contact minus five times no contact at weight 15, so its income is 15 per second in single support, ZERO in double support and minus 75 in flight, and `feet_air_time_positive_biped` at 12.5 is likewise gated on exactly one foot in contact. Together they supply 23.3 percent of the positive budget and pay nothing whatever during the weight transfer.

Moving from the observed 2.3 percent double support to the commanded 19.6 at fixed cadence changes four terms, rew_no_fly minus 2.53, feet_air_time minus 0.85, rew_foot_clearance minus 1.76 through the shorter swing, and rew_gait plus 7.02, for a NET of plus 1.87 per second. Compliance is worth 3.0 percent of the positive budget, which is a real gradient but a shallow one and well inside what a policy converged at a learning rate of 2.7e-5 with a noise standard deviation of 0.83 will resolve. The schedule is therefore under determined by the reward, and the tie is broken by whatever is dynamically easier.

The mechanical consequence is the stomp itself. At 2.3 percent double support and a 1 Hz cadence there are 23 ms per cycle in which both feet are loaded, that is 11 ms per transfer, and the whole body weight must move within it. No swing trajectory makes such a transfer gentle. The touchdown aligned mean force profile confirms it, rising 0 to 1.72 to 2.07 to 2.13 body weights over the first 30 ms and decaying to a steady 0.55 by 100 ms.

### Why the clearance profile is a trapezoid, the arithmetic

`foot_clearance_reward_v2` is `exp(minus (sole_clearance minus 0.08)^2 / 0.035^2)` times `tanh(1.0 times horizontal foot speed)`, zeroed in contact, summed over feet, at weight 20. Its integrand depends on the instantaneous height alone, so over a swing of fixed duration its maximiser is the trajectory reaching 0.08 soonest, holding longest and leaving latest.

The measured mean normalised swing profile, 21 samples over the swing of each of 961 and 957 swings, is 0.015, 0.054, 0.079, 0.086, 0.089, 0.092, 0.092, 0.092, 0.091, 0.090, 0.087, 0.085, 0.082, 0.081, 0.080, 0.078, 0.076, 0.074, 0.075, 0.070, 0.025 metres. The foot reaches 92 percent of apex within the first 15 percent of swing, and 74 percent of the swing is spent above 0.07 m.

Evaluating the term over candidate profiles of equal apex and duration at a fixed foot speed of 2.5 m/s gives, in income per second, the observed trapezoid 16.37 (mean kernel 0.852), the same with a gentle descent over the final 30 percent 13.56 (0.705), a symmetric sinusoidal arc of the same apex 11.24 (0.585), and a sinusoidal arc of apex 0.12 giving 9.03 (0.470). The trapezoid earns 46 percent more than the sinusoid of equal apex and 21 percent more than a profile differing only in the gentleness of its descent, so the snap is worth about 2.8 per second and is paid for by nothing, no term in the configuration pricing vertical foot velocity, foot acceleration or landing.

The tanh factor compounds it from the other side, saturating at 0.96 by 2 m/s and 0.995 by 3, so the term pays a foot to travel at least twice body speed, and the measured 99th percentile horizontal foot speed is 3.40 m/s against a base speed of 0.61.

### `feet_distance` measures the wrong axis, so the stance width is not regulated at all

`feet_distance` takes the Euclidean norm of the PLANAR separation of the two ankle frames and hinges below the minimum. At 0.21 m and weight minus 100 it reads 0.014 per second, an implied func value of 0.00014, and its per step trace is flat at zero.

That inactivity is not evidence of a wide stance. The LATERAL separation has a mean magnitude of 0.225 m, a minimum of ZERO, and stands below 0.19 m, the width at which the two sole plates would touch, for 39.8 percent of all steps, while the fore and aft separation has a mean magnitude of 0.217 m and a maximum of 0.767. The planar norm therefore stays above the threshold even when the lateral separation is exactly zero, because at that moment the stride has carried the feet apart along the direction of travel. The `feet_distances.png` panel shows the lateral trace reaching zero and passing below it twice within the plotted window, that is the feet CROSSING the midline, with the total XY panel never dipping below 0.21.

The consequence is that raising `min_feet_distance` cannot regulate stance width in this term's present form, since it raises the threshold against the stride as much as against the stance. This supersedes the nineteenth pass recommendation to raise the value to 0.32, which remains the right target but requires the term to measure the lateral component first. The same figures across the four runs are 39.8, 35.9, 44.6 and 40.9 percent below the overlap width, essentially unmoved by minimum values of 0.21, 0.30, 0.21 and 0.25.

### Why the yaw command is not followed

Uniform across all four runs, yaw RMS error 0.74, 0.69, 0.69 and 0.56 rad/s, untouched by any of the three ablated parameters. The causes are one morphological and four configurational.

Morphologically, `HipYawR` and `HipYawL` are `type="fixed"` in `SD_BRS.urdf`, so the machine has NO actuator with a vertical axis and every yaw moment must come from the ground, either as a friction moment under the stance sole or as the reaction to a change in whole body angular momentum about the vertical.

Configurationally, the stance width is at or below the sole overlap width for 40 percent of the cycle, which minimises the moment arm of any differential foot placement. The `lin_vel_y` command range is plus or minus 0.01 m/s, so the policy has never been asked to step sideways and has no lateral placement skill to recruit. `feet_slide` at minus 5 penalises the horizontal velocity of a foot in contact, which is exactly the stance pivot a flat footed biped uses to turn. And no term anywhere rewards the feet for pointing along the direction of travel, the reference instrument being Booster Gym's feet yaw term at twice its linear tracking weight.

The failure is additionally SELF SUSTAINING through the heading controller. `heading_command` is true with `rel_heading_envs` 1.0 and `heading_control_stiffness` 0.5, so the yaw command is proportional to the heading error rather than sampled, and a robot that cannot turn accumulates heading error, which saturates the command, which it still cannot follow. The measured command sits pinned near 0.887 rad/s against a nominal range of plus or minus 0.3 that the curriculum widened, and the curriculum widened it on a threshold of 0.7 on `rew_ang_vel_z` that the measured func value of 0.397 no longer meets.

The yaw kernel itself is neither dead nor saturated. At a curriculum contracted width of 0.267 and a measured mean squared error of 0.39 it returns 0.49, so the term is simply outbid, at 9.4 percent of the positive budget against the linear pair's 48.3.

### The joints work against one another

The torque against velocity scatter shows the knee with two MOTORING arms, positive torque with positive velocity and negative with negative, so it performs positive work both folding and extending, while the hip pitch scatter lies in the two BRAKING quadrants. The power traces confirm the timing, the knee delivering peaks near 3 kW and the hip pitch absorbing peaks near 2 within the same few tens of milliseconds, with the ankle pitch alternating between plus 1.8 and minus 2.7 kW.

The aggregate is a mean absolute joint power of 850 W against a net mechanical power of 89 W, a ratio of 9.5, so ninety percent of the actuation is antagonistic circulation. The peak instantaneous absolute power is 12.6 kW. The mechanical cost of transport is 2.38 on absolute power, an order of magnitude above the 0.2 of human walking and comparable to the 1.6 to 2 of the previous generation of full scale humanoids, while the same figure on NET power is 0.25, close to human. The trajectory the base follows is not expensive. What the joints are made to pay to produce it is.

### `foot_landing_vel` already exists and would work unmodified

`mdp/rewards.py` carries `foot_landing_vel`, wired into TRON1 PF at `cfg/PF/limx_base_env_cfg.py:403` at weight minus 0.5 with `foot_radius` 0.03 and `about_landing_threshold` 0.08, and commented out in the SF config. It sums the squared vertical foot velocity over feet that are below the height threshold, descending, and not in contact. Evaluated against this dump with the correct SD_BRS1 `foot_radius` of 0.124 and a threshold of 0.10, it takes the value 0.209 and its gate catches 97.4 percent of touchdowns, against 67.7 percent at a threshold of 0.05. It therefore requires no function change, only wiring. The mean absolute vertical velocity when the gate is open is 1.586 m/s with a 99th percentile of 2.876.

A contact force term evaluated over the same dump, in the Humanoid-Gym form of the excess above a threshold clipped at 2000 N, gives a mean value of 33.2 active on 7.2 percent of steps at a threshold of 900 N, 51.8 active on 12.6 percent at 700 N, and 19.2 active on 3.2 percent at 1200 N.

### Cross run ablation results

`pen_ang_vel_xy` at minus 5 rather than minus 2 halves the RMS base roll rate, 0.81 and 1.16 rad/s in the two arms carrying minus 2 against 0.54 and 0.60 in the two carrying minus 5, and reduces the flat orientation func value correspondingly. It should be retained at minus 5.

A `min_feet_distance` of 0.25 or above sharply reduces the knee's residence against its stops, 37.8 and 27.5 percent in the two arms at 0.21 against 5.6 and 4.4 percent at 0.30 and 0.25. The mechanism is not established and is worth a dedicated look, the plausible reading being that a wider stance reduces the lateral instability that the full knee fold was compensating for.

The run whose plots and video the user supplied, `2026-07-28_06-37-24`, is the WORST of the four by episode termination, its `low_height` rate being 29.6 percent against 3.7, 5.8 and the fourth run's comparable figure, so it falls in nearly a third of its episodes. That is the arm with the narrowest stance and the lightest roll rate penalty, and it should not be treated as representative of the configuration line.

### Working tree divergence, flagged

As of 2026-07-31 the working tree has been ROLLED BACK from the configuration the live run `2026-07-31_05-11-37` is training. The tree carries `min_feet_distance` 0.21, `pen_ang_vel_xy` at minus 2, and `pen_ankle_deviation` commented out, while the live run and the committed state carry 0.25 (uncommitted, the committed value being 0.21), minus 5, and the term live. The tree additionally carries the restored `history_index` argument on `keep_ankle_pitch_zero_in_air` that the correction section above records, which the live run predates. The evidence of this pass favours the run's values on both of the reverted items, so the divergence should be reconciled toward the run before any further change is made.

### The remedy

`../plans/GAIT_EFFICIENCY_PLAN.md`, six phases in increasing cost. Phase 1 prices the impact, wiring the existing `foot_landing_vel` at minus 10 with `foot_radius` 0.124 and threshold 0.10, reducing `rew_foot_clearance` from 20 to 12, and adding a threshold hinge on contact force. Phase 2 restores the double support interval through a van Marum grace window, which must be a new `ManagerTermBase` subclass rather than an argument on `no_fly` because the window needs state deeper than the four sample contact history. Phase 3 replaces the clearance kernel with a `foot_clearance_reward_v3` tracking a half sinusoid reference read from the `swing_height` the gait command already declares and no reward reads. Phase 4 gives `feet_distance` an optional `lateral_only` argument defaulting False. Phase 5 widens the lateral command range and adds a feet yaw alignment term. Phase 6 raises the smoothness weights, clips the action, adds a torque tiredness term, and then adds CAPS to the PPO loss. Caller sets verified 2026-07-31, `foot_clearance_reward_v2` and `JointTorqueRatePenalty` are SD_BRS1 exclusive, `no_fly` reaches TRON1 SF, `feet_distance` reaches TRON1 PF and WF, `feet_regulation` reaches SF and PF, `foot_landing_vel` reaches PF, and `ActionSmoothnessPenalty` reaches all four.

### Experiment history table

| Run | Date | Configuration | Iterations | Mean Reward | Key Outcome |
|---|---|---|---|---|---|
| 2026-07-15_06-59-45 | 2026-07-15 | Original gains, zero pose, foot clearance 0.12 | unknown | unknown | Crouched shuffle, forward pitch 40-70 deg |
| 2026-07-17_04-51-50 | 2026-07-17 | Phase 0 gains only (hip/knee damping fixed) | unknown | unknown | Phase 0 baseline |
| 2026-07-17_07-49-02 | 2026-07-17 | Phase 1 partial (clearance 0.20, air time dense, no_fly 15, orientation -35) | ~2500 | unknown | Shuffle persists, tilted foot clearance exploit discovered |
| 2026-07-20_07-52-14 | 2026-07-20 | Self collision fix, foot_clearance_v2, all Phase 1 | 3544 | 1590-1609 | One legged hold, 91%+ single support but no alternation |
| 2026-07-21_06-03-36 | 2026-07-21 | GaitReward clock added at weight 40 | 21992 | ~1400 | First alternation, three residual defects (asymmetry, stiff knee, shuffle in place) |
| 2026-07-22_11-36-53 | 2026-07-22 | Symmetry augmentation added | unknown | unknown | FIRST proper walker, lin_x RMSE 0.08, three naturalness defects (locked knee, forward lean, non-smooth actuation) |
| 2026-07-23_11-31-57 | 2026-07-23 | Phase A knee flexion reward (v1 Gaussian) | 21161 | unknown | FAILED, knee reward mathematically dead (gradient 1.7e-6) |
| 2026-07-27_07-19-39 | 2026-07-27 | Phase A2-b (crouch nominal, no knee reward, contact index fix, min_feet_dist 0.21) | 16987 | 1108 | Walking with alternation, narrow stance, hip roll oscillation, residual forward lean |
| 2026-07-28_06-37-24 | 2026-07-28 | min_feet_dist 0.21, pen_ang_vel_xy -2, no ankle deviation | 15138 | 967 | Walks, but stomps at 3.08 BW and 1.68 m/s, 2.3 pct double support, yaw untracked, CoT 2.38, falls in 29.6 pct of episodes. The run the user's plots and video record |
| 2026-07-29_04-38-14 | 2026-07-29 | as above but min_feet_dist 0.30 | 27662 | 1108 | Best tracking of the four, worst roll rate (1.16 rad/s) and worst base height regulation, knee at stops only 5.6 pct |
| 2026-07-29_10-47-14 | 2026-07-29 | min_feet_dist 0.21, pen_ang_vel_xy -5, pen_ankle_deviation -0.1 | 14999 | 1067 | Roll rate halved to 0.54 rad/s, tilt 5.6 deg, but knee at stops back to 27.5 pct at the narrow stance |
| 2026-07-30_07-59-22 | 2026-07-30 | as above but min_feet_dist 0.25 | 26732 | unknown | Best of the four, roll rate 0.60, knee at stops 4.4 pct, yaw error still 0.56 rad/s. Config of the live run 2026-07-31_05-11-37 |
| 2026-07-31_10-21-10 | 2026-07-31 | Phase 1 PARTIAL, foot_landing_vel -10 and feet_impact_force -1e-2 wired, clearance reduction 20 to 12 NOT applied, min_feet_dist 0.21, pen_ang_vel_xy -2 | 30000 | unknown | FAILED its gate. Touchdown 1.55 to 1.40 m/s against a target of 1.0, peak force 2.35 to 2.19 BW against 2.0. Landing term value fell 44 pct while touchdown velocity fell 5, the policy loitering high and dropping late. Ankle pitch newly saturating at 3.8 pct, power 768 to 910 W, CoT 2.835 to 3.341, double support 1.39 to 0.84 pct. Yaw improved unbidden, error 0.653 to 0.494, correlation 0.275 to 0.515. See the twenty-first pass |
| 2026-08-03_11-19-11 | 2026-08-03 | Phases 1b and 2 COMPLETE, clearance 12, foot_landing_vel_v2 -30 at a 0.06 m true sole gate, feet_impact -3e-2, NoFlyWithGrace at 20 steps, durations 0.62, min_feet_dist 0.25, pen_ang_vel_xy -5 | 29103 | unknown | Phase 1b PASSED, Phase 2 FAILED. Read at 11000 in the twenty-second pass and at completion in the twenty-fourth, which withdraws the provisional Phase 2 pass. Peak force 2.19 to 1.59 BW, sole approach 1.86 to 0.80 m/s, touchdown 1.40 to 0.89 m/s, ankle pitch saturation 100 to 48 pct of ceiling. Double support 0.84 to 11.62 pct at 11k then DECAYED to 7.86 against a gate of 12 and a clock commanding 24. Swing profile single peaked, 1.44 peaks per swing and raised cosine correlation 0.841, but the plateau RETURNED over the second half, within-1sd 0.656 to 0.752, while rew_foot_clearance rose 36 pct, CoT 1.87 to 2.29, power 489 to 626 W. REGRESSIONS, straight legged gait with stance knee flexion 0.039 rad, hip pitch now SATURATED at 100.1 pct of its 500 N·m ceiling with 2.06 pct of steps above 90 pct, ankle roll re-saturated to 100 pct. RECOVERED over the full budget, planar tracking 0.218 to 0.182 which beats PH1's 0.185, low_height 0.385 to 0.216 against PH1's 0.181, yaw correlation 0.296 to 0.430. See the twenty-second and twenty-fourth passes |
| 2026-08-05_07-53-35 | 2026-08-05 | PHASE 3, foot_clearance_reward_v3 at weight 10.0 with std 0.03 reading gait_command, feet_impact threshold 900 to 850 at an unchanged -3e-2. Landing weight left at -30 and impact weight at -3e-2, the two escalations plan section 4.3a derived NOT applied | 29999 | 1004 | PASSED and by a wide margin. Reference tracking error 0.0271 to 0.00798 m, raised cosine correlation 0.860 to 0.945, apex position std 0.123 to 0.067. Sole approach 0.960 to 0.539 m/s, peak force 1.805 to 1.551 BW, worst 8.32 to 5.11. Power 557 to 252 W, CoT 1.953 to 0.890, torque rate rms 2506 to 1081, jerk rms 21180 to 12310. THE STRAIGHT LEGGED GAIT IS REVERSED, stance knee flexion 0.0605 to 0.5747 rad and stance against the extension stop 70.8 to 0.83 pct, hip pitch p99 torque 450 to 97 N·m, low_height 0.233 to 0.0823. Phase 2's 12 pct double support gate incidentally MET at 14.014. Tracking best of four, planar 0.0527 to 0.0470. NEW DEFECT, both ankles parked at their mechanical stops, pitch at its stop for 99.5 pct of swing and the four ankle joints carrying 57.3 pct of the torque budget. See the twenty-fifth pass |

---

## Twenty-first pass, Phase 1 trained and FAILED, and three defects in the term it wired (2026-08-03, run 2026-07-31_10-21-10)

Phase 1 of `../plans/GAIT_EFFICIENCY_PLAN.md` was trained for thirty thousand iterations and evaluated by the same instruments that established the twentieth pass. It failed its gate. This pass records what happened, because the reasons generalise beyond this phase and two of them govern how every play dump this project has produced must be read.

### What was actually implemented

Two of the three prescribed changes. `foot_landing_vel` was wired at minus 10 with `foot_radius` 0.124 and `about_landing_threshold` 0.10, and `feet_impact_force` was written and wired at minus one hundredth with a threshold of 900 newtons. The reduction of `rew_foot_clearance` from 20 to 12 was NOT applied, and both `params/env.yaml` and `params/env.pkl` of the run confirm the weight stood at 20.0. The run also carried the tree's `min_feet_distance` of 0.21 and `pen_ang_vel_xy` of −2 rather than the 0.25 and −5 the plan's preamble asked to be reconciled first, so it differs from its intended predecessor in three parameters rather than two.

### The measured outcome

All figures computed from `data/42/dump.npy` by one script applied identically to both runs, a touchdown being the rising edge of a contact force norm above one newton. That method reports 1.551 m/s on the baseline where the twentieth pass reports 1.68, the difference being the contact detection threshold, so the absolute values below are marginally lower throughout and the comparison is internally consistent.

| quantity | 2026-07-28 baseline | 2026-07-31 Phase 1 |
|---|---|---|
| touchdown vertical velocity, mean | 1.551 m/s | 1.402 m/s |
| sole approach velocity at touchdown | 2.182 m/s | 2.064 m/s |
| peak contact force in 80 ms, mean | 2.354 BW | 2.191 BW |
| peak contact force in 80 ms, worst | 10.315 BW | 7.025 BW |
| double support | 1.39 pct | 0.84 pct |
| mean absolute joint power | 768 W | 910 W |
| cost of transport, absolute | 2.835 | 3.341 |
| ankle pitch torque, 99th pct | 169 N m | 262 N m, its ceiling |
| ankle pitch above 98 pct of ceiling | 0.00 pct | 3.8 pct |
| ankle roll above 98 pct of ceiling | 7.07 pct | 10.7 pct |
| linear tracking error | 0.129 m/s | 0.121 m/s |
| yaw rate error | 0.653 rad/s | 0.494 rad/s |
| yaw correlation | 0.275 | 0.515 |

The guidelines of the plan's section 3.4 held throughout, and the yaw tracking improved substantially without being addressed, which suggests a part of the yaw deficit is a consequence of the impulsive gait rather than of the stance width and the pivot penalty alone. Phase 5 should be re-measured before its weights are set.

### Three defects in foot_landing_vel, and one in the configuration

The term is a TIME INTEGRAL and the policy minimised the integral rather than the impact. Its function value fell 44 percent, from 0.170 to 0.096, while the touchdown velocity fell 5 percent. Averaged over the five control steps before touchdown the baseline descends at 1.26, 1.30, 1.35, 1.43, 1.47 m/s and the Phase 1 policy at 0.36, 0.41, 0.53, 0.74, 1.07 before striking at 1.41. The policy loitered through the upper window where an integral over time accumulates most of its value and dropped through the last three centimetres as fast as before.

The gate measures the WRONG HEIGHT. `foot_landing_vel` computes the clearance as the body frame origin less a constant `foot_radius`, exact only for a level foot. This is the identical proxy `foot_clearance_reward` used and that `foot_clearance_reward_v2` was written to replace, surviving here because the term was ported from the TRON1 point foot task where the foot is a sphere and the proxy is exact. On the SD_BRS1 sole it overestimates the true clearance by 23.5 mm on average and 74.4 mm at the 95th percentile, so the gate stood SHUT on 32.7 percent of the steps at which the true sole clearance was already below 0.10 m.

The term measures the WRONG VELOCITY. It charges the frame's vertical velocity, whereas the collision is governed by the approach velocity of the lowest sole point, and the two differ by the rotational term. At touchdown the sole approaches at 1.41 times the frame velocity in the baseline and 1.47 times in Phase 1, so the ratio moved in the direction the penalty made profitable. The policy rotated its foot into the ground where it was charged for translating it, and the ankle pitch saturation that appeared is that substitution paid for in actuator effort.

The configuration defect is the unapplied clearance reduction, and it is the largest single cause. Take the change actually wanted, a gentle cosine descent over the final thirty percent of swing, apply it to every observed swing while holding apex and duration, and evaluate both terms. The clearance income forgone is 1.95 per second at weight 20 and 1.17 at weight 12. The landing penalty saved is 1.14 per second at −10, 2.28 at −20 and 3.41 at −30. Under the configuration trained, weight 20 against −10, softening the landing was a NET LOSS of 0.81 per second, so the policy was correct to keep stomping and no amount of training would have changed it.

### Two instrumentation findings that govern every play dump

`scripts/rsl_rl/play.py` builds its environment through `parse_env_cfg`, which reads the LIVE SOURCE TREE, so the reward weights in a play dump are those the tree carried when play was run and not those the policy trained under. Here they differed, the play having used `pen_ang_vel_xy` −2 against a tree now carrying −5, `min_feet_distance` 0.21 against 0.25, and a landing weight of −30 against the −10 in `env.pkl`. The behavioural channels are unaffected, since they record what the trained policy did. A per term function value must be recovered by reconstructing the term from the dump, not by dividing a logged rate by an assumed weight. The reconstruction was validated by agreement to four significant figures on `joint_torques_l2`, `joint_vel_l2` and `feet_regulation`.

The dump UNDERSTATES contact force. `feet_impact_force` takes the maximum over the contact sensor's force history and so sees transients falling between two control steps, whereas the dump records the instantaneous force. Its logged value of 183.65 stands against 15.31 reconstructed from the dump, a factor of twelve on the excess above threshold. Every peak contact force this project has quoted, the 3.08 BW of the twentieth pass and the 2.19 above alike, is a LOWER BOUND.

### A correction to the plan's Phase 3, found before it was trained

The plan proposed a half sinusoid `A sin(pi phi)` as the swing clearance reference, asserting that it has zero vertical velocity at both ends. That is false. The derivative of the sine is the cosine, which is greatest where the sine vanishes, so a half sinusoid attains its maximum vertical velocity exactly at lift off and touchdown, 0.66 m/s at this robot's swing duration of 0.38 s and swing height of 0.08 m. It would have commanded the stomp that Phases 1 and 1b pay to remove. The implemented reference is the RAISED COSINE `A sin^2(pi phi)`, whose vertical velocity vanishes at both ends and at the apex and is greatest at the quarter points, which is the property the plan intended and described.

### A correction to the commanded double support figure

Reconstructing the desired contact state exactly as `GaitReward.compute_contact_targets` does and thresholding at one half, the stance duty per foot equals the duration and the double support fraction is `2 * duration - 1` exactly, the anti phase offset of 0.5 making the identity clean. A duration of 0.6 commands 20.0 percent double support, not the 19.6 the twentieth pass recorded, that figure having been an artefact of evaluating the clock on the discrete episode step grid. A duration of 0.62 commands 24.0 percent against a stance duty of 62 percent, the human figures exactly.

### A correction to the caller enumeration

`feet_distance` has FOUR callers, not the two the twentieth pass recorded. It is called from `cfg/PF/limx_base_env_cfg.py`, `cfg/WF/limx_base_env_cfg.py`, `cfg/SF/limx_base_env_cfg.py` and `cfg/SF/brs_base_env_cfg.py`. The SF caller was overlooked. The Phase 4 remedy is unaffected because the `lateral_only` default preserves the planar norm for all three TRON1 callers.

### What is now in the tree

Implemented and wired, awaiting training. Phase 1b, being `rew_foot_clearance` at 12, `pen_foot_landing_vel` repointed at the new `foot_landing_vel_v2` at −30 with a true sole clearance gate of 0.06 m, and `pen_feet_impact` raised to −3e-2. Phase 2, being `rew_no_fly` repointed at the new `NoFlyWithGrace` with `grace_steps` 20, and `durations` raised to 0.62. The twelve point sole table is hoisted to the module constant `SD_BRS1_SOLE_OFFSETS` so the clearance reward and the landing gate cannot drift apart.

Implemented and NOT wired, for review and later phases. `foot_clearance_reward_v3` with the raised cosine reference and the shared `_swing_phase_from_gait_command` helper, `feet_distance` with its `lateral_only` argument defaulting False, `feet_yaw_alignment`, and `joint_torque_tiredness`. None has a caller, so none changes any behaviour.

`foot_landing_vel` and `no_fly` are left exactly as they stand for their TRON1 callers, with all four defects recorded in their docstrings per rule 6 of `../CLAUDE.md`.

The next run therefore confounds Phase 1b with Phase 2. That is deliberate, the two acting on the same defect from opposite ends, and Phase 1 having already spent one run establishing that the impact terms alone do not carry it. Setting `grace_steps` to 0 and `durations` back to 0.6 isolates Phase 1b exactly, `NoFlyWithGrace` at zero grace being bit for bit identical to `no_fly` at the same history index.

---

## Twenty-second pass, Phases 1b and 2 trained and PASSED, and the straight legged gait they bought (2026-08-04, run 2026-08-03_11-19-11)

The corrected phases succeeded. Every change the twenty-first pass prescribed was applied this time, verified term by term against the run's own `params/env.yaml` before anything was computed, and the run met both phase gates at a third of the training budget its predecessor consumed.

### Three confounds that qualify every figure below

The run reached 11000 iterations against the 29999 of `2026-07-31_10-21-10`, so every comparison sets a third of a training budget against a whole one. Phases 1b and 2 were wired together, which was deliberate and is recorded above. And `pen_ang_vel_xy` stood at −5.0 in this run against −2.0 in both predecessors, a change belonging to the tree reconciliation of the twenty-first pass and to no phase of the plan, which matters because it and not the grace window is the cause of the reduced base roll.

### The measured outcome, against both predecessors

| quantity | 2026-07-28 base | 2026-07-31 Phase 1 | 2026-08-03 Phases 1b and 2 |
|---|---|---|---|
| touchdown frame vertical speed, mean | 1.551 m/s | 1.402 m/s | 1.107 m/s |
| sole approach speed at touchdown, mean | 1.910 m/s | 1.864 m/s | 0.706 m/s |
| peak contact force in 80 ms, mean | 2.354 BW | 2.191 BW | 1.584 BW |
| peak contact force in 80 ms, worst | 10.315 BW | 7.025 BW | 4.939 BW |
| double support | 1.394 % | 0.838 % | 11.615 % |
| swing apex position within swing | 0.265 | 0.271 | 0.463 |
| correlation of swing profile with raised cosine | 0.421 | 0.515 | 0.917 |
| clearance peaks per swing, mean | 3.644 | 3.003 | 1.702 |
| mean absolute joint power | 768 W | 910 W | 489 W |
| joint power 99.9th percentile | 9648 W | 12841 W | 3041 W |
| cost of transport, absolute | 2.835 | 3.341 | 1.870 |
| ankle roll torque p99, of a 131 N·m ceiling | 100.1 % | 100.0 % | 51.6 % |
| ankle pitch torque p99, of a 262 N·m ceiling | 63.6 % | 100.0 % | 62.9 % |
| hip pitch torque p99, of a 500 N·m ceiling | 35.1 % | 57.5 % | 81.8 % |
| hip pitch torque, root mean square | 57.9 N·m | 78.1 N·m | 122.4 N·m |
| stance knee flexion, mean magnitude | 0.068 rad | 0.074 rad | 0.030 rad |
| base roll rate, mean magnitude | 0.510 rad/s | 0.554 rad/s | 0.323 rad/s |
| opposed hip and knee power | 108 W | 170 W | 43 W |
| planar tracking error, mean | 0.050 m/s | 0.046 m/s | 0.071 m/s |
| yaw correlation | 0.275 | 0.515 | 0.296 |

### The straight legged gait, the durable finding of this pass

The policy paid for its softer landing by straightening its legs rather than by crouching, which is the opposite of what the rising hip and knee torques would suggest to a reader who had not measured the posture. The mean stance knee flexion fell to 0.030 radians, under two degrees, and the whole cycle mean to 0.381 radians from 0.589, while the base height held at 1.167 metres against 1.168.

Two consequences follow mechanically and both are now measured. A leg held straight cannot use the knee for sagittal work, so the work migrated to the hip, whose torque root mean square rose from 78.1 to 122.4 newton metres and whose 99th percentile reached 82 percent of its ceiling. And a straight leg has no compliance with which to absorb a disturbance, so episodes ending on the low height condition rose from 0.200 to 0.384 at the matched iteration of 11000. The push force curriculum confounds the second in part, reaching its maximum of 3.0 at iteration 7800 in this run against 12000 in the predecessor, but the divergence precedes that, standing at 0.161 against 0.030 at iteration 5000, so the fragility is real and not an artefact of the curriculum.

This is the defect the nineteenth pass recorded as the knee living at its extension stop, unresolved and now more strongly expressed, and nothing in the reward set prices it.

### `NoFlyWithGrace` is saturated, so its coefficient is not a lever

Reconstructing the grace counter over the play dump, the recently single condition is true on 99.796 percent of steps. It is saturated in both arms of the comparison that matters, paying 15.000 per second in single support and 14.754 in double, because 98.4 percent of double support episodes are shorter than the 20 step window and are therefore covered by it. Double support episodes have a median length of 4 steps, a 99th percentile of 12, and a maximum of 34.

Raising the coefficient from 1.0 to 2.0 therefore cannot move the single to double margin, since it multiplies a quantity nearly identical in both branches, and it moves that margin from −0.246 to −0.492 per second, which is the wrong direction. It adds a constant of 14.97 per second to a positive budget of 75.49, a rise of 19.8 percent, which the advantage normalisation at `/ws/rsl_rl/rsl_rl/storage/rollout_storage.py:150` removes while every other shaping term's share falls by 16.5 percent. This was proposed on 2026-08-04 and declined on this evidence, and the finding is recorded here so the proposal is not revisited from recall.

### `feet_air_time` is the term that actually prices double support out

`feet_air_time_positive_biped` gates its entire reward on a single stance condition at `/ws/IsaacLab/source/isaaclab_tasks/isaaclab_tasks/manager_based/locomotion/velocity/mdp/rewards.py:63`, computing `single_stance = torch.sum(in_contact.int(), dim=1) == 1` and zeroing the reward where it is false. It therefore pays exactly zero during double support and 2.741 per second during single support under the present configuration, and that figure is the true marginal cost of the transfer interval. It is the largest single price on double support in the reward set, charged by a term whose stated purpose is to reward long steps, and it is wired at weight 12.5 in `cfg/SF/brs_base_env_cfg.py`.

### The impact term has gone nearly inert, and 800 N is the floor for its threshold

The median peak force at touchdown is now 853 newtons, below the term's own 900 newton threshold, so the term is exactly zero on the median step. It fires on 46.0 percent of touchdowns against 96.2 percent before, and its value fell from 183.6 to 21.8.

Excluding the 80 milliseconds after each touchdown, the steady stance force has a 99th percentile of 813 newtons and a 99.9th of 1123. A threshold of 800 touches 1.15 percent of steady stance while catching 55.2 percent of touchdowns, whereas 700 catches 66.9 percent of touchdowns but touches 4.93 percent of steady stance, at which point the term stops being a collision penalty and becomes a load penalty. The impact and stance force distributions now overlap, an impact mean of 930 newtons against a stance 99th percentile of 813, which is itself the measure of how far Phase 1b carried the gait and is why no threshold below 800 is available.

### The residual swing plateau, which is what remains for Phase 3

The profile is already near sinusoidal, but the residue a set point kernel cannot remove is measurable. The foot spends 65.6 percent of its swing within one standard deviation of the 0.08 metre set point where the raised cosine reference spends 46.0, and 28.4 percent above ninety percent of its own apex where the reference spends 20.0. The clearance tracking error against the raised cosine has a mean magnitude of 0.0225 metres and a 95th percentile of 0.0511, which sizes the v3 kernel width at 0.03.

### `feet_distance` measures the wrong axis, now quantified on this run

The lateral separation of the feet averages 0.206 metres, below the 0.25 metre threshold the hinge nominally enforces, while the planar separation the hinge actually measures averages 0.316 metres and therefore reports compliance. The fore and aft stride component supplies the difference. This is the substitution the nineteenth pass predicted, measured directly for the first time, and it is what the `lateral_only` argument of Phase 4 exists to close.

### A methodological finding that governs all future comparisons

The tracking error metrics do not converge downward with training, so reading a metric at the end of one run against the end of another is unsound wherever a curriculum is active. In `2026-07-31_10-21-10` the planar error ROSE from 0.173 at 11000 iterations to 0.183 at 30000, because `modify_linear_tracking_reward_std` falls from 0.151 to 0.086 over that span and the commanded velocity range widens, so the metric measures a moving target. Comparisons must be taken at matched iteration counts with the curriculum values verified equal. At the matched 11000 with both curricula identical, the planar error is 0.216 in this run against 0.173 in its predecessor, and that 25 percent gap is the honest figure.

### An instrumentation note, correcting the applicability of the twenty-first pass finding

The twenty-first pass established that `play.py` rebuilds the configuration from the live tree through `parse_env_cfg`, so a play dump's reward channels carry the tree's weights rather than the run's. For THIS run the tree matched the trained configuration exactly, verified term by term, so its reward channels may be read directly. The defect stands and applies to any future play whose tree has moved, but it does not contaminate `2026-08-03_11-19-11`.

### What is now in the tree

Implemented and wired, awaiting training. Phase 3, being `rew_foot_clearance` repointed at `foot_clearance_reward_v3` at weight 10 with a kernel width of 0.03 reading `swing_height` from `gait_command`, together with the escalations that section 4.3a of the plan derives, `pen_foot_landing_vel` from −30 to −45 and `pen_feet_impact` from −3e-2 to −5e-2 with its threshold from 900 to 800 newtons.

`foot_clearance_reward_v2` now has no callers and is left in place intact, as are `foot_clearance_reward` v1, `foot_landing_vel` v1, and `no_fly`, with their defects recorded in their docstrings per rule 6 of `../CLAUDE.md`.

Implemented and NOT wired. `feet_distance` with its `lateral_only` argument defaulting False, `feet_yaw_alignment`, and `joint_torque_tiredness`. None has a caller, so none changes any behaviour.

The landing weight escalation is COUPLED to Phase 3 and must not be taken without it. The failure mode of a strong landing penalty is the loitering the v1 term produced, and a set point kernel pays for exactly that hover, whereas a phase reference penalises a foot that is high at the wrong moment and closes it.

---

## Twenty-third pass, the statistics pipeline specified and three instrumentation defects found (2026-08-04)

This pass computed no new gait result. Its subject is the instrument rather than the gait, and it was opened because every pass since the twentieth has re-derived its measurements in a throwaway script, so that the touchdown detector, the peak force window, the swing segmentation, the cost of transport and the reward budget inversion have each been written afresh several times and none survives in the repository. The specification of a pipeline that computes them once is `../plans/GAIT_STATISTICS_PLAN.md` and the factual cache is `gait_metrics.md`. What follows is the residue that bears on the reading of this record.

### THE PER TERM REWARD SERIES IS A RATE PER SECOND, and total_reward is not

`RewardManager.compute` forms `value = func × weight × dt` at `/ws/IsaacLab/source/isaaclab/isaaclab/managers/reward_manager.py:150` and accumulates that into the buffer it returns, which `DataLogger.log_reward` stores as `total_reward`, but at line 157 it stores `value / dt` into `_step_reward`, which is the array the per term loop reads. Every per term series in `rewards.npy` is therefore `func × weight`, a rate per second, while `total_reward` is a per step value. Verified on `2026-08-03_11-19-11`, the mean total is 0.6011485 against a summed term mean of 60.114845, a ratio of 0.010000001 which is the control period exactly.

This had not been recorded anywhere. The consequence for this record is benign, since every budget quoted in the twentieth and twenty second passes was computed as a rate and is correct, but a future analysis that adds the total to the terms, or that treats a term series as a per step reward, is wrong by a factor of one hundred. The useful corollary is that a term value divides directly by its weight to give the unweighted function value with no `dt` bookkeeping, which is the same identity the third pass established for the TensorBoard scalars, the two being the same quantity over different populations.

### A SYMMETRY INDEX MUST NOT BE POOLED ACROSS ENVIRONMENTS

Established by measurement while validating the pipeline. Forming the Robinson index from statistics pooled over every environment cancels the asymmetry, because the left and the right pooled quantities each take the union over environments whose asymmetries point in opposite directions. On `2026-07-28_06-37-24` the ankle roll range of motion index reads 4.68 percent pooled, minus 5.17 as a signed per environment mean, 13.4 as a mean of the magnitudes, and 14.09 in environment zero alone which is the panel the exported plots display.

A pooled index near zero is therefore not evidence of symmetry. The index must be formed per environment and averaged as a magnitude. This also resolves what would otherwise look like a discrepancy against the twentieth pass, which reports a frontal ankle roll index of 41 percent, since a pooled recomputation reports single figures and the two are different statistics rather than a disagreement. The corrected per environment implementation reproduces the twentieth pass structure exactly on the same run, a hip pitch torque index of 5.20 percent mean magnitude against an ankle roll torque index of 27.03 with a ninety ninth percentile of 66.42, which is the sagittal against frontal contrast that pass drew its conclusion from.

### `SOLE_OFFSETS` IS DEAD CODE and the SD_BRS1 sole is applied to every robot

`scripts/rsl_rl/play.py:113` declares `SOLE_OFFSETS = None` and documents it, and the DataLogger note above records the same contract, that under the default only the frame height is logged so TRON1 and every existing caller are unaffected. The code does not read it. The guard at `play.py:299` tests `SD_BRS1_SOLE_OFFSETS is not None`, which is a table literal and therefore always true, and `_sole_clearance` at `play.py:349` transforms that same literal. The SD_BRS1 twelve point sole table is consequently applied to whatever robot is played.

No conclusion in this record is affected, every figure quoted having come from an SD_BRS1 run for which the table is correct, but any TRON1 dump written since the seventh pass carries a `feet_sole_clearances` channel computed from a sole that robot does not have. The repair is specified in section 4.1.5 of the plan and must be taken together with an assignment of `SOLE_OFFSETS = SD_BRS1_SOLE_OFFSETS`, since otherwise it silences the channel for the robot that needs it.

### Five channels the dump does not carry, and the statistic each one blocks

There is no orientation channel of any kind, so the torso tilt quoted throughout this record was inverted from `pen_flat_orientation` and has never been measured, the foot yaw alignment cannot be evaluated, and the expected base position cannot be integrated, the commanded velocity being body frame and its rotation into the world requiring the yaw. There are no actuator or joint limits, so every saturation and limit residence figure in the twentieth and twenty second passes was computed against constants transcribed into a script by hand. There is no episode boundary marker, and inspection of `2026-08-03_11-19-11` finds one reset per environment producing a base position discontinuity of 19.56 metres at step 2998, invisible to any consumer. There is no record of the reward weights, and they cannot be read from the dumped `params/env.yaml` outside the container, that file carrying Python object tags requiring `bipedal_locomotion` to be importable, though the permissive loader in `scripts/analysis/compare_experiments.py` is a workable fallback. And there is no history maximum of the contact force, which is why every peak this project has quoted is a lower bound.

### The pipeline reproduces this record

The reference implementations were validated by computing them over the surviving dumps before the plan was written, and they reproduce sixteen figures from the twentieth, twenty first and twenty second passes, among them the double support fraction at 11.6149 percent against a recorded 11.615, the mean peak contact force at 1.5836 body weights against 1.584, the mean absolute joint power at 768.19 and 488.92 watts against 768 and 489, the absolute cost of transport at 2.8354 and 1.8700 against 2.835 and 1.870, the touchdown frame vertical speed at 1.5510 metres per second against 1.551, and the whole reward budget decomposition including a positive side of 75.492 per second against 75.49. The complete record set runs without a family failing on both runs, 2658 records on one and 2657 on the other.

Four quantities agree less exactly and the residual is the swing segmentation convention rather than the statistic, the raised cosine correlation reproducing 0.892 against a recorded 0.917 and the apex position 0.452 against 0.463, and closing that gap is an open item of the plan.

---

## Twenty-fourth pass, the completed run read against its predecessors at matched budget, and the set point term shown to be an attractor (2026-08-05, run 2026-08-03_11-19-11)

Run `2026-08-03_11-19-11` finished at 29103 iterations and was re-evaluated on 2026-08-05, the dump at `data/42/dump.npy` carrying a timestamp of 06:10 that postdates its final checkpoint. The twenty-second pass read the same run at 11000 iterations and qualified every finding with the budget confound. That confound is gone and this pass reads the three runs matched, which withdraws one gate verdict, reverses one caution, and settles by measurement a question the record could previously only pose by construction.

### The comparison is now matched, and the curricula are verified equal

At their final iterations `modify_linear_tracking_reward_std` stands at 0.0859 in both this run and `2026-07-31_10-21-10`, `modify_push_force` at 2.970 against 3.000, and `modify_command_velocity_lin_x` at 0.87 against 0.89. The one curriculum that differs materially is `modify_angular_tracking_reward_std`, 0.2473 here against 0.2124, so this run faced a looser yaw specification and its yaw figures carry that allowance. Every cross run figure below is taken at these matched values, which is the discipline the twenty-second pass established after the 11000 iteration comparison proved unsound.

### Phase 1b holds, Phase 2 does not

Phase 1b holds and holds more comfortably than at 11000 iterations. The mean peak contact force is 1.591 BW against a gate of 2.0, the worst 5.001 against Phase 1's 7.025, the sole approach speed 0.800 m/s against a gate of 1.4, and the touchdown frame speed 0.889 m/s, which now also clears the original 1.0 gate the twenty-second pass had to set aside. Ankle pitch stands at 47.7 percent of its 262 N·m ceiling against the complete saturation of Phase 1.

Phase 2 fails its gate. The double support fraction reached 11.615 percent at 11000 iterations and DECAYED to 7.858 by the end, against a gate of 12 percent and a clock commanding 24. The provisional pass recorded in the twenty-second pass is withdrawn. The mechanism is effective without being sufficient, having carried the fraction up by a factor of nine from 0.838, and `feet_air_time_positive_biped` remains the term that opposes it, paying exactly zero during double support at `/ws/IsaacLab/source/isaaclab_tasks/isaaclab_tasks/manager_based/locomotion/velocity/mdp/rewards.py:63`.

### THE SET POINT CLEARANCE TERM IS AN ATTRACTOR, established in two independent halves

This is the durable finding of the pass and it settles the case for Phase 3 in the only currency that settles such arguments, which is what a policy does with a full budget and the term left in place.

The within run half. `Episode_Reward/rew_foot_clearance` rises without interruption from 3.315 at 8000 iterations through 3.590 at 11000, 4.080 at 17000 and 4.314 at 20000 to 4.518 at the end, a gain of 36 percent, over exactly the interval in which the swing spent within one standard deviation of the set point rose from 0.656 to 0.752, the cost of transport from 1.870 to 2.294, the mean absolute joint power from 489 to 626 W, and the raised cosine correlation fell from 0.917 to 0.841. The policy spent the second half of its budget buying clearance income and paid for it in shape.

The cross run half. Reconstructing the term from the dumped kinematics rather than from its logged reward, which makes the figure independent of any weight, `foot_clearance_reward_v2` has value 0.69936 on `2026-07-28_06-37-24`, 0.69786 on `2026-07-31_10-21-10` and 0.45054 here. The term pays a THIRD LESS for the profile that is better by every shape measure. Its value is anti correlated with the quality of the quantity it exists to govern.

Together these say the trapezoid of the seventeenth pass and of plan section 3.6.1 is not merely the analytic maximiser but the attractor the policy climbs toward whenever budget remains. The 11000 iteration reading caught the profile at its best only because the policy had not yet finished paying itself.

### A PLAY DUMP'S REWARD CHANNELS CARRY THE TREE'S WEIGHTS, and this one is contaminated

The twenty-first pass established that `play.py` rebuilds the environment configuration from the live source tree through `parse_env_cfg` at `/ws/tron1-rl-isaaclab-cozum/scripts/rsl_rl/play.py:417`, and the twenty-second pass recorded that the tree happened to match the trained configuration for the 11000 iteration dump so its channels could be read directly. That exemption does NOT extend to the completed run's dump. Dividing the logged `rew_foot_clearance` by the value reconstructed from kinematics returns an implied weight of 19.98 against the trained 12.0, which identifies the contamination directly rather than by inference and is the general method for detecting it.

Comparing `params/env.yaml` against the live configuration term by term finds exactly three divergences and twenty three agreements, `rew_foot_clearance` at 20.0 against a trained 12.0, `pen_foot_landing_vel` at -45.0 against -30.0, and `pen_feet_impact` at -5.0e-2 with an 800 N threshold against -3.0e-2 with 900. Rescaling to the trained weights, the corrected budget is a positive side of 77.42 per step, `rew_lin_vel_xy` 62.4 percent, `rew_no_fly` 19.3, `rew_ang_vel_z` 8.0, `rew_foot_clearance` 7.0 and `feet_air_time` 3.1, against a negative side of 15.11 of which `rew_gait` is 6.43. Any future analysis must perform this comparison before reading a reward channel, and the script that does it is worth keeping.

### The 11000 iteration dump no longer exists

The re-evaluation overwrote `data/42/dump.npy` and `data/42/rewards.npy` in place, so every figure the twenty-second pass quotes is now unreproducible from the tree and survives only in that pass and in plan section 4.2a. The validation of the twenty-third pass, which reproduced sixteen figures against that dump, likewise cannot be re-run as written. Neither record is thereby wrong, but neither can be checked, and a future evaluation should be written to a directory named for its checkpoint rather than to a fixed `data/<seed>` path.

### The straight legged gait persists, partly healed, with its cost migrated to the hip

Stance knee flexion recovered from 0.030 rad to 0.039, still under two and a quarter degrees against Phase 1's 0.074, and `low_height` terminations fell from 0.385 to 0.216 against Phase 1's 0.181, having peaked at 0.454 at 8000 iterations. The policy therefore recovered most of the robustness it had lost without recovering the posture that cost it. The price appears in the hip, which is the one torque figure that worsened across the whole run, `HipPitch` now at 100.1 percent of its 500 N·m ceiling with 2.06 percent of all steps above ninety percent of it, against 57.5 percent and no measurable saturation on Phase 1. `AnkleRoll` has also returned to its ceiling after standing at half of it at 11000 iterations. A leg that will not bend must be driven from the hip. This is the second independent run to show it and it is the argument for a knee posture term in Phase 6.

### The impact threshold floor of 800 N must move to 850

The twenty-second pass fixed 800 N as the floor on the 11000 iteration reading, where steady stance had p99 813 N and p99.9 1123, so 800 touched 1.15 percent of steady stance while catching 55.2 percent of touchdowns. On the completed run the stance distribution has risen, p99 now 872 N and p99.9 1129, while the touchdown distribution barely moved, median 898 N and mean 934. A threshold of 800 now touches 2.10 percent of steady stance and catches 72.8 percent of touchdowns, 850 touches 1.28 and catches 60.0, and 900 touches 0.76 and catches 49.5. 850 restores the contamination to the level 800 was chosen to achieve. The rise in the stance figure is itself a consequence of the straight legged posture, since a leg that cannot yield transmits more of a stance load as force.

### The tracking caution of the twenty-second pass is REVERSED

That pass argued from `2026-07-31_10-21-10`, whose planar error ROSE from 0.173 at 11000 iterations to 0.183 at 30000 as its curricula tightened, that no improvement should be expected over a remaining budget. The mechanism is real and those figures stand, but the inference does not. This run's training error FELL from 0.218 at 11000 to 0.182, which beats the predecessor's 0.185, and its evaluation error from 0.071 m/s to 0.051 against the predecessor's 0.046. A tightening curriculum makes the metric a moving target, it does not make improvement impossible, and the two must not be conflated again.

### `feet_distance` measures the wrong axis, now confirmed across all three runs

The lateral separation averages 0.198, 0.198 and 0.194 m across the baseline, Phase 1 and this run, in every case below the 0.25 m threshold the hinge nominally enforces, and its fifth percentile stands at 0.021 m in all three, which is the feet very nearly crossing. The planar separation the hinge actually measures averages 0.323, 0.316 and 0.314 m and reports compliance throughout. The lateral figure lies below threshold for 68.7 percent of this run. That these figures do not move across three runs whose gaits differ substantially confirms the defect to be structural in the term rather than incidental to a policy, which is the strongest form the evidence for Phase 4 could take.

### What is now in the tree, and why it must not be trained as it stands

`foot_clearance_reward_v3` is defined at `/ws/tron1-rl-isaaclab-cozum/exts/bipedal_locomotion/bipedal_locomotion/tasks/locomotion/mdp/rewards.py:427` and HAS NO CALLERS. The active clearance term at `cfg/SF/brs_base_env_cfg.py:984` is `foot_clearance_reward_v2` at weight 20.0, which is the baseline and Phase 1 value that Phase 1b reduced to 12.0. The commented block at `cfg/SF/brs_base_env_cfg.py:1006` that appears to preserve the v3 configuration names `foot_clearance_reward_v2` while passing `command_name`, which v2 does not accept and v3 requires, so it would raise on being uncommented and cannot serve as the record of the intended wiring. The landing weight of -45.0 at line 1105 and the impact threshold of 800 N at line 1161 are both live.

That combination is the one plan section 4.3a expressly forbids. A landing penalty at -45 standing beside a set point kernel reopens the hover exploit of the twenty-first pass, the kernel paying a foot to reach the target height early and hold it while the penalty pays it to arrive slowly, and the two agree on a trajectory that rises promptly and descends as late and as slowly as the gate allows. At weight 20 the kernel pays 9.011 per step against the 5.406 the measured run trained on, which strengthens the attractor by two thirds at the same moment the landing penalty is strengthened by half. No training should be launched from this tree until it is resolved deliberately in one direction.

---

## Twenty-fifth pass, Phase 3 trained and PASSED, the straight legged gait reversed, and the ankle found parked at its stops (2026-08-07, run 2026-08-05_07-53-35)

Run `2026-08-05_07-53-35` trained Phase 3 to completion at 29999 iterations and was evaluated on 2026-08-07. This pass reads it against the three predecessors, all four now carrying a `statistics.npy` computed by the pipeline of `/ws/plans/GAIT_STATISTICS_PLAN.md`, so for the first time every figure below comes from one instrument and no reconciliation between ad hoc scripts is required. Where a figure disagrees with an earlier pass the pipeline governs and the disagreement is recorded.

### The experiment is cleaner than the plan specified, because two changes did not reach it

The dumped `params/env.yaml` differs from its predecessor's in exactly two places out of twenty six reward terms. `rew_foot_clearance` moved from `foot_clearance_reward_v2` at weight 12.0 with `target_height` 0.08, `std` 0.035 and `tanh_mult` 1.0 to `foot_clearance_reward_v3` at weight 10.0 with `std` 0.03 reading `command_name` `gait_command`, and `pen_feet_impact` moved its `force_threshold` from 900.0 to 850.0 at an unchanged weight of −3.0e-2. Every other term is identical in function, weight and parameter.

The two weight escalations that plan section 4.3a derived were NOT applied. `pen_foot_landing_vel` stands at −30.0 and not the −45.0 the plan prescribed, and `pen_feet_impact` at −3.0e-2 and not −5.0e-2, and the threshold went to 850 rather than the 800 the plan first derived and then itself revised upward. The twenty-fourth pass warned that the tree carried a forbidden combination and must not be trained as it stood. Whatever resolved that, the run that was launched carried none of it, and the result is a single term ablation of a kind this programme has not previously achieved. The impact threshold move is worth 0.099 per second against a positive budget of 77, which is 0.13 percent, so the outcome is attributable to the clearance term alone to within that margin.

### THE OUTCOME, four runs on one instrument

| quantity | 07-28 base | 07-31 PH1 | 08-03 PH1b+2 | 08-05 PH3 |
|---|---|---|---|---|
| raised cosine correlation, mean | 0.411 | 0.492 | 0.860 | 0.945 |
| rms error against the reference, m | 0.0428 | 0.0418 | 0.0271 | 0.00798 |
| swing above 90 pct of its apex, pct | 52.38 | 28.57 | 38.10 | 23.81 |
| swing apex position, std | 0.363 | 0.197 | 0.123 | 0.0668 |
| clearance peaks per swing, mean | 3.558 | 3.110 | 1.414 | 1.612 |
| swing apex clearance, m | 0.0920 | 0.0947 | 0.0983 | 0.0716 |
| touchdown sole approach speed, m/s | 1.863 | 1.875 | 0.960 | 0.539 |
| touchdown frame vertical speed, m/s | 1.577 | 1.393 | 0.869 | 0.727 |
| sole to frame speed ratio | 1.182 | 1.347 | 1.104 | 0.741 |
| peak contact force mean, BW | 2.617 | 2.463 | 1.805 | 1.551 |
| peak contact force worst, BW | 10.32 | 8.31 | 8.32 | 5.11 |
| loading rate p99, BW/s | 488.6 | 395.0 | 294.4 | 284.9 |
| double support, pct | 4.432 | 2.867 | 10.269 | 14.014 |
| cycle period, s | 0.986 | 0.986 | 0.970 | 0.924 |
| swing duration, s | 0.468 | 0.476 | 0.431 | 0.395 |
| absolute joint power, W | 772 | 906 | 557 | 252 |
| absolute joint power p99, W | 7729 | 9794 | 3045 | 957 |
| cost of transport, absolute | 2.726 | 3.467 | 1.953 | 0.890 |
| antagonism ratio | 4.068 | 4.491 | 2.834 | 2.745 |
| opposed hip and knee power, W | 55.9 | 88.1 | 40.6 | 9.1 |
| joint velocity p99, rad/s | 14.10 | 13.81 | 9.81 | 5.54 |
| joint acceleration rms | 241.5 | 213.2 | 340.8 | 107.9 |
| torque rate rms | 1911 | 2182 | 2506 | 1081 |
| joint jerk rms | 29990 | 24840 | 21180 | 12310 |
| stance knee flexion mean, rad | 0.0841 | 0.0825 | 0.0605 | 0.5747 |
| stance against the extension stop, pct | 64.4 | 63.0 | 70.8 | 0.83 |
| hip pitch p99 torque, N·m of 500 | 180.7 | 272.3 | 450.3 | 97.1 |
| ankle roll p99 torque, N·m of 131 | 130.5 | 130.6 | 131.0 | 122.2 |
| base height, m | 1.166 | 1.168 | 1.170 | 1.133 |
| planar velocity error, m/s | 0.0559 | 0.0477 | 0.0527 | 0.0470 |
| training error_vel_xy, last 200 | — | 0.1850 | 0.1825 | 0.1610 |
| training error_vel_yaw, last 200 | — | 0.3872 | 0.4207 | 0.4186 |
| low_height terminations | — | 0.180 | 0.233 | 0.0823 |
| training mean reward, last 200 | — | 990 | 798 | 1004 |
| policy entropy, last 200 | — | 8.32 | 7.74 | 6.59 |

### The swing profile, and the gate that cannot be adjudicated

Resampling every swing longer than fifteen steps onto a common phase and averaging, the Phase 2 profile reads 0.017, 0.054, 0.075, 0.086, 0.094, 0.096, 0.093, 0.084, 0.068, 0.045 and 0.014 metres at successive tenths, which is the trapezoid. Phase 3 reads 0.010, 0.019, 0.030, 0.047, 0.064, 0.070, 0.064, 0.049, 0.033, 0.021 and 0.008, which is a raised cosine of amplitude 0.070 m standing on a pedestal of about 9 mm. The rms departure from the commanded reference fell 71 percent and the fifth percentile of the per swing correlation is 0.820, so the shape holds on individual swings and not merely on the average.

The gate of "fraction above 90 percent of apex below 0.22" reads 0.2381 and is MISSED, and the miss is an instrument artefact. That statistic is computed at `/ws/tron1-rl-isaaclab-cozum/scripts/analysis/stats.py:582` from a mean profile resampled onto 21 points, so it takes only multiples of 1/21 = 4.76 percentage points, and the achievable values bracketing the gate are 0.1905 and 0.2381. A gate stated to a hundredth cannot be adjudicated on a quantity quantised to a twentieth. The plan restates the gate on the rms error at 0.015 m, which is continuous.

Two smaller readings qualify the success without undoing it. The peaks per swing rose 1.414 to 1.612, because a raised cosine has a broad flat maximum and a millimetre of ripple near the apex registers as a second peak under the prominence test at `stats.py:565`, so the measure is informative against a trapezoid and uninformative once the profile is smooth. And the apex undershoots the commanded 0.08 m by 12 percent at 0.0704, because a kernel of width 0.03 returns 0.89 of its maximum against a uniform 10 mm deficit while punishing a shape error twice, so the policy correctly bought shape with amplitude.

### THE STRAIGHT LEGGED GAIT IS REVERSED, by a term that says nothing about the knee

This is the largest single behavioural change the programme has produced and it was neither sought nor predicted. The stance knee flexion rose 0.0605 rad to 0.5747, from three and a half degrees to thirty three, and the share of stance spent within 0.02 rad of the full extension stop fell 70.8 percent to 0.83. The share of stance samples below 0.05 rad fell 75.0 percent to 1.07.

The consequences follow the posture. Hip pitch p99 torque 450.3 N·m to 97.1, which is 90 percent of its ceiling to 19. Hip pitch mean |torque| 68.7 N·m to 22.7 and mean |power| 112 W to 23. Knee mean |power| 92 W to 46. `low_height` terminations 0.233 to 0.0823, less than half the Phase 1 figure and the best of the four runs, which is the direct measurement of the disturbance rejection the twenty-fourth pass predicted a compliant leg would restore.

THE MECHANISM, established by measurement and not by inference. The raised cosine has zero vertical velocity at the END of swing, which a set point kernel cannot see, that kernel rewarding a foot for reaching a height and never for the manner of its return. The measured descent speed over the final quarter of swing fell 0.545 m/s to 0.292 and the sole approach at touchdown 0.960 to 0.539. A foot that must arrive slowly and on schedule must begin its descent from a lower apex, 0.0983 m to 0.0716, and a lower apex is only useful if the hip is also lower, the clearance being measured from the ground. Base height 1.170 m to 1.133, which is now BELOW rather than above the 1.15 that `pen_base_height` targets. The crouch is the kinematic precondition of the swing shape, not a correlate of it.

The alternative explanation is ruled out on magnitude. The impact threshold move is worth 0.099 per second, 0.13 percent of the positive budget, against a stance knee flexion that moved by a factor of 9.5 and a hip pitch torque by 4.6.

### THE NEW DEFECT, both ankles parked against their mechanical stops

The gains were bought with a degree of freedom, and the pitch axis has effectively been removed from the machine.

| ankle measurement | base | PH1 | PH2 | PH3 |
|---|---|---|---|---|
| AnklePitch at its stop in SWING, pct | 17.3 | 97.6 | 88.3 | 99.5 |
| AnklePitch at its stop in STANCE, pct | 4.35 | 1.80 | 0.86 | 28.2 |
| AnklePitch position std, rad | 0.183 | 0.225 | 0.219 | 0.067 |
| AnklePitch at its stop AT TOUCHDOWN, pct | — | — | 5.3 | 93.5 |
| AnkleRoll at its stop in SWING, pct | 59.3 | 56.5 | 59.8 | 62.0 |
| AnkleRoll at its stop in STANCE, pct | 0.25 | 1.30 | 0.38 | 5.68 |
| AnkleRoll p99 torque as pct of its 131 N·m ceiling | 99.6 | 99.7 | 100.0 | 93.3 |
| AnklePitch spectral arc length | −27.6 | −34.4 | −26.3 | −47.9 |
| four ankle joints, share of pen_joint_torque, pct | — | — | 25.0 | 57.3 |
| rew_keep_ankle_pitch_zero_in_air, unweighted | 0.451 | 0.108 | 0.111 | 0.0918 |

Hard limits are AnklePitch ±0.454 rad and AnkleRoll ±0.359 left and ±0.349 right, read from `joint_position_limits` in the dump. In swing the left roll pins at its POSITIVE limit and the right at its NEGATIVE, which is the same physical direction on a mirrored pair, so the behaviour is systematic and not noise. The ankle roll defect stands between 56 and 62 percent in EVERY run including the baseline, so Phase 3 aggravated it rather than creating it, whereas the ankle pitch defect is Phase 3's own.

THE JOINT IS HELD, NOT DRIVEN. Sampling the ankle roll at its stop during swing, the mean holding torque is 49.1 N·m, the joint velocity is below 0.1 rad/s on 91.6 percent of samples, and the mean power is 1.74 W. Only 3.2 percent of at-stop samples reach 95 percent of the actuator ceiling, so the 122 N·m p99 is the transient of arrival and not the holding state. The stop supplies the constraint and the actuator supplies a modest bias against it.

THE COST. The four ankle joints carry 57.3 percent of the value of `pen_joint_torque` against 25.0 under Phase 2, the two pitch joints alone rising 9.3 to 42.8 percent as their mean |torque| went 38 to 68 N·m. They dissipate 68 W of the machine's 252 while doing no net work. Charged at −1.0e-5 that is 0.165 per second of a 0.287 total. The behaviour is expensive in physics and free in economics.

WHY IT IS FREE. No term reads the ankle roll position at all. The one term reading the ankle pitch, `keep_ankle_pitch_zero_in_air` at `mdp/rewards.py:925`, is wired at weight 1.0 and reads 0.0918 against a maximum of 0.8599 that the 85.99 percent airborne fraction permits, so the policy FORFEITS 0.768 per second and chooses to. CORRECTION appended the same day, that figure overstates the incentive actually available. The term targets the coordinate ZERO while the ankle pitch default stands at 0.2435 rad, so an ankle at its own default already scores only 0.2545 and one at its stop 0.0888. The REACHABLE lever between the stop and the nominal pose is 0.1657 per second at unit weight, which is 0.21 percent of the positive budget, and 0.602 of the nominal shortfall cannot be recovered at any posture the machine would otherwise adopt. Plan section 4.4 sizes the remedy on 0.1657 and not on 0.768. A behaviour bought at three quarters of a unit per second must be worth more elsewhere, and the two terms reading the sole geometry are the clearance at 6.07 per second and the gait clock at −5.85, both computed from the minimum of twelve sole points at `mdp/rewards.py:76`, whose height depends on the foot's orientation as much as on the leg's pose. Freezing the orientation reduces the tracked quantity to a deterministic function of the shank alone, and a narrow kernel rewards precision. At touchdown the foot arrives at a fixed angle on 93.5 percent of steps against 5.3 under Phase 2, which is the repeatability that reading buys.

A DEFECT OF TARGET recorded per rule 6 of `/ws/CLAUDE.md`, since no reweighting removes it. `keep_ankle_pitch_zero_in_air` penalises the ABSOLUTE joint coordinate at `mdp/rewards.py:970`, taking `torch.abs(asset.data.joint_pos[...])` rather than the deviation from the default. The AnklePitch default is 0.2435 rad before randomisation and reads 0.2163 and 0.2599 after it, so the term's target of zero stands roughly a fifth of a radian from the posture the rest of the configuration treats as nominal, and a perfectly compliant policy standing at its own default could collect at most 0.30 of the term's unit. The `pen_ankle_deviation` now in the tree targets the randomised default instead, so the two terms as wired pull one joint toward points 0.22 rad apart.

FORECAST for the running experiment, recorded before its result so that it is falsifiable. `pen_ankle_deviation` is a `joint_deviation_l1` at weight −0.2 over `AnklePitch[LR]` and `AnkleRoll[LR]` at `cfg/SF/brs_base_env_cfg.py:852`. Evaluated on the Phase 3 gait its unweighted value is 0.7151 rad, the four joints contributing 0.194, 0.159, 0.182 and 0.181, so at −0.2 it charges 0.143 per second. That is 0.16 times the 0.768 per second the policy already forfeits and declines, and 0.18 percent of the positive budget. THE FORECAST IS THAT IT WILL NOT UNPIN THE ANKLE. Matching the forfeited income needs a weight near −1.27, and one, two and five percent of the positive budget need −1.12, −2.24 and −5.59.

### `feet_distance` MEASURES THE RIGHT AXIS IN THE WRONG FRAME, which reverses the twenty-second and twenty-fourth pass findings

Both earlier passes recorded the lateral separation as standing below the sole width for 40 to 69 percent of steps and reaching zero, the feet crossing the midline, and called the defect structural because the figures did not move across runs. Every one of those figures was measured in the WORLD frame. `scripts/rsl_rl/play.py:407` dumps the separation as `body_pos_w[:,0] - body_pos_w[:,1]`, a world frame vector, and `feet_distance_statistics` at `scripts/analysis/stats.py:751` names its components fore and aft, lateral and vertical without rotating them. That quantity equals the stance width only at zero heading, and this robot turns continuously under a heading controller, its yaw covering the whole circle with a standard deviation of 1.76 rad, so the reported lateral separation is a rotating mixture of the stance width and the stride.

Rotated into the base frame by the yaw of the dumped `base_quaternion`, the picture reverses.

| separation, PH3 run | world frame as shipped | base frame, corrected |
|---|---|---|
| mean lateral, m | 0.1916 | 0.2591 |
| median lateral, m | 0.1976 | 0.2469 |
| p5 lateral, m | 0.0202 | 0.2169 |
| p1 lateral, m | — | 0.2045 |
| min lateral, m | 0.0000 | 0.0907 |
| P(lateral below the 0.194 m sole width), pct | 47.90 | 0.27 |
| sign changes of the lateral component | many | ZERO in 96032 samples |

Base frame lateral across the four runs is 0.283, 0.273, 0.258 and 0.259 m with fifth percentiles 0.218, 0.213, 0.216 and 0.217. THE FEET DO NOT CROSS. The stability the twenty-fourth pass read as evidence of a structural defect is real and is evidence of the opposite, a well regulated stance sitting just below its recommended band.

THE DECOMPOSITION IS VALIDATED on both components independently. The base frame lateral never changes sign, the left foot remaining left of the right throughout, which is what a stance width must do. The base frame fore and aft crosses zero at 0.0185 per step, one crossing every 0.54 s against a half cycle of 0.46, which is what a stride must do. And its p99 of 0.4371 m agrees with the 0.4461 predicted from the measured 0.9242 s cycle at 0.4827 m/s. In the world frame BOTH components cross zero and both have |mean| near 0.19 to 0.20, which is the signature of a rotating frame.

WHAT SURVIVES. The algebraic objection is untouched, a planar norm still cannot regulate a stance width. The measured 0.259 m sits at or marginally below the lower edge of the 0.26 to 0.338 m band Perry gives, so there is a case for widening, but it is a case for moving a quantity from the bottom of its band toward the middle and not for correcting a collapse. A lateral hinge at 0.30 m at weight −100, which plan section 4.4 previously specified, would fire on 85.2 percent of steps and charge 4.61 per second against a negative budget of 12.31. It must not be wired.

### INSTRUMENTATION DEFECTS in the statistics pipeline, two found this pass

THE TORQUE AND VELOCITY CEILINGS ARE THE SIM DEFAULTS, so every saturation statistic is inert. The dump records `joint_effort_limits` as 1e9 for all ten joints and `joint_velocity_limits` as 5.939e36, which are Isaac Lab's unbounded sim defaults and not the identified actuator limits of the `IdentifiedActuatorCfg` blocks. In consequence `p99 as fraction of ceiling` reads about 1.3e-5 percent everywhere, `fraction above 98 pct of ceiling` is exactly zero for every joint in every run, and `fraction exceeding ceiling` likewise. Every saturation figure in this record has therefore been computed by hand against constants transcribed from `assets/config/sd_brs1_identified_cfg.py`, which are HipYaw 351, HipRoll 500, HipPitch 500, KneePitch 600, AnkleRoll 131 and AnklePitch 262 N·m. The remedy is to dump the actuator's `effort_limit` rather than `effort_limit_sim`.

THE FOOT SEPARATION IS NAMED IN THE WORLD FRAME, per the section above, and the defect is one of INTERPRETATION rather than of representation. A change of basis does not alter a vector, only the tuple of components expressing it, so every rotation invariant may be computed from the world frame components exactly. Verified on this run, the planar norm from world components and from base frame components agree to 5.96e-8 m over 96032 samples and the full 3D norm to 8.94e-8, which is float32 identity, while the two horizontal components taken singly differ by up to 0.4827 m. `feet_distance` at `mdp/rewards.py:581` therefore NEEDS NO CHANGE, its default branch taking a rotation invariant norm at line 630 and its `lateral_only` branch already rotating at lines 622 to 627. `play.py:407` needs no change either, a world frame vector being lossless and `base_quaternion` being dumped beside it in all seven runs. The remedy belongs in `stats.py`, which must rotate before naming components, and belongs there because it then corrects every dump on disk RETROACTIVELY without a replay.

### Corrections to earlier passes

DOUBLE SUPPORT ON RUN 2026-08-03_11-19-11 IS 10.269 PERCENT, not the 7.858 the twenty-fourth pass recorded. Recomputed directly from the dump with the same 1.0 N threshold the pipeline uses, the four runs read 4.432, 2.867, 10.269 and 14.014 percent, and the direct computation agrees with `statistics.npy` to three decimals on all four. Phase 2 still FAILED its 12 percent gate on its own run, by 1.7 points rather than by 4.1. Phase 3 carries it to 14.014 without any change to `NoFlyWithGrace`, `feet_air_time` or the clock, so the gate is met and the mechanism should not be credited, the overlap having been bought by shortening the swing from 0.431 s to 0.395 rather than by pricing the transfer.

THE YAW IS NOW PROPERLY MATCHED AND HAS NOT RECOVERED. The twenty-fourth pass had to allow for an angular tracking std of 0.2124 in the Phase 1 run against 0.2473 in its successor. The Phase 1 and Phase 3 runs both ended at 0.2124 with identical angular command ranges of 0.300, so the allowance no longer applies. Training yaw error 0.4186 against Phase 1's 0.3872 and evaluation correlation 0.468 against 0.529, so Phase 3 is slightly WORSE than Phase 1 on both, and the recovery the twenty-fourth pass recorded is partly attributable to the looser curriculum that flattered the intervening run.

THE YAW DEFICIT IS AN AUTHORITY LIMIT, established here for the first time by binning. The regression slope of achieved on commanded yaw rate is 0.263 on PH3, 0.334 on PH1 and 0.213 on the baseline, and the achieved magnitude averages 0.218 rad/s against a commanded 0.489. Binned by command the machine delivers 0.067 rad/s when asked for 0.1 to 0.3, 0.141 when asked for 0.3 to 0.6, and 0.187 when asked for 0.6 to 0.9. The response is sublinear throughout and flattens near a fifth of a radian per second regardless of demand. The sign is correct on 78.98 percent of steps at commands above 0.3, so the policy knows which way to turn and cannot turn far enough. Phase 5 must be judged on the SLOPE and the ceiling, not on the error, which is dominated by commands the machine cannot meet at any weight.

### What is now in the tree

`rew_foot_clearance` at `cfg/SF/brs_base_env_cfg.py:1005` is `foot_clearance_reward_v3` at weight 10.0 with std 0.03, `pen_foot_landing_vel` at line 1103 is −30.0, and `pen_feet_impact` at line 1156 is −3.0e-2 against 850.0 N. All four match the trained run. `foot_clearance_reward_v2` is left intact at `mdp/rewards.py:311` and now has no callers.

The addition not present in any run analysed here is `pen_ankle_deviation` at line 852, `joint_deviation_l1` at weight −0.2 over `AnklePitch[LR]` and `AnkleRoll[LR]`, whose forecast is recorded above. A commented `rew_knee_flexion` block at line 819 names `knee_flexion_in_swing_v2` and is inert. The straight legged gait that block was staged against no longer exists, so it should be deleted rather than left as a standing invitation.

### Why `keep_ankle_pitch_zero_in_air` fails, decomposed (2026-08-07, added with plan Phase 4)

Three defects compound, and the term's weight is the least of them. Each is measured on run 2026-08-05_07-53-35 and each has a distinct remedy, so the causes are separated here rather than being merged into a single "too weak".

DEFECT ONE, THE TARGET IS THE COORDINATE ZERO AND NOT THE NOMINAL POSE. `mdp/rewards.py:970` takes `torch.abs(asset.data.joint_pos[:, asset_cfg.joint_ids])`. The SD_BRS1 AnklePitch default is 0.2435 rad before randomisation and reads 0.2163 and 0.2599 after it, so an ankle standing exactly at its own default is already 0.2435 rad into the penalty. At `pitch_scale` 0.2 that ankle scores exp(-0.2435/0.2) x 0.8599 = 0.2545 per second, against a ceiling of 0.8599 that the airborne fraction sets, so a perfectly compliant policy collects under a third of the term. The ankle at its 0.454 rad stop scores 0.0888. THE ENTIRE REACHABLE LEVER IS 0.2545 - 0.0888 = 0.1657 per second at unit weight, which is 0.21 percent of the 79.95 positive budget.

Reproduction check. Recomputing the term from the dump with a single sample contact test gives 0.0896 against the 0.0896 logged in `statistics.npy` as 0.0918, the small gap being the two sample contact history OR that the reproduction approximates. The decomposition may therefore be trusted.

Setting the target to the randomised default instead gives 0.2945 on the same gait against the same 0.8599 ceiling, so THE LEVER RISES FROM 0.1657 TO 0.5654 per second at unit weight, a factor of 3.4 obtained from one optional argument and no weight change.

BACKWARDS COMPATIBILITY IS FREE HERE. `keep_ankle_pitch_zero_in_air` has exactly two callers, `cfg/SF/brs_base_env_cfg.py:790` and `cfg/SF/limx_base_env_cfg.py:1148`. The TRON1 SF caller declares `".*_Joint": 0.0` for every joint at `assets/config/solefoot_identified_cfg.py:91`, so for that robot the absolute coordinate and the deviation from default are NUMERICALLY IDENTICAL and it is unaffected whichever way an argument is set. An optional argument defaulting False is therefore rule 4 of `/ws/CLAUDE.md` and not rule 5, and no v2 is warranted.

DEFECT TWO, THE ROLL AXIS IS READ BY NOTHING, and it carries the larger lever. A second instance of the SAME function wired against `AnkleRoll[LR]` reads 0.2498 per second on the Phase 3 gait against the same 0.8599 ceiling, so it forfeits 0.6101 at unit weight, which is 3.7 times the reachable pitch lever. Two properties make this exact. The AnkleRoll default is 0.0 rad, so the absolute target the function already uses is CORRECT for the roll axis and no argument is needed there. And the contact mask has one column per sensor body, so `ankle_pitch * ~contact_filt` at `mdp/rewards.py:971` requires n_joints to equal n_feet, meaning a single instance CANNOT cover four joints against two feet and a second term is the only form the function admits. NO CODE CHANGE IS REQUIRED for the roll coverage, only a second RewTerm.

DEFECT THREE, THE WEIGHT, which must be set last because the first two change what a weight buys by 3.4x on one axis and bring the other into existence. Against a positive budget of 79.95, a term reaches one, two and five percent of it at levers of 0.8 per unit weight when weighted 1.25, 2.5 and 6.2.

`pitch_scale` SENSITIVITY, on the deviation from default target, lever per unit weight between the PH3 gait and a nominal swing ankle. scale 0.1 gives 0.757, scale 0.2 gives 0.565, scale 0.3 gives 0.440, scale 0.5 gives 0.301. A tighter kernel offers a larger lever and is harder to satisfy, and 0.2 as configured is a reasonable point.

A DESIGN NOTE ON PHASE GATING, which decides which term should carry the weight. `joint_deviation_l1`, the form now running as `pen_ankle_deviation`, charges in stance as well as in swing. The measured stance ankle pitch is 0.378 rad against a swing value of 0.453, so a phase agnostic penalty charges the push off at more than half the rate it charges the parked swing, and suppressing an ankle's stance contribution is not the intention. The airborne gated form prices only the swing, which is where the defect is. The swing gated pair should therefore carry the weight and `pen_ankle_deviation` should be kept small or withdrawn once it is wired, and the running experiment will inform which, since it is measuring exactly the phase agnostic form.

### Phase 4 IMPLEMENTED, 2026-08-07, verified against the run 2026-08-05_07-53-35 dump

Plan `../plans/GAIT_EFFICIENCY_PLAN.md` section 4.4a records the implementation in full and this entry records the outcome against the decomposition of the preceding subsection, which forecast the levers before the code existed. Two edits carry the phase. `keep_ankle_pitch_zero_in_air` gained an optional `use_default_offset` at `mdp/rewards.py:933`, branching at `:990` with the original expression preserved character for character in the `else` arm, and the SD_BRS1 configuration sets it on the pitch term at `cfg/SF/brs_base_env_cfg.py:833` at weight 1.5 and adds a roll instance of the same function at `:858` at the same weight.

The verification reimplemented the reward over the dumped `joint_positions` and `feet_contact_forces` rather than trusting the forecast, and the contact test survives the frame defect of the twenty fifth pass because it reads a norm. The pitch term measures 0.0873 per second at the coordinate zero and 0.2871 at the default, a factor of 3.3 against the 3.4 forecast, and the roll term measures 0.2359 where nothing read the axis at all. The ceiling set by the airborne fraction measures 0.8385, so the pair leaves 1.731 per second of lever at the weights set, which is 2.17 percent of the 79.95 positive budget and lands where the decomposition recommended. The forecast figures of 0.0896, 0.2945 and 0.2498 against a ceiling of 0.8599 stand slightly above these, the dump sampling contact once per control step where the reward reads a history buffered at the simulation step, and preserving only environment zero's randomised default where the reward reads each environment's own, both of which move every variant together and leave the ratios intact.

Three findings arose in implementation that the decomposition did not carry. The policy cannot observe the absolute joint coordinate at all, the action term offsetting from the default at `cfg/SF/brs_base_env_cfg.py:156` and both observation groups reading `joint_pos_rel` at `:190` and `:231`, so the uncorrected term named its target in a frame neither the actor nor the critic sees, which is a sharper statement of the defect than the arithmetic alone. The startup event `joint_offsets` at `:568` adds a uniform sample on the interval from −0.05 to 0.05 radians to every default, so the claim that the ankle roll default is zero holds of the configured value and not of the runtime one, and the roll term consequently charges each environment for a nominal displacement it can neither see nor remove, worth at most 22 percent of that term in the worst environment and nothing in the mean. The alternative was measured at 0.2193 against 0.2359 and declined, since targeting the randomised default asks the sole to hold a random tilt, and the residue is recorded as a defect left standing per rule 6 rather than corrected.

The articulation ordering was confirmed rather than assumed, since the reward broadcasts one contact column per foot against one deviation column per joint and would charge each foot against the opposite ankle in silence were the orders to disagree. The dump gives `AnkleRollL` and `AnkleRollR` at indices six and seven against `Link6L` and `Link6R` at eleven and twelve, every pair numbered left before right, so the ascending resolution of `SceneEntityCfg` pairs each ankle with its own foot and `preserve_order` is unnecessary on either term.

Two divergences from the plan and one stale claim are recorded. The implementation kept the regular expression `"Link6[LR]"` where the plan wrote an explicit list, the two resolving identically. It adopted the plan's `force_threshold` of 1.0 on the pitch term, which moves that term off the function default of 2.0 and is the one behaviour change beyond the three defects, taken for consistency with the roll instance and with every other contact gated term in the file, one newton against a stance load near five hundred being sensor noise in both directions. And `pen_ankle_deviation` was found already withdrawn at `cfg/SF/brs_base_env_cfg.py:915`, so the tree no longer carries the phase agnostic experiment the preceding subsection describes as running, and the swing gated pair carries the ankle weight alone.
